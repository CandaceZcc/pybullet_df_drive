# 阶段三测试反馈解决

> 文档用途：记录阶段三 GUI 人工测试反馈、当前证据、候选方案、实施顺序和待确认决策。
> 编制日期：2026-07-30
> 当前状态：方案评审中；本文不是“问题已修复”的证明，不替代真实 GUI/eCAL 门禁。
> 实施边界：用户确认 RTK 三点语义和 LiDAR 输出层级前，不修改 Protobuf/eCAL 对外合同。

## 1. 当前结论

1. 卡顿不能只归因于“电脑配置低”或“PyBullet 渲染慢”。当前 GUI 会话同时运行 240 Hz 物理、前后各 2880 射线的 10 Hz LiDAR、地形/接触查询、跟随相机、Qt Dashboard、Matplotlib 和日志。它们共享主机资源，其中若干操作还位于同一物理主线程，必须先分段计时才能下最终结论。
2. 当前机器为 4 核 8 线程的低功耗移动处理器，内存容量尚可；混合显卡是否真正用于 PyBullet OpenGL 仍未确认。代码有明确的轻量化空间，不能把卡顿简单归咎于用户设备。
3. 高尔夫场地使用三角化 heightfield 碰撞面，物理接触和 LiDAR 射线查询都比平面更贵；因此“只优化显卡渲染”不一定能解决高尔夫卡顿。
4. 图表的刷新频率已经受限，但每次可见折线绘制仍执行 `relim()` 和 `autoscale_view()`。坐标范围随新样本改变是图表浮动的一个确定原因；卡顿造成的非均匀采样会进一步放大观感。
5. 当前 LiDAR 线协议实际上已经输出以每台雷达为原点的逐点三维 `x/y/z`。Dashboard 为便于俯视观察又把点变换到 `base_link` 并只画 `x/y`，所以界面没有直接呈现雷达坐标系下的三维结果。
6. “障碍物三维位置”必须区分原始点云与障碍物级中心/包围盒。真实物理 LiDAR通常输出命中表面点，不直接知道障碍物中心；若需要中心位置，应作为独立感知或仿真真值输出，不能冒充原始雷达回波。

## 2. 反馈追踪

| 编号 | 反馈 | 优先级 | 当前判断 | 状态 |
|---:|---|---|---|---|
| 1 | GUI 卡顿严重，图表波动 | P0 | 主线程负载、渲染路径、LiDAR、相机和图表共同作用；需要正式分段 profiling | 待测量并优化 |
| 2 | RTK 改为三点输出 | P0 合同变更 | 当前仅主天线位置和双天线 yaw；建议扩展为固定角色的三参考点 | 等待点位语义确认 |
| 3 | 高尔夫场地卡顿更严重 | P0 | heightfield 对碰撞与射线都更贵；不能预先认定只是视觉渲染 | 待 flat/golf 隔离测量 |
| 4 | LiDAR 输出障碍物三维相对位置 | P1/待定合同 | 原始逐点 `x/y/z` 已存在；障碍物中心属于另一输出层 | 等待真实传感器型号和输出要求 |

## 3. 当前 Linux 设备证据

2026-07-30 在当前 Linux 端只读采集到：

| 项目 | 结果 | 判断 |
|---|---|---|
| CPU | Intel Core i7-1165G7，4 核 8 线程，最高 4.7 GHz | 单核性能尚可，但同时承担 240 Hz Python 主循环、Qt、Matplotlib 和传感器时余量有限 |
| 内存 | 15 GiB；采样时约 8.9 GiB available | 容量不是当前首要瓶颈 |
| Swap | 4 GiB，总使用约 1.1 GiB；采样窗口 `si/so=0` | 有历史换页，但采样时没有持续 swap 抖动 |
| 核显 | Intel Iris Xe，`i915` | 很可能负责桌面显示，但需在真实桌面会话确认 OpenGL renderer |
| 独显 | NVIDIA GeForce MX350，PRIME `on-demand` | 模块已加载；当前 Codex 沙箱没有 `/dev/nvidia*`，因此 `nvidia-smi` 失败不能证明宿主驱动损坏 |
| CPU 策略 | governor=`powersave`，EPP=`balance_performance` | 可能影响持续高频负载，需要在接电与性能模式下做一次对照 |
| 当前 load | 约 `3.2`，主机有多个登录用户 | 不能作为安静主机性能基线 |

真实桌面测试前应在启动 GUI 的同一个终端记录：

```bash
glxinfo -B | rg 'OpenGL vendor|OpenGL renderer|OpenGL core profile version'
prime-select query
nvidia-smi
```

判读规则：

- 若 renderer 为 `Mesa Intel Iris Xe`，当前会话走核显。
- 若 renderer 为 `NVIDIA GeForce MX350`，当前会话走独显。
- 若宿主终端的 `nvidia-smi` 也失败，再排查驱动；Codex 沙箱中的失败不作为宿主故障证据。
- 应分别用默认启动和 PRIME offload 启动做 A/B，不先假定 MX350 一定更快。

## 4. 卡顿的现有机制与测量方案

### 4.1 当前一秒内的大致工作量

| 子系统 | 当前行为 | 可能影响 |
|---|---|---|
| 物理 | 240 次 `stepSimulation` 及控制/状态读取 | 主循环基本负载 |
| 企业 LiDAR | 前后各 `2880 × 10 Hz`，共约 57,600 ray/s | CPU、Bullet 碰撞查询和 Python 点对象构造 |
| 跟随相机 | 当前每个物理步调用 `resetDebugVisualizerCamera` | 最高约 240 次/s，明显高于人眼需要 |
| Qt 事件 | 每个物理循环调用 Dashboard `processEvents` | 保证响应，但调用频率过高 |
| 障碍物表 | 主循环每步进入快照刷新入口，内部再限频 | 仍存在不必要的函数/快照检查 |
| 遥测 CSV | 每个物理步组装完整行并写入 Python 缓冲文件 | 240 行/s 和较宽字段序列化 |
| 图表 | 可见页最高 2 Hz；每次重新计算数据边界并自动缩放 | 高点数页绘制成本和坐标轴浮动 |
| LiDAR 图 | 合并前后成功帧的全部命中点并交给 Matplotlib scatter | 命中点多时影响 Qt 绘制 |

### 4.2 必须先加的轻量 profiler

新增显式 `--profile-runtime`，默认关闭。只使用 `perf_counter_ns()` 记录耗时，不改变物理、消息或绘制语义；每秒聚合一次，不逐帧打印。至少分开统计：

- deadline lateness、`sim/wall`、物理步耗时；
- 企业 LiDAR 前/后批量射线和点云编码；
- 地形探测、接触/遥测读取；
- 跟随相机更新；
- Qt 事件、Dashboard 快照、障碍物表、Matplotlib draw 完成耗时；
- CSV 与接口日志入队/写入；
- 每段 p50、p95、p99、max、调用次数和总占比。

profiler 结果写独立 JSON，不写进 240 Hz CSV，避免测量工具本身制造卡顿。

### 4.3 隔离矩阵

每项严格串行、同一车型、同一驾驶脚本、相同 20 个障碍物、相同持续时间；开始前扫描并关闭其他测试负载。

| 组 | 场地 | Dashboard | 企业接口 | 目的 |
|---|---|---|---|---|
| A | flat | 关 | 关 | PyBullet GUI、车辆和相机基线 |
| B | golf | 关 | 关 | 仅增加 heightfield，定位地形物理/渲染差值 |
| C | flat | 开 | local | 增加 Qt、图表、企业 LiDAR和日志 |
| D | golf | 开 | local | 复现用户完整卡顿 |
| E | golf | 开 | local，临时仅关闭接口日志 | 判断日志占比，不作为交付模式 |
| F | golf | 开 | local，应用优化 | 与 D 使用同一 workload 做最终比较 |

还需补一组不可见渲染的 DIRECT 对照：若 DIRECT 中 `golf-flat` 差值已经明显，主因不是显卡绘制；若 DIRECT 接近而 GUI 差距很大，再检查 heightfield 可视网格和 OpenGL。

### 4.4 建议验收指标

以下是待用户确认的工程目标，不通过降低接口规格达成：

- GUI 完整负载 `sim/wall >= 0.98`，与真实 eCAL 门禁一致；
- 100/10 Hz 话题、每雷达 2880 射线、20 个障碍物和日志合同保持不变；
- Dashboard/键盘事件不出现超过 100 ms 的处理空窗；
- 相机视觉更新目标至少 30 Hz，物理仍为 240 Hz；
- 可见图表 draw 的 p95 小于 100 ms，坐标轴不因普通新样本每次收缩/放大；
- transport/logger drop 为 0，性能优化不得复制旧物理状态冒充新帧。

## 5. 性能优化顺序

### 5.1 P0：不改变业务合同的主循环降载

1. 将跟随相机从 240 Hz 独立限频到 30 Hz；车型/场地重建后立即刷新一次。
2. Qt 事件泵设独立上限（建议 60 Hz），键盘输入仍在 50 ms 内响应。
3. 障碍物表快照只在障碍 revision 变化或 10 Hz deadline 到期时构造，不在每个物理步进入完整路径。
4. 遥测采集拆分为“控制安全所需的逐帧最小状态”和“GUI/CSV 所需的低频扩展状态”；任何降频前先由 profiler 证明其占比。
5. 保留共享绝对 deadline；发生超期时不增加固定 sleep，也不突发补绘 GUI。

### 5.2 P0：图表稳定和轻量化

1. x 轴使用固定最近 20 s 时间窗，不随每次新增点重新计算整体范围。
2. 对已知物理范围的图使用稳定 y 轴或带滞回的范围：超界立即扩张，连续数秒后才允许收缩。
3. 100 Hz 轮组曲线只在显示层执行 min/max envelope 或 LTTB 降采样；原始接口日志、消息和统计不删点。
4. LiDAR Dashboard 只对显示副本做体素/屏幕像素降采样并设置显示点数上限；eCAL 原始点云保持完整。
5. 只有当前页签提交 Matplotlib draw；车型/场地 generation 变化时仍立即清空旧代数据。

推荐稳定范围：轮组频率保留目标线和 `0..120 Hz` 初始范围，传感频率使用 `0..15 Hz`，转向反馈以机械限位 `±0.55 rad` 为基准。RTK世界位置不能固定死，使用只扩张/慢收缩的滞回范围。

### 5.3 P1：PyBullet GUI 轻量化

按单项开关 A/B，不一次性混改：

- 默认关闭阴影和不需要的 PyBullet debug GUI 面板；Dashboard 已提供正式控制区。
- 验证 `COV_ENABLE_RENDERING` 配合 `COV_ENABLE_SINGLE_STEP_RENDERING` 的 30/60 Hz 单次渲染路径；若出现窗口冻结或输入问题则回退。
- 保留硬件 OpenGL 为首选，不把 Tiny Renderer 当作默认 GUI 加速方案。
- 在宿主桌面比较 Intel 与 PRIME NVIDIA renderer，记录同一 workload 的 profiler JSON；不凭显卡名称选择。

### 5.4 P1：日志和对象分配

- 先测量 CSV 行转换和写入占比；若显著，再使用有界批量 writer，关闭时仍保证 flush/fsync 和完整终态。
- 保留当前 LiDAR compact indexed-hit 路径；避免重新构造 5760 个 miss 对象。
- Dashboard 点云副本和接口完整点云继续分离，headless 路径不创建 Matplotlib 数据。

## 6. 高尔夫场地专项

### 6.1 已确认差异

- flat 是简单平面；golf 使用 `96 × 144 = 13,824` 个高度样本。
- 对应规则网格约有 `(96-1) × (144-1) × 2 = 27,170` 个三角形。
- 高尔夫碰撞体启用 `GEOM_CONCAVE_INTERNAL_EDGE`，用于改善内部三角边接触。
- LiDAR 与贴地查询面对的是 heightfield 碰撞结构，而不是无限平面。
- 列数曾从 96 增到 144，以覆盖 flat 的完整 20 m X 范围；不能直接降回 96，否则会重新引入场地切换回滚问题。

因此高尔夫卡顿可能来自三部分：可视三角网格、轮地接触/求解、LiDAR 对 heightfield 的批量射线。必须通过 DIRECT 与 GUI、LiDAR 开与关的分段计时区分。

### 6.2 推荐优化

1. 第一选择：碰撞 heightfield 保持现有分辨率，单独构建更低面数的视觉 LOD；视觉网格不参与碰撞和 LiDAR。
2. 第二选择：若 profiler 证明射线/碰撞才是主因，再实验较低碰撞分辨率，但保持场地物理尺寸不变，并以高度误差、法向误差、出生点、20 障碍保留和车辆接触稳定性作为硬门禁。
3. 不先删除 `GEOM_CONCAVE_INTERNAL_EDGE`；只有接触回归证明无抖动、卡边或穿透时才考虑。
4. 高尔夫地形只在 seed/relief 改变时重新生成；普通物理帧不得重算高度数组或视觉网格。

## 7. RTK 三点输出方案

### 7.1 当前合同

当前 `RtkState` 只有：

- 一个主天线世界位置 `main_x/main_y/main_z`；
- 主天线指向副天线的 `baseline_yaw_rad`；
- 10 Hz 仿真时间戳。

它不是三点输出。修改将影响 proto、生成代码、codec、runtime、Dashboard、日志、eCAL peer/verifier、场景传感器外参和大量契约测试。

### 7.2 推荐的固定三角色语义

RTK 严格说是定位设备，不是雷达。为了让四种车型使用同一企业消息，推荐始终输出三个角色：`LEFT`、`CENTER`、`RIGHT`，顺序和枚举都固定。

二轮差速车：

- `LEFT`：左驱动轮轴心世界坐标；
- `RIGHT`：右驱动轮轴心世界坐标；
- `CENTER`：左右驱动轮轴心的三维中点。

这样 `df_front/df_mid/df_back` 会自然使用各自 `drive_center_x = +0.22/0/-0.22 m`，不能都硬编码到 `base_link` 原点。

四轮主动转向车的推荐解释是：

- `LEFT`：前左、后左轮轴心的三维中点；
- `RIGHT`：前右、后右轮轴心的三维中点；
- `CENTER`：四个轮轴心的几何中心。

这恰好仍是三个输出点，并且比从 `base_link` 随意平移更能反映实际模型几何。这里需要用户确认：原反馈中的“水平左右平移各两个点”是否就是“左右两组车轮各聚合成一个点”；若实际要求中心加左侧两个、右侧两个，则总数会是五点，与“三点输出”冲突。

所有点应在同一个 10 Hz timestamp 下原子采样，单位为 m，默认输出世界坐标。点角色不能依赖数组下标猜测，必须有稳定 enum。

### 7.3 Protobuf 方案比较

| 方案 | 优点 | 缺点 | 建议 |
|---|---|---|---|
| A. 在现有 `RtkState` 增加 `repeated RtkReferencePoint` | 保持 `/sim/rtk/state` 和旧字段可读；旧消费者忽略新增字段 | 过渡期存在旧主天线和新三点两套字段 | 推荐 |
| B. 直接替换旧字段为三个 point | 合同最干净 | 立即破坏所有旧日志、peer和消费者 | 不推荐直接采用 |
| C. 新增第七个 RTK 三点话题 | 旧合同完全不变 | 破坏阶段三固定六话题边界，状态/频率/日志更复杂 | 仅外部系统明确要求时采用 |

推荐 A：新增点结构和固定角色，旧字段标记为过渡兼容，不重新解释旧字段含义。待所有消费者迁移后，再单独决定是否移除旧字段。

候选逻辑结构如下，字段号需在正式设计评审后冻结：

```text
RtkReferencePoint:
  role = LEFT | CENTER | RIGHT
  x_m, y_m, z_m

RtkState:
  timestamp_ns
  existing main_x/main_y/main_z/baseline_yaw_rad
  reference_points[3]
```

### 7.4 Dashboard 建议

不建议在一张时间图里同时画九条 `left/center/right × x/y/z` 曲线。推荐：

- `RTK位置` 保留 CENTER 的 x/y/z 时间曲线；
- 页面增加最新三点数值表和车体俯视几何小图；
- `RTK航向` 继续显示 yaw，并明确它如何由三点/车体前向求得；
- 保存图和 eCAL 原始消息仍包含全部三点。

### 7.5 RTK 验收

- 四车型每帧恰好三个唯一角色，全部有限且同 timestamp；
- 二轮 LEFT/RIGHT 精确匹配实际轮轴中心，CENTER 等于两者中点；
- 四轮 LEFT/RIGHT 分别等于同侧两轮中心均值，CENTER 等于四轮几何中心；
- flat、slope、golf 上 x/y/z 与 `getLinkState` 世界变换误差不超过现有 `1e-4` 门槛；
- codec、日志、Dashboard、10 Hz、真实 eCAL peer/runtime 双向序列和重建 generation 全部更新；
- 先提供 wire 兼容测试，再改生产生成逻辑。

## 8. LiDAR 三维障碍物输出建议

### 8.1 当前已经具备的原始数据

当前每个 `LidarPoint` 已有：

- `x/y/z`：以对应前/后 LiDAR 安装坐标系为原点的三维命中位置；
- `offset_time_ns`、`line`：扫描顺序；
- `reflectivity`、`tag`：当前仿真的固定类别语义；
- 点云级 `timebase_ns`、`frame_id`、`point_num`、`lidar_id`。

这套字段接近真实 LiDAR point cloud，而不是障碍物列表。Dashboard 当前把命中点变换到 `base_link` 后只画俯视 x/y，所以应先增加坐标系说明、可选 3D 视图或选点详情，不能据界面缺少 z 判断线协议没有三维数据。

### 8.2 三种输出层级

| 方案 | 输出内容 | 真实性 | 计算量 |
|---|---|---|---|
| 原始点云 | 每条射线的命中表面 x/y/z | 最接近物理 LiDAR | 当前已有 |
| 仿真真值障碍物列表 | 直接读取 PyBullet 障碍物中心、尺寸、速度，再变换到雷达坐标系 | 适合验证规划，但不是雷达测得结果 | 低 |
| 点云感知结果 | 对原始点云做降采样、去地面、聚类、包围盒和跟踪 | 更接近真实感知流水线 | 高，且会加重当前卡顿 |

推荐保留原始点云为正式 LiDAR 输出；若业务确实需要“每个障碍物一个三维坐标”，新增独立、明确标注 `ground_truth` 的障碍物检测消息，不要把中心点塞进 `LidarPointCloud`。

候选障碍物检测字段：

```text
timestamp_ns
frame_id                 # lidar_front 或 lidar_rear
source_lidar_id
track_id                 # 稳定逻辑 ID，不使用 PyBullet body id
center_x/y/z_m           # 雷达坐标系
size_x/y/z_m             # 三维包围盒
velocity_x/y/z_mps       # 雷达坐标系相对速度
category                 # static/moving/unknown
confidence
is_ground_truth = true
```

真实 LiDAR 只能看到可见表面，遮挡时未必能推断物体中心；因此真值中心和点云聚类中心必须用不同消息名、topic 和状态标记。

### 8.3 等待用户带回的真实设备信息

冻结输出前需要确认：

1. 物理 LiDAR 厂商和型号；
2. SDK/ROS 消息名称及字段；
3. 坐标轴方向、单位和 `frame_id`；
4. 时间戳来源、同步方式和一帧定义；
5. 是否输出多回波、反射率、ring/line、return type；
6. 用户要的是原始点云、厂商检测目标，还是项目自建障碍物列表。

若目标设备是文档中曾参考的 Livox MID-360，当前 `offset_time + x/y/z + reflectivity + tag + line` 结构已经接近其自定义点消息；但当前固定 16×180 扫描、类别 tag 和固定反射率仍是仿真约定，不能声称完全复现硬件。

## 9. 建议执行顺序

| 阶段 | 内容 | 前置条件 | 完成证据 |
|---|---|---|---|
| A | profiler 与 flat/golf 隔离矩阵 | 无 | 分段 p50/p95/p99、sim/wall、CPU/GPU renderer 记录 |
| B | 相机/Qt/障碍表限频，图表稳定轴和显示降采样 | A 找到占比 | 同 workload 前后 profiler 对比，业务频率和 drop 不回归 |
| C | 高尔夫视觉 LOD；必要时再实验碰撞分辨率 | A 证明 heightfield 是主因 | 地形误差、接触、LiDAR、20 障碍和场地事务门禁 |
| D | RTK 三点 RED/GREEN | 用户确认三点定义和兼容方案 | 四车型三地形、proto/codec/eCAL/Dashboard 全链路 |
| E | LiDAR/障碍物输出设计 | 用户提供物理设备格式和输出层级 | 单独接口设计获批，不与原始点云混淆 |
| F | 严格串行 GUI、DIRECT、非 eCAL 全量和真实 eCAL | A-E 相应实现完成；真实 eCAL 另行授权 | 新旧性能对比、四组 GUI、全量 pytest、两车型 eCAL |

性能 A-C 应先于 RTK 大范围协议改动：先让测试环境稳定，才能区分新合同带来的回归和旧卡顿。

## 10. 待用户确认

1. **RTK 四轮三点：**是否采用“左侧两轮中心的均值、四轮几何中心、右侧两轮中心的均值”这三个点？如果不是，请明确五点还是固定横向偏移三点。
2. **RTK 兼容：**是否接受在现有 `/sim/rtk/state` 中新增 repeated 三点、保留旧字段一个过渡周期？
3. **LiDAR 输出：**需要原始点云、仿真真值障碍物列表，还是点云算法检测结果？本文推荐前两者分话题并存。
4. **物理设备：**请补充厂商、型号和一份真实消息/字段样例，再冻结 LiDAR 协议。
5. **GUI 性能目标：**是否接受以完整负载 `sim/wall >= 0.98`、事件空窗不超过 100 ms、相机至少 30 Hz 为第一版验收线？

用户确认以上决策后，再编写逐文件、逐测试的实施计划；在此之前只允许 profiling 和不改变外部合同的性能实验。
