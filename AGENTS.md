# PyBullet 仿真项目协作规则

## 可审查性与范围

- 常用、重要文件以简短中文头注释说明职责；重要函数及不直观关键段落写简短中文注释，不逐行复述。
- 用户是 PyBullet 初学者：完成较复杂的 PyBullet、坐标系、传感器或进程操作后，简短说明原理和结果。
- 优先复用项目既有依赖、脚本、测试模式和 `references/` 中可运行的成熟实现；新增依赖前确认现有方案不够用。
- 采用满足验收条件的最小实现：禁止无关重构、预留式抽象、额外配置层或兼容层；无公开接口、调用方或明确需求时，不新增兼容、fallback 或迁移路径。
- 删除旧代码、路径或生成物前，检查调用方、测试、锁文件、交付流程和并行任务依赖；范围不明先询问。每项行为变更须有与风险相称、可运行的验收命令。

## TDD 与验证

- 重要功能、新公共接口、并发/跨进程行为、实质性故障修复和 P0 门禁，必须线性执行：聚焦测试 RED（仅因目标行为缺失）→ 最小实现 GREEN → 必要重构后重跑聚焦测试与直接相关回归；最终报告测试名、RED/GREEN 命令与结果及实际相关回归。
- 纯文档、注释、格式、测试移动、pytest marker、测试制品分类、无行为配置整理和已有覆盖的微小等价重构不制造 RED；只做相应静态、收集或聚焦验证。
- 验证逐级停止：文档/计划仅路径、链接、语法或格式检查；测试移动/marker/等价维护做收集和受影响测试；普通生产行为做 TDD 聚焦测试与直接模块回归；公共协议、eCAL 跨进程、并发、构建/发行边界再做相关集成与外部门禁；正式阶段收口或发行才在子任务合并后运行一次 README 默认完整回归。
- 成功的同一快照完整回归可复用；真实 eCAL、GUI、离线制品、C++ 构建和发布门禁仅在对应边界变化或阶段验收时运行。环境/制品缺失时报告原因与风险，不恢复大型历史构建树；测试失败先定位根因，不盲目安装、升级或删除依赖。

## 阶段、审查与委派

- “大阶段”仅指用户宣布进入验收、正式计划里程碑全部完成，或一批公共协议/跨进程架构/发布流程改动准备交付；文档收口、清理、测试搬家、marker、判据合并、单个辅助函数和审查修复均不是独立大阶段。
- 每个大阶段只做一次独立只读六维审查（需求完整性、逻辑正确性、边界情况、代码质量、测试覆盖、实际运行结果；不适用写 `N/A`）。连续子任务合并审查；优先复用同快照 diff 与验证证据，不重复完整回归。审查者不改代码；修复后做针对性复验，除非实质改变架构、公共接口或验收范围。
- 单个重要 TDD 单元遵守 RED → 最小实现 → GREEN → 相关回归。不同模块、只读调查和独立测试可并行，但不得并改同一文件、构建根或功能链。
- 仅在用户明确要求或任务确有必要时，使用额外技能、联网和 agent 委派；选择最小数量与最小上下文，避免为常规小任务产生额外指令、输出或协作开销。
- 用户明确要求委派时，主 agent 说明各子任务的文件边界、禁止修改范围、验收命令和交付结果；子 agent 只处理隔离小任务，主 agent 集成并运行所需回归，完整回归按上节仅一次。审查 agent 必须只读；未经用户明确要求，不创建公开仓库、公开包、外部发布或上传制品。

## 磁盘与制品

- `build/`、`results/`、日志、点云、录制包、离线包和 `references/repos/` 可能很大；每平台/配置/目标只保留一个当前构建根和一个可复用 canonical cache，禁止时间戳完整构建树、重复源码归档、虚拟环境副本和重复 cache。
- 预计写入超过 5 GiB 时，先说明内容、估算、不能复用的原因和收尾方案并等待确认；本任务制品超过 20 GiB、磁盘余量低于 15%、出现重复完整构建根或无界增长时立即停止报告。
- 不要逐命令删除可复用缓存；在任务或阶段结束时清理本次失败构建、临时副本和无用中间物。删除现有超过 1 GiB 目录前，列出精确路径、大小、依赖检查和理由；禁止 broad delete、递归通配符和删除来源不明文件。
- 仅在创建或清理至少 1 GiB 时，最终报告新增、保留、删除和剩余空间；普通源码/文档改动不做全盘扫描。

## Git 提交

仅在用户明确要求时 `git commit` 或 `push`。提交概述最多三条数字分点，每条不超过 10 字；大阶段加 `阶段n: ` 前缀。
