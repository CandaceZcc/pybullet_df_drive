1. 写代码时，比较重要的函数需要写简短中文注释，每个常用、重要的文件开头需要有头注释来简述这个文件，方便我审查时知道这段是什么意思

2. 函数内重要段落也可以有简短中文注释

3. 由于我是pybullet的初学者，在你写完代码之后，有些较为复杂的操作需要你输出文本告诉我

4. 尽量参考reference里的仓库，这些都是可直接运行的较为成熟项目，很多东西能拆出来借鉴

5. 每做完一个大阶段后：启动一个独立审查线程，不要直接修改代码，只负责从需求完整性、逻辑正确性、边界情况、代码质量、测试覆盖和实际运行结果六个方面验证，如果有修改的需要再修改代码。

6. 当我提出需要git commit, push时，提交的概述要求：数字分点（1. 2. 3. 但不超过三个）加上不超过10个字的简述，描述此次更新做的什么；如果在执行大阶段则加上“阶段n: ”的前缀

7. ## Test-Driven Development (TDD)

对于重要的功能、新的公共接口、并发/跨进程行为、故障修复和 P0 门禁相关改动，如果不是sol模型下自动启用TDD，则必须采用 TDD：

1. 先增加或修改一个能表达目标行为的测试，并在实现前运行它。
2. 记录或报告该测试的 RED 结果：它必须因目标行为尚未实现而失败，
   不能因为环境、导入错误或无关失败而失败。
3. 只做使该测试通过的最小实现，并重新运行，报告 GREEN 结果。
4. 如需重构，重构后重跑该测试及相关回归测试。
5. 最终报告必须包含：测试名称、RED/GREEN 命令与结果、相关回归命令与结果。

以下改动通常不需要机械执行 TDD：纯文档、纯格式调整、无行为变化的注释，
以及已有充分测试覆盖且只做等价重构的微小改动。即使不使用 TDD，
也必须说明原因并运行恰当的验证。

TDD 不替代阶段完成后的独立六维审查。每个大阶段仍须以只读方式审查：
需求完整性、逻辑正确性、边界情况、代码质量、测试覆盖和实际运行结果。
审查者不得直接修改代码；发现问题后再由实现流程修复并验证。