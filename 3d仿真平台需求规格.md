# 3D 移动机器人仿真平台需求规格

> 文档状态：当前需求基线
>
> 初版日期：2026-07-16
>
> 架构修订：2026-08-10
>
> 适用目标：一台 Ubuntu 24.04 amd64 电脑

## 1. 文档边界

本文只回答“最终必须具备什么能力”。阶段四的组件关系、协议细节、安装事务和
验收数字以
[`docs/superpowers/specs/2026-07-31-stage4-mid360-ecal-cpp-delivery-design.md`](docs/superpowers/specs/2026-07-31-stage4-mid360-ecal-cpp-delivery-design.md)
为准；已完成和未完成状态以各阶段交付报告为准。

`3d仿真需求.md` 是历史原始输入，不再定义当前 LiDAR、RTK、通信或阶段四范围。
历史上使用的前后双 LiDAR、双天线 RTK 和自动导航要求已经被本次用户确认的
阶段四范围取代。

本文的“必须”均是交付要求。“内部诊断”仅用于开发、测试和日志分析，不扩大
Dashboard 或 eCAL 公共接口。

## 2. 产品目标

建设一个轻量的单机移动机器人仿真平台：PyBullet 提供唯一物理真值，用户可
切换车型、场地和障碍物，通过 GUI 或 headless 模式运行；正式实时接口使用
eCAL Protobuf v2，并提供 C++ 订阅、命令、记录、回放和导出工具。

最终系统必须满足：

1. 在一台 Ubuntu 24.04 amd64 电脑原生运行，不依赖 Docker、集群、数据库或
   第二台机器。
2. GUI、Dashboard、PyBullet、eCAL 和记录链路可同时运行；headless 模式可
   执行自动测试和批量仿真。
3. 安装只交付一个可执行 `.run` 文件；安装时允许联网下载依赖并请求 sudo。
4. 安装完成后的正常仿真、记录、回放和导出不主动访问网络。
5. 保留现有车型、场地、障碍物、人工驾驶和 Dashboard 功能，不以轻量化为由
   合并物理进程、删除 C++ 工具或改变公开 topic。

## 3. 明确非目标

当前交付不包含：

- 自动导航、全局或局部路径规划、动态避障算法和导航状态机。
- SLAM、定位融合、多机器人协同、车队调度和云端仿真。
- ROS 2 作为 Simulator 与 eCAL 之间的必经链路。
- 真实 GNSS/RTK 解算过程，以及真实 MID-360 的光学、电气、雨雾、噪声和
  多回波数字孪生。
- 连续柔性履带、履带土壤高保真接触或旧履带模型兼容层。
- 离线依赖缓存、断网构建、双根字节复现和跨机器证据事务。

这些非目标不能进入验收门槛，也不能通过预留接口扩大本期工程量。

## 4. 单机运行架构

最终运行形态必须保留以下进程边界：

- Python/PyBullet Simulator：唯一物理世界和传感器真值生产者。
- C++ Command：唯一正式轮控 publisher。
- C++ Subscriber/SDK：只读消费和外部 C++ 接入。
- C++ Recorder：无损记录正式 topic，不阻塞物理主线程。
- Replay/Export：离线读取完整会话并回放或导出。
- 可选 ROS 2 Jazzy Bridge/RViz2：只做下游显示适配。

Python 编排器只负责本机启动、状态、人工目标和有序停止。实时数据走 eCAL v2；
本机控制面使用受权限保护的 Unix socket。系统不得为当前单机需求引入远程服务、
消息中转层或第二套运行时编排框架。

正式模式初始化真实 eCAL 失败时必须退出，不能静默回退到本地替身。
LocalTransport 只允许用于单元测试和显式开发模式。

## 5. 机器人模型

必须提供四种机器人：

| 标识 | 驱动与转向 |
|---|---|
| `df_front` | 前置左右差速驱动，后部支撑轮 |
| `df_mid` | 中置左右差速驱动，前后支撑轮 |
| `df_back` | 后置左右差速驱动，前部支撑轮 |
| `active_steering_4wd` | 四轮独立驱动，左右前轮主动转向 |

模型要求：

- `tracked_proxy` 和旧履带运行入口不得恢复。
- 关节通过语义名称解析，控制器不得依赖当前 URDF 的数字索引。
- 差速车接收左右驱动轮命令；主动转向车接收四个驱动轮速度和两个前轮转向
  速度，并执行机械限位。
- 轮态必须读取 PyBullet 实际关节反馈，不能直接回显命令。
- 阶段四车型只保留一个位于车体几何中心的 `lidar_link`，并提供固定
  `LEFT/CENTER/RIGHT` RTK 几何。

## 6. 场地与障碍物

### 6.1 场地

必须支持：

- `flat`：连续平面。
- `slope`：高位平地、坡段和低位平地组成的连续斜面。
- `golf_heightfield`：可由随机种子和起伏参数确定性复现的连续高尔夫地形。

Dashboard 必须能在运行期显式应用车型和场地。场景修改只能由物理主线程执行：
新世界成功后才替换旧世界；失败必须保留或回滚到上一个有效世界，不能留下无
地面、重复刚体或引用已删除机器人状态。

场景必须支持版本化导入和导出，内容覆盖车型、场地、障碍物、随机种子和传感器
安装信息。v1 场景由冻结读取器显式转换到 v2，并报告双 LiDAR/双天线到新语义
的变化；未知且不能无损转换的字段必须拒绝。

### 6.2 障碍物

必须支持箱体、圆柱体和球体的静态或往返移动障碍物，并支持：

- 按种子确定性生成，避开出生保护区、车辆和已有障碍物。
- 静态、移动和混合追加；混合模式默认移动比例为 30%。
- Dashboard 列表选择、删除选中、清空全部和重新生成。
- 场地重建时保留逻辑 ID、XY、轨迹、进度和方向；无法完整恢复时回滚事务。
- 添加、删除、清空、重建和回滚的事件日志。

障碍物管理和 Dashboard 操作不得长时间阻塞 240 Hz 物理循环。

## 7. 人工驾驶与 Dashboard

GUI 必须保留键盘人工驾驶、车辆复位、相机跟随和车型/场地/障碍物操作。
人工控制使用有界租约；按键释放、窗口失焦、命令源断开或租约过期均必须归零。

Dashboard 主页面只展示业务操作和企业接口状态，包括：

- 当前车型、场地、障碍物和场景 revision。
- 轮态、LiDAR、RTK、IMU 与逐 topic 连接、频率、序列和错误状态。
- 启动、暂停、复位、应用切换、障碍物管理和记录状态。

摩擦、接触、打滑、内部队列和线程细节属于默认折叠的内部诊断。Dashboard
只消费有界快照，不保存或绘制完整点云；相机、图表和点云预览分别限频。
在没有单独 UI 重设计审批前，保留当前 15 个一级页面、主窗与 Dashboard
`67:33` 分配、Dashboard 内部 `50:50` 分配、固定 LiDAR 视野和 30 m 距离环。

## 8. eCAL Protobuf v2

正式生产数据面固定为：

| 方向 | Topic | 类型 | 频率 |
|---|---|---|---:|
| Simulator 订阅 | `/sim/wheel/command` | `WheelCommand` | 100 Hz |
| Simulator 发布 | `/sim/wheel/state` | `WheelState` | 100 Hz |
| Simulator 发布 | `/sim/lidar/points` | `LidarPointCloud` | 10 Hz |
| Simulator 发布 | `/sim/rtk/state` | `RtkState` | 10 Hz |
| Simulator 发布 | `/sim/imu/attitude` | `ImuAttitude` | 10 Hz |

协议要求：

- v1 schema 和 descriptor 保持冻结，仅用于历史数据读取；生产 Simulator 不
  长期双发 v1/v2。
- Python 与 C++ 从同一 `.proto` 生成 binding 和 descriptor，并用固定 golden
  bytes 验证互操作。
- 顶层消息携带固定长度 session、descriptor SHA、world generation、逐 topic
  sequence 和统一仿真时间戳。
- WheelCommand 还携带 command generation、source ID 和 source session。
- 旧 session/generation、错误 owner、重复或逆序 sequence、错误 descriptor、
  NaN/Inf 和错误数组长度必须整条拒绝，且不刷新命令租约。
- 正式会话只允许一个 Command peer；零 peer 时等待并停车，多 peer 冲突时停车。
- WheelState 为 100 Hz；LiDAR、RTK 和 IMU 使用同一 10 Hz 采样格，四类输出
  在同刻样本上的时间戳必须完全一致。

字段号、车型数组顺序、状态机和拒绝顺序的规范定义在阶段四设计与 `.proto`，
不得在 Dashboard、Recorder 或 ROS Bridge 中复制第二套判据。

## 9. 传感器

### 9.1 中心 MID-360 风格 LiDAR

- 每个阶段四车型只有一个中心 360 度 LiDAR。
- 实时 profile 使用 5,760 条候选射线；离线 dense profile 使用 20,000 条。
- 一帧扫描必须基于同一冻结世界快照，排除机器人本体并输出可见表面命中。
- 点字段保持 Livox `CustomPoint` 风格：时间偏移、XYZ、反射率、tag 和 line。
- 第一版反射率和 tag 可以为零；不得用障碍物中心或真值列表冒充点云。

### 9.2 三点 RTK 与 IMU

- RTK 以 10 Hz 输出世界坐标中的 `LEFT/CENTER/RIGHT` 三点和车体航向。
- CENTER 位于机器人轴心中心；LEFT/RIGHT 使用车型固定 canonical 几何。
- 航向由 `RIGHT -> LEFT` 基线计算，不得在非零 roll/pitch 时直接用 Euler yaw
  代替。
- IMU 以 10 Hz 输出有限的 roll、pitch 以及完整消息身份。
- Simulator、Export 和 ROS Bridge 必须复用同一姿态恢复判据。

## 10. C++ 工具、记录与数据

必须交付 C++17 SDK、只读 Subscriber、Command、Recorder、Replay 和 Export。

Recorder 必须保存五个正式 topic 的原始 eCAL Protobuf bytes，并为每条业务
消息配对记录 topic、类型、session、descriptor、payload SHA、时间、generation、
sequence 和 scene revision。一个完整会话包含最终 session manifest、顺序编号的
MCAP segment 和场景 YAML attachment。

写入中的 segment 使用临时名；只有 flush、fsync 和完整性检查成功后才能原子
完成。磁盘不足、队列溢出或持久化失败必须触发安全停车并保留诊断。

Replay 默认只发布到 `/replay/sim/*`，不得污染实时 `/sim/*`，也不得默认回放
WheelCommand。Export 必须支持 PCD、PLY 和标记为 synthetic 的 LVX2；原始
MCAP 始终是权威数据。

用户配置放在 `~/.config/slope-sim`，记录与导出默认放在
`~/slope-sim-data`。升级和回退不得覆盖这些目录。

## 11. 可选 ROS 2

ROS 2 Jazzy、Livox 消息依赖和 RViz2 仅在安装器使用 `--with-ros` 时安装。
Bridge 是可单独重启的只读下游，不改变 eCAL payload，也不进入核心启动依赖。

Bridge 必须输出 live/replay 分离的 Livox `CustomMsg`、`PointCloud2`、轮态、
RTK、IMU、TF 和 clock；实时点云显示由此 ROS/RViz2 路径承担。TF 与点云只能使用
同 session、generation 和完全相同 timestamp 的输入。关闭或失败时，Simulator 和
Recorder 必须继续运行。Livox Viewer 2 的 loopback-only 启动与 `/Game/Maps/Viewer`
地图加载 smoke 不构成 LVX2 导入、模拟点云显示或真实 MID-360 发现的验收；标准
LVX2 离线回放须另行完成验收。

## 12. 单文件联网安装

唯一交付文件为：

```text
slope-sim-stage4-<version>-ubuntu24.04-amd64.run
```

安装要求：

- `.run` 内含项目源码、资源、默认配置、依赖锁、许可证和安装工具，但不打包
  第三方源码缓存、Conda package cache 或开发构建目录。
- 安装器可请求 sudo；网络下载和项目构建仍以原普通用户身份执行。
- 依赖固定版本、URL、SHA-256、许可证和关键 ABI；传输错误可有限重试，摘要
  不符必须立即失败。
- 安装、回退和卸载共用一个全局排他锁；每轮使用唯一工作目录和唯一 staging。
- 版本安装到 `/opt/slope-sim/releases/<version>`，通过 doctor/smoke 后才原子
  切换 `/opt/slope-sim/current`。
- 同版本重复安装只校验 payload、`with_ros` 安装选项和文件完整性，不下载、
  不构建、不覆盖；若版本完整但尚未激活，只原子补做 `current` 切换。
- 同版本损坏或想改变安装选项时拒绝覆盖，必须发布新版本。系统不提供原地
  `--repair`。
- 失败不得切换当前版本；允许回退到已校验旧版本和卸载非当前版本。
- systemd user service 默认禁用，不得安装后自动常驻。

## 13. 性能、安全与可靠性

- 物理步进目标为 240 Hz，WheelCommand/WheelState 为 100 Hz，三类传感器为
  10 Hz。
- 联合负载基线为高尔夫 heightfield、20 个障碍物、5,760 射线、GUI、
  Dashboard 和 logging；正式指标见阶段四设计，不能通过降低业务频率掩盖问题。
- eCAL callback、Dashboard、日志和 Recorder 不得直接调用 PyBullet。
- 编排器使用单实例锁；本机 control socket 权限为 `0700` 并核对 peer uid/PID。
- 场景切换、命令权变更、Command peer 丢失和 Recorder fatal 均必须安全停车。
- 正常停止按“零命令、冻结输出、Recorder drain、flush/fsync、finalize、释放锁”
  的顺序执行。
- 所有队列必须有界；任何 drop、sequence gap 和持久化错误都必须可观察，不能
  静默伪装为成功。

## 14. 验收与阶段状态

### 14.1 自动与实际验收

完成门槛至少包括：

1. 四车型乘三场地的加载、运动、传感器真值、场景重建和安全停车回归。
2. v2 Python/C++ golden、descriptor、session/generation/sequence 和非法输入拒绝。
3. 真实 eCAL 下代表性 `4+2` 与 `2+0` 会话，五 topic 频率、身份和零 drop/error。
4. Recorder 故障、完整 session、Replay 隔离及 PCD/PLY/LVX2 回读。
5. GUI 三种分辨率、Dashboard 响应和联合性能验收。
6. 启用 ROS 时的 Bridge、TF、clock 和 RViz2 smoke。
7. 一台干净 Ubuntu 24.04 amd64 电脑上的单文件联网安装、新版本切换、失败不
   切换、回退和卸载。
8. 每个大阶段结束后的独立六维只读审查；最终必须
   `Critical=0, Important=0`。

默认 pytest 回归不得依赖历史 build 目录或真实 eCAL；真实 eCAL、GUI、性能和
干净机验收使用明确的独立门禁，不能用 skip 或旧制品伪装通过。

### 14.2 当前实施边界

| 范围 | 当前边界 |
|---|---|
| 阶段一、二基础功能 | 四车型、三场地、运行期切换、障碍物和 Dashboard 已有实现并持续回归 |
| 阶段三 v1 | 作为历史接口基线保留，不再定义阶段四传感器语义 |
| 阶段四 A | 已完成：v2 协议、会话、命令权、跨语言 golden、raw eCAL Phase-0 与独立六维审查均已通过 |
| 阶段四 B | 已完成：单中心 LiDAR、三点 RTK、正式 v2 runtime、性能与 Dashboard/GUI 门已通过 |
| 阶段四 C | 已完成：C++ SDK、Command、Subscriber、Recorder 与联合会话停止门已通过 |
| 阶段四 D | 已完成：Replay、Export、可选 ROS/RViz2 实时显示门已通过；LVX2 profile 8 已在 Livox Viewer 2 中读取、播放并显示非空点云，退出 `SIGSEGV/rc139` 作为专有 Viewer 残余风险单列，不阻塞项目内回放 |
| 阶段四 E | 已完成：`.run` 联网安装器、干净 Ubuntu smoke、联合负载与独立六维审查已通过 |
| Stage 4 v2 Golf | 已完成：固定 Golf 场景的路线、运动、地形覆盖、去畸变、完整五 topic MCAP 与同会话 GUI QA 均已通过；入口与自动 QA 已纳入 4.0.1 `.run`，并完成干净安装 smoke |

旧阶段四 master/A-E 详细计划已经暂停，不得继续执行其中的离线缓存、断网、
`.tar.zst`、双根构建或跨机器证据步骤。轻量实现的实时状态与证据以
`docs/阶段四交付报告.md` 为准。

## 15. 参考与借鉴边界

- Bullet racecar 与 heightfield 示例：参考关节控制、地形和运行时对象创建，
  不复制硬编码关节索引。
- 仓库 `references/`：参考成熟项目的传感器、记录和工具边界；复制资源或代码
  时必须核对并保留许可证。
- Eclipse eCAL、Livox ROS Driver 2、ROS 2 Jazzy 和 MCAP 官方资料：只作为
  协议和格式参考，不作为安装缓存或运行时网络依赖。
