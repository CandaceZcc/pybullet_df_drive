# 3D 移动机器人仿真平台需求规格

> 文档状态：需求基线
> 编制日期：2026-07-16
> 最近更新：2026-07-20（确认阶段二动态障碍物设计，并新增阶段四自动导航与避障）
> 需求来源：`3d仿真需求.md`、当前 `pybullet_df_drive` 仓库实现、补充企业需求及开源项目调研
> 适用范围：本仓库后续机器人模型、场地、Dashboard、eCAL 接口和验收工作
> 当前交付状态：阶段一已验收；阶段二设计已确认，等待按实施计划开发。

## 1. 文档标记说明

本文使用以下标记区分需求优先级，防止把当前实验性参数误当成企业交付接口：

- **【企业必需】**：来自主企业需求或本轮明确补充需求，必须实现和验收。
- **【工程补充】**：为安全、可测试、可维护而补充的实现约束，不扩大企业业务接口。
- **【内部诊断】**：仅用于开发调试和日志分析，不进入主 Dashboard 参数区，也不作为企业 eCAL 接口字段。

## 2. 当前项目现状

截至 2026-07-16 的阶段一候选交付，当前仓库已具备：

- PyBullet `DIRECT` 与 `GUI` 两种运行模式。
- `flat`、可调 `slope` 和可复现 `golf_heightfield` 三类基础场地。
- `df_front`、`df_mid`、`df_back` 三种差速模型和 `active_steering_4wd` 主动转向四驱模型。
- 统一车型注册表、语义关节解析、真实关节反馈和物理驱动。
- 轮速控制、物理驱动、摩擦/接触/打滑记录、简化水平 LiDAR、CSV 日志和实时 Dashboard。
- GUI 手动驾驶、跟随相机、车辆复位及必要诊断能力。
- Dashboard 显式“应用车型 / 应用场地”、斜面坡度修改、高尔夫种子/起伏修改和场地失败回滚。

阶段一已删除旧履带和旧场地的可运行入口。与最终四阶段目标相比，剩余缺口属于阶段二、阶段三或阶段四：

- 尚无障碍物管理器和移动障碍物。
- 当前 LiDAR 只有二维距离摘要，未输出前后多线雷达点云。
- 尚无 eCAL 输入/输出、双天线 RTK 和企业要求的 IMU 接口。
- Dashboard 当前展示大量摩擦、接触和打滑实验字段，未按企业交付接口收敛。
- 尚无自动刹停、全局路径规划、局部动态避障和导航安全状态机。

## 3. 建设目标

### 3.1 总体目标

建设一个轻量、可更换机器人与场地、支持 eCAL 闭环接口及后续自动导航的 3D 移动机器人仿真平台。平台应能在本机 GUI 中演示，也能在 SSH 环境下通过 PyBullet `DIRECT` 模式运行自动测试。

### 3.2 必须交付的能力

1. **【企业必需】** 删除现有履带模型，不再提供 `tracked_proxy`。
2. **【企业必需】** 提供 `df_front`、`df_mid`、`df_back`、`active_steering_4wd` 四个机器人模型。
3. **【企业必需】** 提供平面、斜面、高尔夫球场模拟三类可更换场地。
4. **【企业必需】** Dashboard 可在运行期间切换机器人、切换或修改场地，并添加静态或移动障碍物。
5. **【企业必需】** 轮子命令和反馈通过 eCAL 以 100 Hz 交换。
6. **【企业必需】** 前后各安装一个多线雷达，以 eCAL 10 Hz 输出点云。
7. **【企业必需】** 双天线 RTK 以 eCAL 10 Hz 输出主天线位置和主天线指向副天线的世界坐标系 yaw。
8. **【企业必需】** IMU 以 eCAL 10 Hz 输出 roll 和 pitch。
9. **【企业必需】** 主 Dashboard 和企业接口只保留本文明确列出的企业参数。
10. **【企业必需】**（2026-07-20 用户补充确认）在场景与企业传感器接口稳定后，提供自动刹停、路径规划和动态避障导航。

### 3.3 当前范围边界

- 连续柔性履带、履带-土壤高保真接触和履带模型兼容层。
- 真实 GNSS 卫星、RTK 差分解算过程和真实雷达光学/电子噪声建模。
- ROS / ROS 2 作为必需通信链路。
- 多机器人并行仿真、云端分布式仿真和大规模强化学习训练。
- 自动路径规划、自动避障算法在阶段四交付，阶段一至阶段三只提供车辆、场景、传感器与通信基础。
- 多任务调度不纳入当前四阶段交付范围。

### 3.4 分阶段交付门禁

- **【企业必需】** 项目严格按第 14 节的阶段一、阶段二、阶段三、阶段四顺序交付，不并行提前实现后续阶段功能。
- **【企业必需】** 每个阶段由开发方先完成自动化测试和无界面验证，再停止开发并提交阶段报告与 GUI 人工验收清单。
- **【企业必需】** 用户通过 GUI 自行观察和评估，人工验收不设置倒计时或固定测试时长。
- **【企业必需】** 用户反馈应先用于完善当前阶段；在用户明确提出“开始下一阶段”前，不得进入下一阶段。
- **【工程补充】** 自动化测试通过不等于阶段自动通过，阶段是否关闭以用户的明确验收结论为准。

## 4. 仿真架构选型

### 4.1 框架比较

| 方案 | 优点 | 主要代价 | 本项目结论 |
|---|---|---|---|
| PyBullet | 现有代码可复用；Python 接口轻量；支持 URDF、关节控制、heightfield、运行时创建/删除刚体、批量射线和无界面运行 | 多线雷达和 RTK 需要自行封装；复杂轮胎/土壤模型保真度有限 | **当前主后端** |
| MuJoCo | 动力学效率高；支持 URDF/MJCF、heightfield、执行器和传感器 | 现有场景、Dashboard 和模型控制需要迁移；多线雷达仍需自建 | 保留为后续动力学替代候选 |
| Webots | 自带 LiDAR、GPS、IMU、Supervisor 动态场景管理，机器人仿真功能完整 | 安装和运行比 PyBullet 重；现有代码复用度低 | 传感器保真不足时的优先替代候选 |
| Gazebo Sim | 传感器、噪声、SDF、Ackermann/差速控制和生态完整 | 依赖和资源占用最大，工程复杂度明显增加 | 当前不采用；需要 ROS 2/高保真传感器时再评估 |

### 4.2 选型结论

**【工程补充】当前继续使用 PyBullet，不进行整体迁移。** 四种底盘、场地切换、heightfield、动态障碍物、批量射线点云和 eCAL 接口均可在现有架构上完成。

为避免后续被单一引擎锁死，代码边界应按以下职责拆分：

```text
Dashboard / eCAL
        |
SimulationCoordinator
        |
+-------+---------+-----------+-----------+
|                 |           |           |
RobotRegistry  TerrainRegistry  ObstacleManager  SensorSuite
        \           |           /           /
         \----------SimulationBackend------/
                         |
                 PyBulletBackend
```

- `SimulationBackend`：连接、步进、加载/删除刚体、关节控制、射线检测和状态读取。
- `RobotRegistry`：管理四种机器人模型及统一关节语义。
- `TerrainRegistry`：管理三类场地和安全重建。
- `ObstacleManager`：管理静态/移动障碍物的创建、移动和删除。
- `SensorSuite`：生成雷达、RTK、IMU 和轮子反馈。
- `EcalBridge`：按固定频率收发 Protobuf 消息，不在物理模块内直接处理通信。

只有出现以下情况之一，才启动 Webots/Gazebo/MuJoCo 替代验证：

- PyBullet 批量射线无法在目标计算机上稳定达到前后雷达 10 Hz。
- 企业要求真实 MID-360 扫描规律、遮挡、回波、噪声或 GPU 传感器渲染。
- 企业要求高保真轮胎、草地/软土形变或硬件在环认证级动力学。
- 后续必须直接接入 ROS 2 / Gazebo 现有插件生态。

## 5. 坐标系与单位

### 5.1 统一坐标约定

- **【企业必需】世界坐标系**：右手系，`+X` 为场地主前进方向，`+Y` 为左侧，`+Z` 向上。
- **【企业必需】车体坐标系**：`+X` 为车头方向，`+Y` 为车体左侧，`+Z` 向上。
- **【企业必需】角度单位**：弧度 `rad`；角速度单位 `rad/s`。
- **【企业必需】位置单位**：米 `m`；时间戳单位采用纳秒 `ns`。
- **【工程补充】yaw**：绕 `+Z` 轴按右手定则增加，输出范围统一为 `[-π, π)`。

### 5.2 时间基准

- **【工程补充】** 所有 eCAL 消息携带仿真时间戳，传感器之间使用同一仿真时钟。
- **【工程补充】** 物理步长可继续使用当前 `1/240 s`，100 Hz 与 10 Hz 接口使用仿真时间累加调度，不能通过固定整数步简单取模造成频率漂移。

## 6. 机器人模型需求

### 6.1 通用要求

- **【企业必需】** 仓库最终只向 Dashboard 暴露四种车型：`df_front`、`df_mid`、`df_back`、`active_steering_4wd`。
- **【企业必需】** `tracked_proxy` 从配置、Dashboard、运行时注册表、测试和文档中删除。
- **【企业必需】** 四种模型均可在三类场地上加载、驱动、转向和重置。
- **【工程补充】** 四种模型使用一致的车体尺寸、总质量、轮半径和轮距基线，便于对比驱动布局影响。
- **【工程补充】** 关节索引不得硬编码，必须通过关节名或模型元数据查找。
- **【工程补充】** 模型切换前清零命令，切换后从当前场地安全出生点重新加载。

### 6.2 `df_front` 前置驱动差速模型

- **【企业必需】** 左、右驱动轮布置在车体前部，通过左右轮速度差转向。
- **【企业必需】** 车体后部设置一个低摩擦球形支撑轮，构成稳定的三点支撑。
- **【工程补充】** 支撑轮只承担支撑，不参与驱动，不应显著阻碍原地转向。
- **【工程补充】** 驱动轮关节名固定为 `left_drive_wheel_joint`、`right_drive_wheel_joint`，支撑轮 link 名包含 `support`。

### 6.3 `df_mid` 中置驱动差速模型

- **【企业必需】** 左、右驱动轮布置在车体纵向中心附近。
- **【企业必需】** 车体前、后各设置一个低摩擦球形支撑轮，避免中置驱动时车体俯仰失稳。
- **【工程补充】** 前后支撑轮使用球形碰撞体或等效全向支撑结构，减少高尔夫起伏地形上的卡滞。
- **【工程补充】** 质量中心应位于驱动轴附近，使主要法向载荷落在驱动轮上。

### 6.4 `df_back` 后置驱动差速模型

- **【企业必需】** 左、右驱动轮布置在车体后部，通过左右轮速度差转向。
- **【企业必需】** 车体前部设置一个低摩擦球形支撑轮，构成稳定的三点支撑。
- **【工程补充】** 当前 `diff_drive.urdf` 可作为结构参考，但需要按统一命名、统一尺寸和传感器安装位重建为 `df_back`。

### 6.5 `active_steering_4wd` 主动转向四驱模型

- **【企业必需】** 四个车轮均具有独立驱动关节。
- **【企业必需】** 左前、右前轮各具有独立转向关节；后轮不转向。
- **【企业必需】** 输入为四个驱动轮速度和两个前轮转向速度，输出为四个实际驱动轮速度和两个前轮实际转向角度。
- **【工程补充】** 前轮结构采用“转向关节作为父关节、车轮旋转关节作为子关节”的 URDF 层级。
- **【工程补充】** 转向速度命令通过积分形成转向角目标，并受对称机械角度限位保护。
- **【工程补充】** 可参考 Bullet 官方 `racecar.py`、`racecar_differential.py` 和 `racecar_differential.urdf` 的前轮转向、四轮联动及齿轮约束思路，但不直接复制其模型尺寸和硬编码关节索引。

### 6.6 模型与支撑轮汇总

| 模型 | 驱动轮 | 转向轮 | 支撑轮 | eCAL 驱动输入数 | eCAL 转向速度输入数 |
|---|---:|---:|---:|---:|---:|
| `df_front` | 左右前轮 | 0 | 后部 1 个球形支撑轮 | 2 | 0 |
| `df_mid` | 左右中轮 | 0 | 前后各 1 个球形支撑轮 | 2 | 0 |
| `df_back` | 左右后轮 | 0 | 前部 1 个球形支撑轮 | 2 | 0 |
| `active_steering_4wd` | 四轮 | 左右前轮 | 0 | 4 | 2 |

## 7. 场地模型需求

### 7.1 场地类型

#### 7.1.1 平面场地 `flat`

- **【企业必需】** 提供水平、连续、可碰撞的平面。
- **【工程补充】** 固定场地边界，供随机障碍物生成和越界判断使用。

#### 7.1.2 斜面场地 `slope`

- **【企业必需】** 提供可修改坡度的连续斜面。
- **【企业必需】** Dashboard 可修改坡度并重新应用场地。
- **【工程补充】** 当前 `box_slope`、参考 5 度斜面和大坝场景可作为几何与出生点计算参考，最终统一到 `slope` 场地接口。

#### 7.1.3 高尔夫球场模拟 `golf_heightfield`

- **【企业必需】** 使用 heightfield 生成连续缓坡、局部起伏和小丘，模拟高尔夫球场地形。
- **【企业必需】** Dashboard 可重新随机生成起伏地形。
- **【工程补充】** 使用随机种子保证同一场地可复现；高度变化必须连续，不能生成使底盘必然卡死的尖峰。
- **【工程补充】** 第一版只模拟硬质起伏表面，不模拟草叶、湿地、松软土壤和轮胎下陷。

### 7.2 场地切换与实时修改语义

- **【企业必需】** Dashboard 可在仿真程序不退出的情况下更换场地。
- **【工程补充】** 场地碰撞体不在车辆运动中直接变形。点击“应用场地”后，协调器按以下事务执行：暂停物理步进、清零车辆命令、删除旧场地、创建新场地、重新投放车辆与障碍物、恢复仿真。
- **【企业必需】** 障碍物的新增、移动和删除可以在物理仿真持续运行时立即生效，不要求重建场地。
- **【工程补充】** 场地切换失败时回滚到上一个有效场地，Dashboard 显示错误，不能留下无地面或半构建状态。

### 7.3 Dashboard 最小场地参数

以下参数是完成企业场地需求所需的最小集合：

| 参数 | 类型 | 适用场地 | 标记 | 说明 |
|---|---|---|---|---|
| `terrain_type` | enum | 全部 | 【企业必需】 | `flat` / `slope` / `golf_heightfield` |
| `slope_deg` | float | `slope` | 【企业必需】 | 斜面坡度，单位 `deg` |
| `golf_seed` | integer | `golf_heightfield` | 【工程补充】 | 复现同一随机起伏场地 |
| `golf_relief` | enum | `golf_heightfield` | 【工程补充】 | `low` / `medium` / `high` 起伏预设，避免暴露过多底层 heightfield 参数 |

场地长度、宽度、网格分辨率、碰撞厚度和纹理等保留为内部配置，不进入主 Dashboard。

## 8. 障碍物需求

### 8.1 障碍物类型

- **【企业必需】静态障碍物**：质量为 0，不主动移动，可被雷达检测并与机器人碰撞。
- **【企业必需】移动障碍物**：按照预设轨迹在场地内移动，可被雷达实时检测并与机器人碰撞。
- **【工程补充】** 第一版几何只提供箱体、圆柱体、球体，避免导入复杂网格影响稳定性。

### 8.2 添加与随机位置

- **【企业必需】** Dashboard 可指定障碍物数量并在当前场地随机位置添加。
- **【企业必需】** 可选择静态、移动或按比例混合模式；混合模式默认移动比例为 30%。
- **【工程补充】** 混合模式移动数量按 `floor(count * ratio + 0.5)` 计算；`count >= 2` 且比例在 `(0, 1)` 内时夹紧到 `[1, count - 1]`。
- **【工程补充】** 随机生成必须遵守场地边界、车辆出生保护区、车辆当前 AABB 安全裕量、障碍物间最小间距和地面高度。
- **【工程补充】** 斜面和高尔夫场地中的障碍物应按局部地面高度放置，并根据局部法向调整底面姿态，避免悬空或穿入地面。
- **【工程补充】** 地表射线只接受当前场地 body，必须跳过车辆、障碍物自身和其他障碍物。
- **【工程补充】** 同一随机种子应生成相同的障碍物位置、尺寸和运动初始状态。
- **【企业必需】** Dashboard 支持删除单个障碍物和清空全部障碍物。

### 8.3 移动障碍物

- **【企业必需】** 移动障碍物至少支持往返直线运动。
- **【工程补充】** 移动由 `ObstacleManager` 在每个仿真步更新，碰到场地边界后反向，不允许瞬移出界。
- **【工程补充】** 移动障碍物属于运动学刚体，轨迹不受机器人碰撞反推，确保演示可重复。
- **【工程补充】** DIRECT 验证要求路径横向误差不超过 `1e-6 m`、接触穿透不超过 `0.03 m`，车辆线速度不超过 `3.0 m/s`、角速度不超过 `10.0 rad/s` 且状态有限；在相同初态、命令和时长下，静态障碍场景的车辆前进位移不得超过无障碍基线的 50%。

### 8.4 Dashboard 最小障碍物参数

| 参数 | 类型 | 标记 | 说明 |
|---|---|---|---|
| `obstacle_mode` | enum | 【企业必需】 | `static` / `moving` / `mixed` |
| `obstacle_shape` | enum | 【工程补充】 | `box` / `cylinder` / `sphere` |
| `obstacle_count` | integer | 【企业必需】 | 本次随机添加数量 |
| `obstacle_seed` | integer | 【工程补充】 | 随机位置复现 |
| `obstacle_speed_mps` | float | 【企业必需】 | 仅移动障碍物使用 |
| `obstacle_moving_ratio` | float | 【企业必需】 | 仅混合模式使用，默认 `0.30` |

障碍物的质量、摩擦、颜色和详细尺寸使用稳定默认值，不进入主 Dashboard。

## 9. eCAL 接口需求

### 9.1 总体要求

- **【企业必需】** 使用 eCAL 发布/订阅通信。
- **【工程补充】** 使用 Protobuf 定义消息，所有进程共享同一套 `.proto` 文件。
- **【工程补充】** 话题名集中配置，禁止散落在物理、Dashboard 或传感器代码中。
- **【工程补充】** eCAL 不可用时，仿真可在本地测试模式启动，但必须明确显示“eCAL 未连接”，不能伪装成接口正常。

### 9.2 话题与频率

| 方向 | 建议话题 | 频率 | 标记 | 内容 |
|---|---|---:|---|---|
| 订阅 | `/sim/wheel/command` | 100 Hz | 【企业必需】 | 驱动轮速度、转向轮速度命令 |
| 发布 | `/sim/wheel/state` | 100 Hz | 【企业必需】 | 实际驱动轮速度、实际转向轮角度 |
| 发布 | `/sim/lidar/front/points` | 10 Hz | 【企业必需】 | 前多线雷达点云 |
| 发布 | `/sim/lidar/rear/points` | 10 Hz | 【企业必需】 | 后多线雷达点云 |
| 发布 | `/sim/rtk/state` | 10 Hz | 【企业必需】 | 主天线位置、主到副天线 yaw |
| 发布 | `/sim/imu/attitude` | 10 Hz | 【企业必需】 | roll、pitch |

话题名为本项目建议基线；若企业已有命名规范，只替换配置，不改变消息语义。

### 9.3 轮子输入 `WheelCommand`

企业必需字段仅保留：

| 字段 | 类型 | 单位 | 车型 | 标记 |
|---|---|---|---|---|
| `timestamp_ns` | uint64 | ns | 全部 | 【工程补充】 |
| `drive_wheel_speed_rad_s[]` | repeated float | rad/s | 差速车 2 个；主动转向车 4 个 | 【企业必需】 |
| `steering_wheel_speed_rad_s[]` | repeated float | rad/s | 差速车为空；主动转向车 2 个 | 【企业必需】 |

数组顺序固定：

- 差速模型驱动轮：`[left, right]`。
- 主动转向模型驱动轮：`[front_left, front_right, rear_left, rear_right]`。
- 主动转向模型转向轮：`[front_left, front_right]`。

**【工程补充】** 连续 100 ms 未收到有效轮子命令时，所有驱动速度和转向速度命令归零；转向角保持在当前安全角度。

### 9.4 轮子输出 `WheelState`

| 字段 | 类型 | 单位 | 车型 | 标记 |
|---|---|---|---|---|
| `timestamp_ns` | uint64 | ns | 全部 | 【工程补充】 |
| `drive_wheel_speed_rad_s[]` | repeated float | rad/s | 差速车 2 个；主动转向车 4 个 | 【企业必需】 |
| `steering_wheel_angle_rad[]` | repeated float | rad | 差速车为空；主动转向车 2 个 | 【企业必需】 |

轮速必须来自 PyBullet 实际关节状态，不能直接回显输入命令。

### 9.5 前后多线雷达点云 `LidarPointCloud`

- **【企业必需】** 车体前、后各安装一个独立多线雷达。
- **【企业必需】** 两个雷达分别以 10 Hz 输出点云。
- **【工程补充】** 第一版使用 PyBullet `rayTestBatch` 生成多层水平/垂直射线，不再只输出最近距离摘要。
- **【工程补充】** 点云格式参考 Livox MID-360 的 Livox ROS Driver 2 `CustomMsg/CustomPoint` 字段设计。

点云消息字段：

| 字段 | 类型 | 单位 | 标记 | 说明 |
|---|---|---|---|---|
| `timebase_ns` | uint64 | ns | 【企业必需】 | 本帧第一个点时间 |
| `frame_id` | string | - | 【工程补充】 | `lidar_front` 或 `lidar_rear` |
| `point_num` | uint32 | - | 【企业必需】 | 本帧有效点数量 |
| `lidar_id` | uint32 | - | 【工程补充】 | 前后雷达固定编号 |
| `points[]` | repeated point | - | 【企业必需】 | 点集合 |

单点字段：

| 字段 | 类型 | 单位 | 标记 | 说明 |
|---|---|---|---|---|
| `offset_time_ns` | uint32 | ns | 【企业必需】 | 相对 `timebase_ns` 的点时间 |
| `x` / `y` / `z` | float | m | 【企业必需】 | 雷达坐标系下坐标 |
| `reflectivity` | uint32 | 0-255 | 【企业必需】 | 第一版按材质默认值生成 |
| `tag` | uint32 | - | 【企业必需】 | 第一版使用仿真命中类别映射 |
| `line` | uint32 | - | 【企业必需】 | 激光线编号 |

### 9.6 双天线 RTK `RtkState`

- **【企业必需】** 机器人上固定主、副两个天线安装点。
- **【企业必需】** 输出主天线在世界坐标系中的位置。
- **【企业必需】** 输出从主天线指向副天线的世界坐标系 yaw。
- **【工程补充】** yaw 计算使用两天线世界坐标差向量：`atan2(y_secondary - y_primary, x_secondary - x_primary)`。
- **【工程补充】** 接口语义参考 UM982 双天线 moving-baseline heading，但本阶段直接从仿真真值生成，不模拟卫星和 RTK 解算过程。

| 字段 | 类型 | 单位 | 标记 |
|---|---|---|---|
| `timestamp_ns` | uint64 | ns | 【工程补充】 |
| `main_x` / `main_y` / `main_z` | double | m | 【企业必需】 |
| `baseline_yaw_rad` | double | rad | 【企业必需】 |

### 9.7 IMU `ImuAttitude`

| 字段 | 类型 | 单位 | 标记 |
|---|---|---|---|
| `timestamp_ns` | uint64 | ns | 【工程补充】 |
| `roll_rad` | double | rad | 【企业必需】 |
| `pitch_rad` | double | rad | 【企业必需】 |

- **【企业必需】** 发布频率为 10 Hz。
- **【工程补充】** roll、pitch 由车体姿态四元数转换得到；第一版默认无噪声。

### 9.8 企业必需参数边界

阶段三主 Dashboard 与 eCAL 对外接口只允许出现以下企业参数；其他物理参数不得混入企业参数区。阶段四可按第 14.4 节增加导航目标、路线、状态和安全告警，但不得重新暴露内部物理参数：

| 类别 | 企业必需参数 |
|---|---|
| 机器人 | `robot_model`：`df_front` / `df_mid` / `df_back` / `active_steering_4wd` |
| 场地 | `terrain_type`，斜面使用 `slope_deg` |
| 障碍物 | `obstacle_mode`、`obstacle_count`，移动障碍物使用 `obstacle_speed_mps`，混合模式使用 `obstacle_moving_ratio` |
| 轮子输入 | `drive_wheel_speed_rad_s[]`、`steering_wheel_speed_rad_s[]` |
| 轮子输出 | `drive_wheel_speed_rad_s[]`、`steering_wheel_angle_rad[]` |
| 前后雷达 | 点云 `timebase_ns`、`point_num`、`points[]`；单点 `offset_time_ns`、`x/y/z`、`reflectivity`、`tag`、`line` |
| 双天线 RTK | `main_x`、`main_y`、`main_z`、`baseline_yaw_rad` |
| IMU | `roll_rad`、`pitch_rad` |

轮半径、轮距、质量、惯量、电机力、摩擦系数、支撑轮摩擦、heightfield 分辨率、雷达射线数量/FOV、接触力和打滑指标均属于内部实现或诊断参数，不是企业对外参数。

## 10. Dashboard 需求

### 10.1 主页面只保留的企业功能

Dashboard 主页面应只保留以下业务区域：

1. **仿真控制**：启动/暂停、复位、退出。
2. **机器人**：四种车型选择、应用车型。
3. **场地**：平面/斜面/高尔夫场地选择，坡度或高尔夫起伏预设，应用/重新生成。
4. **障碍物**：静态/移动/混合、形状、数量、随机种子、移动速度、移动比例、随机添加、删除选中、清空。
5. **轮子接口**：输入命令状态、实际驱动轮速度、实际转向角度、eCAL 连接状态和实际频率。
6. **传感器接口**：前后雷达、RTK、IMU 的连接状态、实际发布频率和最近时间戳。
7. **导航与安全**：阶段四增加目标点、开始/暂停/取消、路线、导航状态和安全告警；该区域不在阶段三提前实现。

### 10.2 运行期操作要求

- **【企业必需】** 车型、场地和障碍物操作不需要退出仿真程序。
- **【工程补充】** 车型、场地和障碍物结构操作进入统一队列；执行期间相关按钮进入忙碌状态，禁止重复提交。
- **【工程补充】** 切换成功后 Dashboard、日志和 eCAL 立即使用新模型/场地标识。
- **【工程补充】** 障碍物列表显示唯一 ID、模式和当前位置，便于删除单个障碍物。

### 10.3 从主页面移除的当前字段

以下当前字段不是主企业接口参数，应从主 Dashboard 移除：

- 地面/车轮摩擦系数。
- 各向异性摩擦系数。
- 接触法向力、接触摩擦力和接触点数。
- 打滑率、打滑速度和低速有效标记。
- 目标/实际电机扭矩、内部速度误差。
- 地形法向、内部 raycast 摘要和诊断曲线。

这些字段可以保留为 **【内部诊断】** 日志或可选“开发者诊断页”，但默认隐藏，不进入企业验收参数表。

## 11. 配置与数据管理

- **【工程补充】** 使用单一版本化场景配置保存车型、场地、障碍物和传感器安装信息。
- **【工程补充】** 阶段二先提供场地事务所需的内存障碍物快照；阶段三在传感器安装配置稳定后完成版本化场景文件的导出和再次加载。
- **【工程补充】** 随机场地和随机障碍物必须记录种子。
- **【工程补充】** 日志至少记录时间戳、车型、场地、障碍物事件和全部企业接口消息，便于回放与验收。
- **【工程补充】** 原始 `3d仿真需求.md` 保留为企业需求来源，不用本文件覆盖。

## 12. 性能与可靠性要求

- **【企业必需】** GUI 模式下可实时观察一个机器人、三类场地和静态/移动障碍物。
- **【企业必需】** DIRECT 模式可在 SSH 无显示环境运行。
- **【工程补充】** 在项目验收计算机上，一个机器人、前后雷达和 20 个障碍物运行时，100 Hz 轮子接口与 10 Hz 传感器接口不得出现持续积压。
- **【工程补充】** 仿真暂停时 eCAL 仍可报告连接状态，但不得继续发布伪造的新物理状态。
- **【工程补充】** 非法数组长度、NaN、无穷值和超出机械限位的命令必须被拒绝并记录原因。
- **【工程补充】** 场地/车型重建、eCAL 断线或传感器生成异常不得导致 PyBullet 客户端残留或 Dashboard 无响应。
- **【工程补充】** 大批障碍物规划和暂存必须跨帧让出控制，单帧软预算为 2 ms；最终原子发布允许集中提交，但项目验收机上的任何单次 Dashboard 事件间隔不得超过 100 ms。

## 13. 验收标准

### 13.1 模型验收

- 仓库和 Dashboard 中不再出现可运行的 `tracked_proxy`。
- 四种目标模型均可独立加载并完成前进、后退和转向测试。
- 三个差速模型的驱动轮位置分别位于前部、中部、后部，支撑轮布局与第 6 节一致。
- 主动转向模型四轮均可驱动，两个前轮可独立转向并输出实际角度。
- 轮子输出来自实际关节反馈，数组长度和顺序符合本文定义。

### 13.2 场地和障碍物验收

- Dashboard 可在同一进程内依次切换平面、斜面和高尔夫起伏场地。
- 修改斜面坡度或高尔夫起伏预设后，碰撞场地与显示场地一致。
- 可随机添加、删除和清空静态障碍物。
- 可随机添加移动障碍物，障碍物在运行时移动、边界反向且可被前后雷达检测。
- 相同随机种子生成相同场地和障碍物布局。

### 13.3 eCAL 与传感器验收

- 自动化频率测试应根据消息时间戳与计数验证：轮子命令接收与状态发布平均频率为 100 Hz，前后雷达、RTK、IMU 平均频率为 10 Hz；GUI 人工验收不要求计时。
- 轮子命令中断超过 100 ms 后，驱动速度命令自动归零。
- 前后雷达点云包含 `offset_time_ns`、`x/y/z`、`reflectivity`、`tag`、`line`，并能反映静态和移动障碍物。
- 无噪声模式下，RTK 主天线位置、基线 yaw、IMU roll/pitch 与 PyBullet 仿真真值一致，数值误差不超过 `1e-4`。
- Dashboard 显示各话题连接状态、实际频率和最近时间戳。

### 13.4 Dashboard 验收

- 主页面只展示第 10.1 节列出的企业功能和接口状态。
- 摩擦、接触、打滑等实验参数不出现在主页面。
- 打开 Dashboard、切换页面、添加障碍物或切换车型/场地时，物理步进和 eCAL 调度不出现持续阻塞。

## 14. 四阶段交付与人工验收门禁

### 14.1 阶段一：四种车型与三类基础场地

#### 阶段目标

先完成最核心、最容易通过 GUI 直接判断质量的车辆和场地，使用户能够独立评估每种车型的完善程度、支撑轮设计和三类场地效果。

#### 本阶段交付内容

1. 删除 `tracked_proxy` 的运行路径、配置入口和 Dashboard 车型选项。
2. 建立统一车型注册表、关节命名和模型元数据。
3. 完成 `df_front`、`df_mid`、`df_back` 三种差速模型及对应支撑轮布局。
4. 完成 `active_steering_4wd` 四轮驱动、双前轮主动转向模型。
5. 将机器人控制拆分为差速控制和主动转向控制，轮子反馈来自实际关节状态。
6. 完成 `flat`、`slope`、`golf_heightfield` 三类场地。
7. 支持通过启动配置或命令行选择车型和场地；根据用户 GUI 预验收反馈，补充 Dashboard 显式应用车型和场地的运行期切换。
8. 保留基础 GUI 驾驶、相机跟随、复位和必要日志，便于用户观察模型行为。

#### 本阶段明确不包含

- 随机静态障碍物和移动障碍物。
- eCAL 通信、前后多线雷达点云、双天线 RTK 和企业 IMU 接口。
- 企业 Dashboard 最终字段精简；现有诊断能力可暂时保留用于定位模型问题。

#### 开发方自动验证

- 检查四个 URDF 可加载、关节名正确、驱动轮/支撑轮数量和位置符合第 6 节。
- 对三个差速模型验证前进、后退、左转、右转和原地差速转向。
- 对主动转向模型验证四轮驱动、左右前轮转向速度积分、角度限位和实际角度反馈；同一前进加转向命令下，四个真实驱动关节必须都有非零轮速反馈，并记录逐轮速度与电机扭矩供复核。
- 在 `flat`、`slope`、`golf_heightfield` 中分别执行 DIRECT smoke test，确认车辆不会出生即穿模、翻转或悬空。
- 对正 `slope_deg` 验证场地有高位平地、沿世界 `+X` 的下坡和低位平地三段；四种车型均从水平出生姿态驶过三段，姿态按平地、坡段、平地恢复的顺序变化。
- 验证 `golf_heightfield` 是单个 heightfield，公共数组语义为 `rows=y`、`columns=x`，传入 PyBullet 时按其快轴映射；相同 `golf_seed` 和 `golf_relief` 必须生成相同高度数组。驾驶廊道可适度平滑局部起伏，但仍须保留大部分丘陵和浅洼。
- 验证 Dashboard 只有点击应用按钮才产生一次性切换请求，修改下拉框本身不改变活动世界。
- 验证相机跟随开关和三种视角是每帧持续状态：车后、侧面视角随车头 yaw 旋转，固定视角只跟随位置；切换车型或场地后继续使用新的活动车体。
- 在真实 DIRECT 物理客户端验证车型替换保留场地、场地重建保留车型，以及车型/场地加载失败时保留或恢复上一个有效世界。
- 运行与配置、控制器、机器人加载、场地创建和手动 GUI 入口相关的现有回归测试。

#### 用户 GUI 人工验收清单

用户可按自己的观察时长逐项评估：

- `df_front`：前置驱动轮位置、后支撑轮接地、直行和差速转向是否自然。
- `df_mid`：中置驱动轮位置、前后支撑轮是否稳定、坡面和起伏地形上是否容易翘头或丢失驱动接触。
- `df_back`：后置驱动轮位置、前支撑轮接地、加减速时俯仰是否合理。
- `active_steering_4wd`：四轮是否均参与驱动，两个前轮转向方向、角度和回正表现是否正确。
- 平面：车辆是否平稳、无明显自移动、横漂或轮子穿地。
- 斜面：正坡度时，车辆是否从高位平地沿 `+X` 进入下坡并驶入低位平地；坡度显示、碰撞面、入坡和出坡姿态是否一致。
- 高尔夫场地：丘陵、浅洼、横坡与平滑驾驶廊道是否连续；同一 `golf_seed` 和 `golf_relief` 重新应用后是否复现，廊道是否仍保留大部分丘洼而不是直线平路。
- 四种车型的外观比例、车体高度、轮距、轮径、传感器安装预留位置是否满足预期。
- 只改变 Dashboard 下拉框时活动世界是否保持不变；点击“应用车型”后是否保留当前场地并安全换车。
- 点击“应用场地”后是否重建场地并重新投放当前车型；修改坡度、高尔夫种子或起伏等级后是否正确生效。
- 切换“启用跟随”后是否立即生效；转弯时车后和侧面视角是否随车头旋转，固定视角是否只跟随位置；车型或场地切换后是否继续跟随新车体。

#### 阶段停止与进入条件

- 开发方完成自动验证后必须停止，提交阶段一变更摘要、测试结果、GUI 启动方式和已知问题。
- 用户可以多轮反馈车型、支撑轮、控制或场地效果问题；所有反馈先在阶段一内修正并重新交付。
- 只有用户明确确认阶段一通过并提出开始阶段二，才允许实施阶段二。

### 14.2 阶段二：Dashboard 动态场景与障碍物

#### 阶段目标

在阶段一模型、场地和运行期切换已经由用户确认的基础上，完成静态/移动障碍物交互，并按实际复杂度抽取场景协调边界。

#### 前置条件

- 用户已明确验收阶段一，并明确提出开始阶段二。
- 阶段一确认的车型几何、支撑轮布局和场地基础效果不再随意重做；如确需修改，应说明回归影响。

#### 本阶段交付内容

1. 在阶段一 `ActiveManualWorld` 和切换事务基础上，按障碍物共享状态的实际需要建立 `SimulationCoordinator` 与 `ObstacleManager` 协作边界。
2. 支持随机添加箱体、圆柱体或球体静态障碍物。
3. 支持按往返直线轨迹移动的运动学障碍物。
4. 支持静态、移动及默认移动比例 30% 的混合追加生成。
5. 支持障碍物列表单选、删除选中、清空全部和随机种子复现。
6. 场地重建时保留障碍物逻辑 ID、XY、路径、进度和方向，仅重新贴合目标地表；无法完整恢复时回滚整个场地切换。
7. 添加、删除、清空、场地重建和回滚写入独立障碍物事件日志，记录种子、逻辑 ID、场地和结果。
8. 保留阶段一 Dashboard 车型/场地切换、控制、绘图和物理步进防阻塞回归保护。

#### 本阶段明确不包含

- 正式 eCAL 轮子接口。
- 前后多线雷达点云、双天线 RTK 和企业 IMU 输出。
- 企业传感器 Dashboard 字段基线；传感器状态区在阶段三接入后完成，导航区域在阶段四增加。
- 自动刹停、路径规划和动态避障导航；这些能力在阶段四实现。

#### 开发方自动验证

- 验证车型切换会清零旧命令、移除旧机器人并在安全出生点加载目标车型。
- 验证场地切换事务成功路径和失败回滚路径，不留下无地面或重复刚体。
- 验证相同障碍物随机种子生成相同位置和运动初态。
- 验证混合模式按确定性比例生成静态和移动障碍物，并保持追加语义。
- 验证障碍物不生成在车辆出生保护区、车辆当前 AABB、场地外或其他障碍物内部。
- 验证地表采样只命中当前地形，不被车辆、障碍物自身或其他障碍物截获。
- 验证移动障碍物完整运动走廊有效，按预设轨迹移动并在边界反向。
- 验证移动障碍物稳定阻挡车辆，无明显穿透、爆炸冲量或轨迹偏移。
- 验证通过 Dashboard 列表逻辑 ID 删除单个、清空全部、场地重建后保持布局等生命周期行为。
- 验证障碍物事件日志完整记录添加、删除、清空、重建和回滚结果。
- 验证 50 个批量添加和 100 个清空会跨帧让出控制，Dashboard 事件最大间隔不超过 100 ms。
- 运行 Dashboard 事件、按钮、键盘、绘图调度和 GUI 手动控制相关回归测试。

#### 用户 GUI 人工验收清单

- 不退出程序，依次切换四种车型，观察旧模型是否彻底移除、新模型是否正确落地。
- 不退出程序，依次切换平面、斜面和高尔夫场地，观察场地、车辆和相机状态是否同步更新。
- 修改斜面坡度并应用，观察显示面与实际车辆接触是否一致。
- 多次重新生成高尔夫场地，观察不同起伏预设和随机种子的视觉效果。
- 随机添加静态障碍物，观察位置、接地、碰撞和删除行为。
- 随机添加移动障碍物，观察运动连续性、边界反向、碰撞和 Dashboard 响应。
- 使用混合模式添加 10 个障碍物，确认默认生成 7 个静态和 3 个移动障碍物。
- 清空后使用相同参数和种子重新生成，确认障碍物布局和运动初态可复现。
- 在切换、添加、删除和打开 Dashboard 页面时持续驾驶，评估是否出现卡顿、断控或焦点丢失。

#### 阶段停止与进入条件

- 开发方完成自动验证后必须停止，提交阶段二变更摘要、测试结果、GUI 操作步骤和已知问题。
- 用户可以多轮反馈 Dashboard 布局、切换逻辑、场地重建或障碍物效果问题；所有反馈先在阶段二内修正。
- 只有用户明确确认阶段二通过并提出开始阶段三，才允许实施阶段三。

### 14.3 阶段三：eCAL、企业传感器与接口基线

#### 阶段目标

在模型、场地和动态交互均已由用户确认后，接入企业闭环通信和传感器输出，并把 Dashboard 收敛为企业传感器接口基线；阶段四可在该基线上增加导航业务区域。

#### 前置条件

- 用户已明确验收阶段二，并明确提出开始阶段三。
- 四种车型、三类场地和障碍物管理接口已稳定，传感器与 eCAL 不再依赖临时模型结构。

#### 本阶段交付内容

1. 定义并版本化 `WheelCommand`、`WheelState`、`LidarPointCloud`、`RtkState`、`ImuAttitude` Protobuf。
2. 实现 eCAL 100 Hz 轮子命令订阅、100 Hz 实际轮子状态发布和 100 ms 命令超时保护。
3. 实现前、后多线雷达及两个独立 10 Hz 点云话题。
4. 实现主、副天线安装点、主天线世界位置和基线 yaw 10 Hz 输出。
5. 实现 IMU roll、pitch 10 Hz 输出。
6. 实现统一仿真时钟、时间戳和非整数频率调度。
7. Dashboard 增加 eCAL 连接、话题状态、实际频率和最近时间戳。
8. 按第 9.8 和第 10 节精简主 Dashboard，将摩擦、接触、打滑等内容移入内部诊断。
9. 完成包含车型、场地、障碍物和传感器安装信息的版本化场景文件导出与加载。
10. 完成 DIRECT、GUI、eCAL 和传感器集成验收及阶段三文档更新。

#### 开发方自动验证

- 验证四种车型对应的轮子命令/状态数组长度、顺序、单位和非法输入拒绝逻辑。
- 验证转向速度积分、机械角度限位和命令中断超时保护。
- 使用消息时间戳和计数验证 100 Hz 与 10 Hz 调度，不把 GUI 人工观察时长作为频率判据。
- 验证前后点云坐标系、字段完整性、线号、时间偏移及静态/移动障碍物命中。
- 验证 RTK 主天线位置、主到副天线 yaw、IMU roll/pitch 与 PyBullet 真值一致。
- 验证 eCAL 未安装、未连接、断线和重新连接时的错误处理与 Dashboard 状态。
- 验证版本化场景文件导出、加载、非法版本拒绝和随机布局复现。
- 运行完整 DIRECT 集成测试和所有既有回归测试，确认 Dashboard 不阻塞物理与通信调度。

#### 用户 GUI 人工验收清单

- 打开 GUI 和 eCAL Monitor，确认轮子、前后雷达、RTK、IMU 话题及频率状态可见。
- 通过 eCAL 发送差速车型轮速，观察车辆运动方向和实际轮速反馈。
- 通过 eCAL 发送主动转向车型四轮速度和前轮转向速度，观察转向方向、角度限位和实际角度反馈。
- 在静态和移动障碍物场景中观察前后雷达点云是否随相对位置变化。
- 在平面、斜面和高尔夫场地中观察 RTK 位置、基线 yaw、IMU roll/pitch 是否符合车辆姿态。
- 暂停、复位、切换车型/场地或断开命令发送时，观察超时停车和接口状态是否符合预期。
- 检查阶段三接口基线 Dashboard 是否只保留企业功能和企业接口状态，诊断参数是否默认隐藏。

#### 阶段停止与进入条件

- 开发方完成自动验证后必须停止，提交阶段三变更摘要、完整测试结果、GUI/eCAL 操作步骤和已知限制。
- 用户可以多轮反馈接口字段、点云效果、RTK/IMU、Dashboard 或整体仿真表现问题；所有反馈在阶段三内修正。
- 只有用户明确确认阶段三通过并提出开始阶段四，才允许实施自动导航与避障。

### 14.4 阶段四：自动导航与避障

#### 阶段目标

在车辆、动态场景、企业传感器和 eCAL 接口已经稳定的基础上，实现可人工观察、可自动验证的单机器人安全导航闭环。

#### 前置条件

- 用户已明确验收阶段三，并明确提出开始阶段四。
- 前后雷达、RTK、IMU、轮子命令和动态障碍物接口不再依赖临时数据结构。

#### 本阶段交付内容

1. 建立障碍物感知输入和独立紧急刹停保护，感知失效或障碍物进入制动距离时安全停车。
2. 建立二维地图或局部代价地图，统一表达场地边界、静态障碍物和移动障碍物风险区域。
3. 实现从车辆当前位置到用户目标点的全局路径规划，并明确不可达结果。
4. 实现局部动态避障，跟踪全局路线并根据移动障碍物更新局部运动决策。
5. 实现导航控制器和安全状态机，覆盖空闲、规划、执行、避障、刹停、到达、失败和取消状态。
6. Dashboard 增加目标点、开始、暂停、取消、导航状态、路线和告警显示。
7. 保留手动驾驶接管能力；人工接管时自动导航必须退出控制权并安全清理旧命令。

#### 本阶段明确不包含

- 多机器人协同、车队调度和云端任务分发。
- SLAM 建图产品化、高精地图生产工具和道路规则语义导航。
- 对真实车辆安全等级或功能安全认证作出保证。

#### 开发方自动验证

- 验证静态障碍、移动障碍、传感器失效和通信超时下的紧急刹停距离及状态转换。
- 验证起点/终点合法性、可达路线、不可达路线和路线复现。
- 验证局部避障不会驶出场地、进入出生保护区或穿过障碍物。
- 验证动态障碍物横穿、迎面和追越场景中的减速、绕行、等待与恢复。
- 验证手动接管、暂停、取消、车型/场地重建后的控制权与旧路线清理。
- 运行阶段一至阶段三完整回归，确认导航循环不阻塞物理、Dashboard 或 eCAL 调度。

#### 用户 GUI 人工验收清单

- 在三类场地设置可达目标点，观察全局路线、车辆跟踪和到达状态。
- 在路线附近添加静态障碍物，观察重新规划或局部绕行。
- 添加横穿路线的移动障碍物，观察减速、等待、绕行和恢复导航。
- 将障碍物放入安全制动区域，确认车辆刹停并显示明确原因。
- 测试不可达目标、暂停、取消和人工驾驶接管，确认状态和控制权正确。

#### 阶段停止与最终验收

- 开发方完成自动验证后必须停止，提交阶段四变更摘要、完整测试结果、GUI/eCAL 操作步骤和已知限制。
- 用户可以多轮反馈刹停阈值、路线、避障行为、状态机或 Dashboard 表现问题；所有反馈在阶段四内修正。
- 只有用户明确确认阶段四通过，整个需求才视为完成。

### 14.5 需求与阶段映射

| 需求范围 | 阶段一 | 阶段二 | 阶段三 | 阶段四 |
|---|---:|---:|---:|---:|
| 删除履带、四种机器人模型 | 完成 | 回归保护 | 接口适配与回归 | 导航控制适配与回归 |
| 平面、斜面、高尔夫场地 | 基础创建、启动选择与 Dashboard 动态切换/修改 | 障碍物场景回归保护 | 传感器与接口回归 | 地图与导航回归 |
| 静态/移动障碍物 | 不实施 | 完成 | 点云检测与接口回归 | 感知与避障输入 |
| Dashboard | 基础驾驶、观察和车型/场地切换 | 障碍物管理与切换回归 | 企业传感器接口基线 | 导航与安全状态 |
| 轮子 eCAL | 不实施 | 不实施 | 完成 | 导航输出与回归 |
| 前后多线雷达 | 不实施 | 不实施 | 完成 | 障碍物感知输入 |
| 双天线 RTK、IMU | 不实施 | 不实施 | 完成 | 定位与姿态输入 |
| 场景快照与日志 | 车辆 CSV | 障碍物事件与内存快照 | 版本化场景导出/加载 | 导航事件扩展 |
| 自动刹停、路径规划、动态避障 | 不实施 | 不实施 | 不实施 | 完成 |

### 14.6 每阶段交付报告格式

开发方在每阶段停止时至少报告：

1. 本阶段已经完成的需求和明确未完成的后续阶段需求。
2. 新增、修改、删除的文件及其用途。
3. 实际运行的自动化测试、DIRECT 验证和结果。
4. 未解决问题、已知限制和需要用户重点观察的风险。
5. GUI 启动命令、操作入口和人工验收清单。
6. 明确写出“当前已停止，等待用户人工验收和反馈”。

在收到用户明确的下一阶段启动指令前，开发方只能修正当前阶段问题，不得提前实施后续阶段范围。

## 15. 开源参考与借鉴边界

### 15.1 当前直接参考

- [bulletphysics/bullet3 `racecar.py`](https://github.com/bulletphysics/bullet3/blob/master/examples/pybullet/examples/racecar.py)：借鉴前轮转向和轮速/转向关节控制。
- [bulletphysics/bullet3 `racecar_differential.py`](https://github.com/bulletphysics/bullet3/blob/master/examples/pybullet/examples/racecar_differential.py)：借鉴四轮联动和齿轮约束；不照搬硬编码关节索引。
- [bulletphysics/bullet3 `racecar_differential.urdf`](https://github.com/bulletphysics/bullet3/blob/master/examples/pybullet/gym/pybullet_data/racecar/racecar_differential.urdf)：借鉴“转向关节 + 驱动车轮关节”层级。
- [bulletphysics/bullet3 `heightfield.py`](https://github.com/bulletphysics/bullet3/blob/master/examples/pybullet/examples/heightfield.py)：借鉴 heightfield 创建与重建高尔夫起伏地形。
- [bulletphysics/bullet3 `createObstacleCourse.py`](https://github.com/bulletphysics/bullet3/blob/master/examples/pybullet/examples/createObstacleCourse.py)：借鉴运行时创建静态和运动障碍物。
- 仓库内 `references/repos/pybullet_diffdrive`：借鉴最小差速轮 URDF 和球形支撑轮。
- 仓库内 `references/repos/pybullet_sim`：借鉴差速机器人、批量射线、障碍环境和传感器封装。

### 15.2 接口与设备格式参考

- [Eclipse eCAL Python 文档](https://eclipse-ecal.github.io/ecal/)，[eCAL Protobuf 示例](https://docs.ros.org/en/iron/p/ecal/doc/rst/getting_started/hello_world_python_proto.html)：借鉴 Python 发布/订阅与 Protobuf 共享消息定义。
- [Livox ROS Driver 2](https://github.com/Livox-SDK/livox_ros_driver2)：借鉴 MID-360 `CustomMsg/CustomPoint` 的 `timebase`、`offset_time`、坐标、反射率、tag 和 line 字段。
- [UM982 用户手册](https://en.unicore.com/uploads/file/20241219/um982-user-manual-en-r1-1.pdf)：借鉴双天线定位与 heading/yaw 的接口语义。

### 15.3 替代框架资料

- [MuJoCo XML Reference](https://mujoco.readthedocs.io/en/stable/XMLreference.html)：确认 URDF/MJCF、heightfield、执行器和传感器能力。
- [Webots LiDAR](https://www.cyberbotics.com/doc/reference/lidar) 与 [Webots Supervisor](https://www.cyberbotics.com/doc/reference/supervisor)：作为原生点云和运行时场景管理的替代候选。
- [Gazebo Sim](https://gazebosim.org/libs/sim)：作为高保真传感器、SDF、插件和 ROS 2 生态的替代候选。

借鉴开源项目时必须检查许可证，优先复用设计思路和接口模式；复制代码或模型资源时保留来源和许可证文件。
