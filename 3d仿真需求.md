构建一个仿真平台：一个机器人在平面和斜面行走。
场地要求：一个平面场地和一个斜面场地，场地中可以任意添加障碍物（能在仿真过程中移动障碍物最佳，添加静态障碍物次之）。平面最好有起伏（模拟高尔夫场地）。
机器人模型要求：差速模型机器人（要求驱动轮可以在中间，也可以放前面或者后面），主动转向机器人模型（四轮都可以驱动，且两个前轮可以转向）
轮子输入接口：驱动轮速度（rad/s），转向轮速度（rad/s）（ecal接口，100hz）。
轮子输出接口：驱动轮速度（rad/s），转向轮角度（rad）（ecal接口，100hz）。
传感器接口：雷达：前后装两个多线雷达，分别返回雷达点云（ecal接口，10hz，可以参考livox mid360点云格式）
          rtk：在机器上装两个天线，返回主天线位置和主天线到副天线在世界坐标系中的yaw角（ecal接口，10hz，可以参考um982双天线系统输出）
          imu：反馈roll和pitch角度（10hz，ecal接口）
