# 历史原始需求（已归档）

> 状态：仅保留需求来源，不是当前验收或接口定义
>
> 当前权威需求：`3d仿真平台需求规格.md`
>
> 当前阶段四设计：
> `docs/superpowers/specs/2026-07-31-stage4-mid360-ecal-cpp-delivery-design.md`

下列内容是项目最初收到的原文。其前后双 LiDAR、双天线 RTK 等语义已由用户
确认的新需求取代，不得据此修改阶段四 topic、传感器几何或实施范围。

> 构建一个仿真平台：一个机器人在平面和斜面行走。
>
> 场地要求：一个平面场地和一个斜面场地，场地中可以任意添加障碍物（能在
> 仿真过程中移动障碍物最佳，添加静态障碍物次之）。平面最好有起伏（模拟
> 高尔夫场地）。
>
> 机器人模型要求：差速模型机器人（要求驱动轮可以在中间，也可以放前面或者
> 后面），主动转向机器人模型（四轮都可以驱动，且两个前轮可以转向）。
>
> 轮子输入接口：驱动轮速度（rad/s），转向轮速度（rad/s）（eCAL 接口，
> 100 Hz）。
>
> 轮子输出接口：驱动轮速度（rad/s），转向轮角度（rad）（eCAL 接口，
> 100 Hz）。
>
> 传感器接口：雷达：前后装两个多线雷达，分别返回雷达点云（eCAL 接口，
> 10 Hz，可以参考 Livox MID-360 点云格式）。
>
> RTK：在机器上装两个天线，返回主天线位置和主天线到副天线在世界坐标系中
> 的 yaw 角（eCAL 接口，10 Hz，可以参考 UM982 双天线系统输出）。
>
> IMU：反馈 roll 和 pitch 角度（10 Hz，eCAL 接口）。
