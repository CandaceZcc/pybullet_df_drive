# 阶段四交付报告

> 基线：ce3bee0
> 实现状态：P0 本地异步 LiDAR worker 已完成并通过 DIRECT 门；阶段四正式 Task 2 与 A-E 尚未启动
> 总计划进度：Task 1 与两车型 P0 真实 eCAL 均已通过；Task 2 已解除阻断，A-E 尚未启动

| 外部门禁 | 当前状态 | 证据 |
|---|---|---|
| 真实 eCAL | 未执行 | 无 |
| 真实 RViz2 | 未执行 | 无 |
| Livox Viewer 2 | 未执行 | 无 |
| 干净机迁移 | 未执行 | 无 |

## 前置门禁 P0

| 检查项 | 当前状态 | 证据 |
|---|---|---|
| 阶段三 post-fix 真实 eCAL（主动转向 4+2） | PASS：六 topic active，command/wheel-state `500/500`、四传感器各 `50`，`sim/wall=0.998075`，20 障碍日志零丢失，wheel 原始日志/peer `500/500` 匹配，双方 clean shutdown | `results/stage4/p0-active-steering-4wd-retest-20260804T152217+0800/{runtime-result.json,peer-result.json,interface-logs/}` |
| 阶段三 post-fix 真实 eCAL（差速 2+0） | PASS：五 topic active，command `500`、wheel 原始日志/peer `499/499`，四传感器各 `50`，`sim/wall=0.998075`，20 障碍日志零丢失，双方 clean shutdown | `results/stage4/p0-df-back-2wd-20260804T153125+0800/{runtime-result.json,peer-result.json,interface-logs/}` |

## P0 本地异步 LiDAR Worker

| 检查项 | 当前状态 | 证据 |
|---|---|---|
| production spawn worker、20 障碍、DIRECT、10x5 实时门 | PASS | `results/stage4/lidar_worker_realtime_10x5_final_20260804.log`：954 captures/completes、0 failure/overrun、最大 heartbeat 6.98 ms、最低窗口 sim/wall 0.999961、worker exitcode 0 |
| 点云/worker 直接回归 | PASS | `tests/test_lidar_pointcloud_direct.py tests/test_lidar_worker.py`：69 passed |
| 阶段三 DIRECT 验收 | PASS | `scripts/verify_stage3_interfaces.py`：21/21 passed；20 障碍项 1200/1200、backlog=False |
| 全量非 eCAL 回归 | PASS | `python -m pytest -q -m "not ecal"`：2402 passed、4 deselected、exit 0 |

headless worker 只回传预编码紧凑 payload，父进程不再反序列化或逐点重构点云；Dashboard top-view 请求保持完整 frame 的点云/俯视图原子性。真实 eCAL、RViz2、Livox Viewer 和干净机迁移均未执行，不能由以上本地证据替代。

独立六维审查已完成：需求完整性、逻辑正确性、边界情况、代码质量、测试覆盖和实际运行结果均已复核，最终 `Critical=0`、`Important=0`、`Minor=0`。审查指出的逐窗口 sim/wall 与 child exitcode 两项门禁缺口均已按 RED/GREEN 修复后复审。

本地候选修复（2026-08-03）：TDD 已证明同步 LiDAR 扫描/编码期间的 GIL 调度会推迟 command callback。正式 simulation runtime 仅在实时循环内把 Python thread switch interval 收紧到不高于 1 ms，并在所有正常或异常退出路径精确恢复调用方原值；未修改 wire、transport API、PyBullet 原子扫描或 25 ms oracle。新鲜本地证据为聚焦测试 `4 passed`、transport/verifier 纯测试 `236 passed, 4 deselected`、runtime/lifecycle/realtime `254 passed`、双 LiDAR DIRECT 预算 `1 passed`，以及 `py_compile`、`git diff --check` rc=0。随后一次独立授权的真实 `4+2` 已确认 command callback 恢复，但 output wheel-state 的 `33.030 ms` 接收节拍失败；因此候选修复不能关闭 P0，`2+0` 继续阻断。

启动门修复（2026-08-04）：本次授权真实 `4+2` 未进入正式窗口，原因不是 LiDAR 性能，而是 peer 在 eCAL discovery 完成前写入 `ready`。eCAL 官方 `Publisher.send()` 对零已发现订阅者返回 `False`；runtime 将 warmup 的这些结果记录为 topic error 后，peer 的 start marker 又立即触发 sensor fence，导致 start snapshot 不能 active。`test_simulation_peer_marks_ready_only_after_all_resources_are_discovered` 先以 `discovery_calls == 0` 观察 RED，随后将 peer 的 discovery 移至 ready 前得到 GREEN `1 passed, 159 deselected`；非真实 eCAL 回归为 `247 passed, 4 deselected`。此修复尚未经过新的真实 `4+2`，因此 P0 仍为 FAIL，`2+0` 与 Task 2 继续阻断。

授权复测与日志快照修复（2026-08-04）：下一次获授权的 `4+2` 已确认 peer discovery 可完成，`measurement_start.ack` 中六话题均 active；但 runtime 在测量窗口初始化时引用已删除的 `log_start` 局部变量，报 `NameError` 后退出。该单次运行未重跑，证据为 `results/stage4/p0-active-steering-4wd-retest-20260804T145908+0800/`。随后以 `test_measurement_start_log_sample_uses_capture_snapshot` 先得到 `1 failed, 160 deselected` 的 RED，再将首条 logger 样本改为读取 `start_capture.log_snapshot`，同一命令 GREEN 为 `1 passed, 160 deselected`；非真实 eCAL 的 process-roundtrip/transport 回归为 `248 passed, 4 deselected`，语法编译和 diff 空白检查均通过。这只是本地修复证据，真实 P0 保持 FAIL，仍须新的单次 `4+2` 授权；`2+0` 与 Task 2 继续阻断。

数据面预检修复（2026-08-04）：下一次获授权的 `4+2` 证实 `log_start` 已不再触发，但 peer 的资源 peer count 可在 output 数据面真正可投递前变为非零。`15:14:30.870` peer ready 和共同 start 之后，runtime 约一秒内所有 warmup output `send()` 均返回 `False`，最终仍报 start snapshot 非 active；单次执行没有重跑，证据为 `results/stage4/p0-active-steering-4wd-retest-20260804T151427+0800/`。`test_simulation_peer_waits_for_output_data_plane_before_warmup` 先以缺少 preflight helper 得到 RED `1 failed, 161 deselected`，随后要求 peer 在 warmup 前收到五个 output topic 各至少一帧，GREEN 为 `1 passed, 161 deselected`；非真实 eCAL 的 process-roundtrip/transport 回归为 `249 passed, 4 deselected`，语法编译和 diff 空白检查均通过。这仍不是新的真实运行证据，P0 继续为 FAIL，须再取一条单次 `4+2` 授权；`2+0` 与 Task 2 继续阻断。

主动转向真实通过（2026-08-04）：用户在空闲主机上手动执行 data-plane preflight 修复后的唯一 `4+2`，退出码 `0` 且 verifier 输出 `PASS`。正式窗口 command/wheel-state 各 `500`、四个传感器各 `50`，所有 topic active；`1199` 步、`4.995833 s` 仿真时间、`sim/wall=0.998075`。20 障碍日志为 `1200` 条、零丢失、无持续 backlog、末尾 pending 为零；wheel 原始日志与 peer 为 `500/500` 完整匹配，双方 clean shutdown。独立读取 runtime/peer JSON 与协议 ACK 均与输出一致，证据在 `results/stage4/p0-active-steering-4wd-retest-20260804T152217+0800/`。接口日志保留的预热和故障注入 `publish_failed` 均在正式 start/end snapshot 外，不影响正式 oracle。P0 的 `4+2` 已通过；差速 `2+0` 现在需要独立单次授权，Task 2 仍等待两车型都通过。

差速真实通过（2026-08-04）：独立预检和授权后执行的 `df_back 2+0` 返回 rc=0 与 `PASS`。正式窗口 command `500`、wheel 原始日志/peer `499/499`，四个传感器各 `50`，五 topic active；`1199` 步、`sim/wall=0.998075`、20 障碍日志零丢失、无持续 backlog、末尾 pending 为零，双方 clean shutdown。start/end snapshot 的 `132` 个 error 是数据面就绪前预热累计，窗口差分为零且逐帧 oracle 已通过。证据在 `results/stage4/p0-df-back-2wd-20260804T153125+0800/`。两车型 P0 已通过，Task 2 解除阻断；A-E 仍未启动。

P0 独立六维复审（2026-08-04）：只读审查任务 `/root/p0_six_dimension_review` 未修改文件、未启动真实 eCAL/PyBullet；从需求完整性、逻辑正确性、边界情况、代码质量、测试覆盖和实际运行结果复核 P0。结论 `Critical=0`、`Important=0`、`Minor=0`。它确认两轮窗口中的历史 error 均不跨入正式 oracle，worker 生命周期/IPC、fence、epoch、generation、重建和关闭合同均有实现与测试。剩余 GUI eCAL、RViz2/Livox Viewer 与干净机迁移属于后续阶段范围，不阻塞 P0。
