# 阶段四交付报告

> 基线：ce3bee0
> 实现状态：阶段 A-E 已完成对应实现与真实门禁；最终全阶段独立六维审查
> `Critical=0`、`Important=0`、`Minor=0`
>
> 最终交付候选：`results/stage4/e-run-golf-4.0.1-20260816/slope-sim-stage4-4.0.1-ubuntu24.04-amd64.run`，SHA-256 为 `51e6d16830fe9f77210dbc377c86c8badf677ef2f354a1b19232b9342e4ea905`。`results/stage4/` 内其余 `.run` 均为保留的历史 TDD/安装证据，不是交接候选。
>
> 总计划进度：Task 1 与两车型 P0 真实 eCAL 均已通过；B1/B2 已完成领域合同、正式 runtime、四车型三场地性能和真实桌面 Dashboard；C 阶段已关闭，D 的 Reader/Replay/Export/ROS Bridge 实现及 live/replay 实装 smoke 已通过，实时点云显示由 ROS/RViz2 路径承担并已完成真实窗口 smoke；Livox Viewer 2 已完成 profile 8 LVX2 的读取、播放和非空显示，退出 `rc139` 单列为专有 Viewer 残余风险；E 的核心安装器、核心干净 Ubuntu smoke 与无预装 ROS 的 `--with-ros` bootstrap 均已通过。

| 外部门禁 | 当前状态 | 证据 |
|---|---|---|
| 阶段四 A Phase-0 真实 eCAL | PASS：严格 verifier 重算 descriptor、三组 send/received raw bytes、conflict participant/scenario JSON 和 parent process return-code/log SHA-256；三条互操作链均 verified；同 topic v1/v2 为 conflict，accepted=0 | `results/stage4/phase0-real-reauthorized-20260810T183206+0800/phase0-result.json` |
| 真实 RViz2 | PASS：GNOME `DISPLAY=:1` 显示 RViz 窗口，OpenGL 4.6 初始化，SIGINT 后 exit 0 | `results/stage4/d-rviz2-real-smoke-20260811/{result.txt,rviz.stderr.log}` |
| Livox Viewer 2 | loopback-only 启动 smoke：历史上官方 Linux 2.6.0 在隔离中完成地图加载；随后 profile 8 LVX2 已读取 100 帧、播放至尾并显示非空点云（`PointsNum=2538`），构成 DISPLAY PASS WITH SHUTDOWN CONCERN。正常退出仍为 `SIGSEGV/rc139`，而 map-load 不作为显示依据 | `results/stage4-lvx2-closeout/viewer/project-lvx2-v4-profile8-attempt2/{result-summary.txt,wait-result.txt}` |
| 干净机迁移（核心） | PASS：Ubuntu 24.04 容器联网安装，`current -> releases/4.0.0`，安装后无网络 Command dry-run 通过 | `results/stage4/e-run-candidate-yaml-system-bootstrap-20260811/`、`/tmp/stage4-clean-ubuntu24.qg3iui/` |
| 干净机迁移（Golf 4.0.1） | PASS：4.0.1 已在新的 `/tmp/stage4-golf-4.0.1-clean-install/` 联网安装，`current -> releases/4.0.1`，doctor `files_verified=true`，安装后无网络 Command dry-run 与 Golf mapping import/CLI smoke 均通过 | `results/stage4/e-run-golf-4.0.1-20260816/`、`/tmp/stage4-golf-4.0.1-clean-install/` |

## 2026-08-16 收口增量：LVX2 与 MID-360 Golf

LVX2 profile 8 已从原始 MCAP 导出，`LiDAR_Type=8`，并在 Livox Viewer 2 中读取 100 帧、
播放至末尾、显示非空点云（`PointsNum=2538`）。这构成显示 PASS；同一专有 Viewer 在
正常退出阶段出现 `SIGSEGV/rc139`，单列为 clean-shutdown concern，不阻塞项目内 MCAP
回放或 Golf 回放，且不通过补点、假点或修改实时 LiDAR 门限规避。

Golf 的 `mid360-golf-mapping-acceptance-v2` 通过，证据为
`results/mid360-golf-mapping-acceptance-route-progress-v2/acceptance.json`：路线 P95
`0.06433634581408047 m`、终点剩余 `0.07440749727453522 m`、运动/地形覆盖均为 `1.0`、
deskew P95 上界 `0.008 m`、五 topic 冻结计数为 `20820/20820/2082/2083/2083`，MCAP SHA-256
为 `bc4b95d199b1d000a8fd36a7ca88235782694831bb51c0fc7fd97a228305643b`。路线停滞、pose race
和 launcher parent-loss 的失败目录保留为历史失败证据，未被覆盖。现有双 OpenGL GUI QA
通过，含非空像素、播放/暂停、逐帧、定位、倍率、回退重建和正常关闭，但其 session 与
上述 acceptance 不同；不得把二者表述为同一端到端会话。该批工作复用已完成的六维审查：
此前两个 Important finding 均已针对性复验关闭；发行或公共边界实质变化前不重复审查。

用户随后确认 Golf 必须纳入新版 `.run`，且 acceptance 与 GUI QA 必须来自同一 simulation
session。新的独占会话 `results/mid360-golf-mapping-release-qa-v3/` 已通过：`acceptance.json`
与 `gui-qa-retry5/qa.json` 同为 session `47a6ff48245c44d1aad22f7a2a1dce57`，MCAP SHA-256 为
`3426d06239042c6a6ca3315532904ee19757d544a1c8ddaa2da822c1076cec85`，并实际保存双 OpenGL
截图、非空像素、逐帧、定位至第 16 帧、回退重建、1x/4x 播放和正常关闭。自动 QA 首次暴露
QImage 枚举、真实中点重建耗时和 Qt framebuffer 重布局三项实现问题；对应聚焦 RED/GREEN
后，最终以共同可见区域的差异判据保存回退证据，不改写或删除任一失败目录。新 manifest
为 `4.0.1`，候选 `.run` 为
`results/stage4/e-run-golf-4.0.1-20260816/slope-sim-stage4-4.0.1-ubuntu24.04-amd64.run`，SHA-256
`51e6d16830fe9f77210dbc377c86c8badf677ef2f354a1b19232b9342e4ea905`。联网干净安装已在
`/tmp/stage4-golf-4.0.1-clean-install/` 以退出码 `0` 完成：`current -> releases/4.0.1`，
manifest/install-state 的 doctor 均为 `files_verified=true`，staging 无子项；从安装后
`bin/slope_sim_stage4_command` 实际运行 dry-run 输出 `validated command: /sim/wheel/command 100Hz`，
并以安装后 Python 成功导入 Golf runner/QA，公开 CLI 显示 `--replay-qa`。

实时 LiDAR `100 ms` 门在空闲、无并行 GUI/eCAL/build 的本机复验中出现一次边界失败：
`tests/integration/test_lidar_worker.py::test_stage4_golf_fifty_frame_budget` 的第 5 帧父端
capture-to-response 为 `104971849 ns`，worker/coordinator/two shards 均仍存活，事件严格为
`job_overrun` 后的 `frame_failed(sensor_overrun)`。相同 nodeid 随后连续两次通过（`8.47 s`、
`9.34 s`）；20 帧 raw coordinator 诊断最大外层时延为 `96.184766 ms`，独立交错双 shard
组件诊断的射线 P95 最高 `11.192445 ms`，当时 1 分钟宿主 load 为 `3.58/8` 核。该证据不足以
归因代码回归，故不修改 `100 ms` 门限、不安装/升级依赖；保留为最终宿主调度 tail-latency
风险，正式发布主机仍须在空闲会话复验。

## 阶段四 A：v2 协议、会话与命令权

| 项目 | 状态 |
|---|---|
| v2 schema/session/authority | PASS：descriptor、确定性 codec、session/world generation、精确 peer count、四态 owner、rebuild/reconnect/late callback 均有自动测试 |
| Python/C++ raw Phase-0 | PASS：严格证据绑定后的 Python↔Python、Python→C++、C++→Python raw bytes/metadata 均 verified；同 topic v1 被硬拒绝 |
| Python/C++ 五消息 golden | PASS：五消息逐 byte 一致；覆盖 `df_mid` command、4+2 WheelState、两点三维 LiDAR 与完整 RTK 三点 |
| 单 LiDAR/三点 RTK runtime | B2 PASS：B1 的模型、三点真值、中心扫描器与候选射线已通过四车型三场地 DIRECT、10 Hz 扫描预算和 240 Hz 物理窗口门；v2 RTK/LiDAR/IMU codec、同帧 identity 工厂、raw-byte publisher、wheel-state authority 投影与 240/100/10 Hz cadence 均有聚焦 TDD。runtime 将已发布帧写入受 session/descriptor/world generation 约束的不可变 Dashboard store；异步 LiDAR 保存 child 原始 bytes，GUI 才解码。四车型三场地 5 秒 headless 性能、offscreen/Xvfb launcher 和 GNOME `DISPLAY=:1` 的真实 5 秒 Dashboard/eCAL 门均通过；后者以 spawned runtime 隔离 Qt，保持 exact-one peer、零 producer drop 和可见 PNG 证据，B2 独立六维复审 P0=0、P1=0、P2=0。 |
| C++ SDK/Recorder | C1 PASS：最新独立六维复验 P0=0、P1=0、P2=0；C2 已完成有界无丢弃队列、原子临时 segment、官方 MCAP v1.4.0 无压缩 writer、RecorderSession、故障安全停车 Supervisor 与五秒真实联合门。Recorder callback 只入队，独立 consumer drain 才写 MCAP，queue overflow 或持久化异常均进入可查询的安全停车状态，并由 Supervisor 终止唯一 Command。writer 以唯一五 topic/schema、session/scene manifest 和原始 protobuf bytes 生成 MCAP，官方 reader 读回测试已通过。`libslope_sim_client` 已通过五 topic metadata、五类 Python raw payload/descriptor、固定 100 ms 安全停车与跨进程单实例合同；唯一 C++ Command、单进程四输出 C++ Subscriber 与 Python/PyBullet runtime 已通过真实 eCAL 联合门。`slope_sim_stage4_recorder` 已在真实 eCAL 依次收齐五个 Python v2 fixture，并在共享 session/world identity 的五秒正式 runtime 中无损录制 Command、WheelState、LiDAR、RTK、IMU 共 1,150 帧后原子完成 MCAP；C 阶段已关闭；最终独立六维复审 P0=0、P1=0、P2=0 |
| 最终真实联合负载 | PASS：已安装 release 的 C++ Command、Subscriber、Recorder 与 Python/PyBullet runtime 完成五秒 eCAL/MCAP 会话，所有 participant clean shutdown | `results/stage4/e-release-c2-joint-20260811/` |

Phase-0 审计链如下：首次授权运行 `results/stage4/phase0-real-authorized-20260810T152526+0800/` 因 C++ participant 缺少 `finalized` 证据而未生成汇总；用户明确再授权后，`results/stage4/phase0-real-authorized-20260810T152832+0800/phase0-result.json` 在旧 verifier 下通过，但该 C++ 结果仍缺少当前严格门要求的 finalization 字段。因此它们都保留为历史证据，不是当前通过结论。修复 C++ participant、排他写入和 verifier 的 descriptor/raw-byte/participant-JSON 绑定后，用户持续授权的 `results/stage4/phase0-real-reauthorized-20260810T173454+0800/phase0-result.json` 通过四场景测试。独立审查随后发现 conflict 汇总未回读 participant/scenario 证据；新增篡改 conflict subscriber 的 RED 后，verifier 现精确绑定 announcer、subscriber 和 scenario JSON，`results/stage4/phase0-real-reauthorized-20260810T180741+0800/phase0-result.json` 在该严格门下通过。第三次审查再要求把非零退出码绑定到独立 process 证据；parent 现在以排他 JSON 写入两端 return code 与 stdout/stderr SHA-256，`results/stage4/phase0-real-reauthorized-20260810T183206+0800/phase0-result.json` 在该门下通过。golden fixture 固定使用 v2 descriptor SHA-256、session `00112233445566778899aabbccddeeff`、world generation `7`，并由 `scripts/generate_stage4_v2_goldens.py` 在默认模式逐 byte 复核；它不会重新执行真实 eCAL。

阶段 A 的历史最终非 eCAL 回归为 `2714 passed, 8 deselected in 150.21s`。本轮 conflict participant 证据绑定 TDD 的 RED 为 `1 failed`（未抛出 conflict evidence 错误），GREEN 为 `15 passed, 4 deselected`；process-exit 证据绑定 TDD 的 RED 为 `1 failed`（未抛出 process evidence 错误），GREEN 为 `16 passed, 4 deselected`；最新真实 eCAL 为 `4 passed, 16 deselected in 18.97s`。第二次独立审查还发现三个未标记 smoke 测试继承 `interface_mode=auto` 并初始化了原生 eCAL；它们现显式使用 local 配置，聚焦回归为 `3 passed`。此前 `results/stage4/a-full-non-ecal-process-evidence-20260810T185222+0800.log` 的 `2722 passed, 8 deselected in 150.24s` 补齐了同次 trace，但后续审查指出临时 hook 会静默吞掉安装失败。为此新增受测试保护的 `scripts/stage4_ecal_init_trace_hook/sitecustomize.py` 和 `scripts/verify_ecal_init_trace.py`：每次加载必须记录成功安装、明确的 eCAL 不可用或硬失败。以冻结的 `STAGE4_PHASE0_BUILD_DIR` 和 `STAGE4_PROTOC` 最终重新运行完整 `not ecal` 回归后，`results/stage4/a-full-non-ecal-process-evidence-20260810T191041+0800.log` 记录 `2726 passed, 8 deselected in 147.95s`；同次 `results/stage4/a-full-non-ecal-init-trace-verification-20260810T191041+0800.log` 通过 `hook_loads=396 unique_pids=394 ecal_unavailable=8 initialize_calls=0`。即 396 次 hook 加载（394 个唯一 PID），其中 388 次成功安装、8 次 eCAL 不可用，原生 eCAL initialize 调用为 0；任何实际安装失败或调用都会使 verifier 非零。此前一次未带这两个显式环境变量的 `not ecal` 尝试产生 18 项缺失构建/protoc 合同的失败，不能作为代码回归。最新独立六维审查为 P0=0，P1=0；阶段 A 已完成。阶段 B1 已完成领域合同：四车型中心 `lidar_link`、Stage4 三点 RTK 真值合同、同帧 parent 快照、中心扫描器和 5,760/20,000 候选射线 profile 已有聚焦 RED/GREEN、四车型三场地 DIRECT 回归及 10 Hz 扫描预算门。B2 当前完成传感器 v2 codec 与同帧投影：RTK、LiDAR 的模型缺失 RED 各为 `1 failed`，对应 GREEN 各为 `1 passed`；传感器 frame factory、raw-byte publisher、wheel-state factory、cadence、五话题 Simulator runtime 和 DIRECT entry 的 RED 均为目标符号缺失，GREEN 各为 `1 passed`。`scripts/stage4_v2_simulation_runtime.py` 现以真实 `df_mid` PyBullet DIRECT 世界运行 24 个 240 Hz 物理步；中心 `lidar_link`、三点 RTK 和 IMU 在 `100 ms` 产生各一条输出，wheel-state 为 10 条，测试替身仅代替 raw eCAL transport。v2 Dashboard store 的 RED 为模块缺失、GREEN 为 `1 passed`；其 runtime 单向写入的 RED 为构造器缺少 store 参数、GREEN 为 `2 passed`。当前相关回归增加 `tests/stage4/test_v2_dashboard_snapshot.py` 后为 `49 passed`。真实 eCAL peer、v2 PySide6 adapter、C++ SDK/Recorder、ROS/replay/export、联合负载和安装器仍未开始，不能由 A 或本轮局部证据替代。

真实 eCAL `df_mid` 5 秒窗口已通过：`results/stage4/b2-v2-runtime-real-ecal-async-retest-20260810T210908+0800/` 的 runtime 记录 `1200` 个物理步、`5.000000 s` 仿真时间、`5.000078 s` wall 时间，wheel 为 `500` 条、LiDAR/RTK/IMU 各 `50` 条；collector 记录一个 command peer、所有输出连续 sequence、单一 session/descriptor/world identity、`callback_errors=0`，runtime 与 worker 均 clean shutdown。此前 `b2-v2-runtime-real-ecal-async-20260810T210608+0800/` 因 runtime 关闭后才读取 command peer 得到 `0`，保留为失败历史证据。本轮补齐异步 Dashboard 数据路径：worker 原始 LiDAR bytes 与预留 identity 进入有界快照，GUI 可在物理循环外按需解码；聚焦 RED 为 `1 failed`（快照缺少 `lidar_payload`），GREEN 为 `1 passed`，Dashboard/传感器/runtime/DIRECT/worker 回归为 `78 passed`。正式入口现持有同一 store，并在结果 JSON 仅暴露 wheel/LiDAR/RTK/IMU 的时间戳和 LiDAR sequence，避免点云 bytes 进入结果文件；该接线 RED 为 `1 failed`（缺少 `dashboard_snapshot`），GREEN 为 `1 passed`。本轮四车型三场地 5 秒 headless 性能门已通过：其 RED 是 `12 failed, 15 deselected in 71.74s`（缺少 `transport_metrics`），最小实现为关闭前冻结 transport 的 published/error/dropped 计数；GREEN 为 `12 passed, 15 deselected in 72.02s`，完整 DIRECT 回归为 `27 passed in 87.60s`。每组均得到 `1200` 物理步、wheel `500`、LiDAR/RTK/IMU 各 `50`、`sim/wall >= 0.95`、实际 raw transport 捕获零丢帧和 worker clean shutdown；这条内存 verified transport 门不替代上述真实 eCAL `df_mid` 证据。v2 PySide6 adapter 已实现并通过 offscreen GUI 测试：其 RED 为 `2 failed`（module missing），GREEN 为 `2 passed`；随后 GUI-side store refresh 的 RED 为 `1 failed`（method missing），GREEN 为 `3 passed`。正式 runtime 的同一 store 注入 RED 为 `1 failed`（unexpected keyword），GREEN 为 `1 passed`，相关回归为 `30 passed, 12 deselected`。独立 launcher 已通过 offscreen 集成测试：其 RED 为 `1 failed`（module missing），GREEN 为 `1 passed`，相关回归为 `20 passed, 12 deselected`；`QT_QPA_PLATFORM=xcb xvfb-run -a conda run -n slope-sim python -m pytest -q tests/integration/test_v2_dashboard_launcher.py` 也以 `1 passed` 证明 Xvfb X11 窗口 smoke 已通过。当前会话为 `XDG_SESSION_TYPE=tty` 且没有 DISPLAY/Wayland，真实窗口验收尚未执行。C++ SDK/Recorder、ROS/replay/export、联合负载和安装器也仍未开始，不能由 A 或本轮局部证据替代。

B2 独立六维审查先发现两个 P1：launcher 未强制 verified eCAL 实时窗口，以及 GUI timer 对同一 LiDAR snapshot 重复解码/重绘。两项均已按 TDD 修复：launcher 现强制 verified peer、墙钟 pacing 与关闭前 wait_idle，针对该门的 RED 为 `1 failed`（`wall_duration_sec=0.0057` 小于 `0.09`），GREEN 为 `1 passed`；GUI store refresh 的 RED 为 `1 failed`（重复 snapshot 仍返回渲染），GREEN 为 `1 passed`，并证明 wheel-only 更新不重解码 LiDAR。审查的 P2 也已处理：Dashboard snapshot 的 descriptor SHA-256 类型注解由 `str` 更正为实际的 `bytes`。真实 GNOME 验收最初暴露同进程 Qt 与 Python/eCAL 运行器的非确定 wheel 丢帧：runtime 本身完整发布而 collector 在 5 秒只收到 `489/500`、`498/500`、`497/500`；减少点数和聚合 scene items 均不足以提供零丢帧保证。修复改为 spawned runtime 子进程与容量 1 的 latest-only immutable snapshot IPC，GUI 进程不持有 PyBullet/eCAL。新 IPC receiver/producer、adapter、launcher 和 gate 聚焦回归为 `12 passed`；`results/stage4/b2-v2-dashboard-real-ecal-isolated-final-20260810T222608+0800/` 在 `DISPLAY=:1` 真实桌面通过：1200 物理步、wall `5.000189 s`、wheel `500`、LiDAR/RTK/IMU 各 `50`、native `published=650/error=0/dropped=0`、唯一 command peer、全输出连续 sequence、双方 clean shutdown。该次复审又发现三项 P1：截图未成为成功硬门、offscreen 可伪装真实桌面、runtime gate 未 fail-closed 复核质量，以及 512px 截图裁切 RTK 文本。它们均已 TDD 修复：请求截图而未形成完整五类遥测会抛错；真实 gate 拒绝 offscreen/minimal 或无 DISPLAY/Wayland；共用 verifier 拒绝错误/drop、非 240 Hz 步数、异常 pacing 或任一非 clean shutdown；Dashboard 固定最小 800px 并让 LiDAR/RTK 自动换行。修复后聚焦回归为 adapter `6 passed`、launcher/peer `11 passed, 1 deselected`；`results/stage4/b2-v2-dashboard-real-ecal-review-fixes-20260810T223713+0800/` 在实际 `DISPLAY=:1` 通过严格 5 秒 gate：1200 步、wall `5.000069 s`、wheel `500`、LiDAR/RTK/IMU 各 `50`、native `650/0/0`、唯一 command peer、所有 sequence 连续、双方 clean shutdown，硬门 `dashboard.png` 为 1600x714 且实查完整显示 wheel、LiDAR sequence、RTK L/C/R、IMU 和 top-view。第二次独立只读六维复审结论为 P0=0、P1=0、P2=0，B2 已关闭。

C1 当前 checkpoint：`libslope_sim_client` 先以 C++17 静态库暴露唯一五 topic metadata。该合同的 RED 为 `1 failed`（CMake target `slope_sim_client_topic_contract_test` 不存在），GREEN 为 `1 passed`；随后新增唯一 `ValidateRawWheelCommand` SDK 判据，直接读取冻结 Python `WheelCommand.bin`，并按 descriptor SHA-256 拒绝篡改 descriptor。其 RED 为 `1 failed, 1 deselected`（validator tool 缺失），GREEN 为 `1 passed, 1 deselected`。第三个 TDD 单元新增固定 100 ms `WheelCommandLease`：其 RED 为 `1 failed, 2 deselected`（租约 test target 缺失），首次实现后因 C++ initializer-list 错误生成错误零数组而运行期失败，修正为显式 size/value 向量构造后 GREEN 为 `1 passed, 2 deselected`，并验证 4+2 命令在边界输出完整零数组。第四个 TDD 单元以真实 `fork()` 验证 `CommandInstanceLock` 在父进程持有锁时拒绝子进程，RED 为 `1 failed, 3 deselected`（lock test target 缺失），GREEN 为 `1 passed, 3 deselected`。第五个单元注册 `slope_sim_stage4_command` dry-run，先复用 SDK 的 raw-byte/descriptor 输入判据；其 RED 为 `1 failed, 4 deselected`（Command target 缺失），GREEN 为 `1 passed, 4 deselected`。第六个单元将 Command 接到真实 eCAL：其 RED 为 `1 failed, 5 deselected`（工具明确拒绝真实发布）；GREEN 为 C++ Command 与 A 阶段 C++ raw subscriber 在本机建立精确一个 peer，150 ms 内 100 Hz 发送 `15` 帧，subscriber 回读的原始 payload 逐 byte 等于 Python golden，result 为 `clean_shutdown=true`。第七个单元将 `WheelCommandLease` 接入该真实 loop：RED 为缺少 active/safe-stop 发布计数，GREEN 为 15 帧中的活动/零命令计数 `10/5`，并保持首帧 Python raw bytes、exact-one peer 和 clean shutdown。第八个单元实现只读 C++ Command Subscriber：RED 为 `1 failed, 6 deselected`（Subscriber target 缺失）；初次真实运行发现 metadata 误把完整 descriptor 长度与 32-byte digest 比较，导致 `received=0/rejected=1`，修正为完整 descriptor SHA-256 比较后 GREEN 为 `1 passed, 6 deselected`，收齐 15 帧并 clean shutdown。第九个单元将原始 payload 判据推广为 `ValidateRawV2Payload`：它按唯一 `TopicContracts()` 表选择五种顶层 protobuf，逐条验证冻结 Python fixture 与内嵌 descriptor SHA-256。其 RED 为 `5 failed, 7 deselected`（通用 validator target 缺失），GREEN 为 `5 passed, 7 deselected`。第十个单元将 Phase-0 probe 与 C++ Subscriber 都接入该通用判据：probe 在保留原有 descriptor/type 预检后只接受冻结五 topic，Subscriber 从 `TopicContracts()` 选择 type 并保持只读。其 RED 为 `4 failed, 12 deselected`（输出 publisher 找不到 peer，因为 Subscriber 拒绝非 command topic），GREEN 为 `4 passed, 12 deselected`；四条输出均在真实 eCAL exact-one peer 下收到一帧 Python fixture。第十一个单元新增单进程 `--all-outputs true` 订阅器：它按 `TopicContracts()` 的 100/10 Hz 导出精确窗口计数，且不创建 command publisher。其 RED 为 `1 failed, 16 deselected`（CLI 不支持 all-output 模式），GREEN 为 `1 passed, 16 deselected`：唯一 C++ Command、C++ 四输出 Subscriber 与 Python/PyBullet DIRECT runtime 建立五话题 verified exact-one peer，100 ms 内收齐 wheel `10`、LiDAR/RTK/IMU 各 `1`，三方 clean shutdown。C1 已关闭；Recorder 尚未开始。

第五次审查修复补充：P1 的 RED 为稀疏 `WheelState` 映射至 LiDAR topic 后错误返回 `0`、合法 LiDAR raw bytes 携带顶层未知 protobuf field 后错误返回 `0`、Command dry-run 对短 session/零 generation/unknown field 三例各错误返回 `0`，以及 LiDAR `LidarPoint`、RTK `Point3d` 的嵌套未知字段各错误返回 `0`；P2 再补 LiDAR `point_num` 不匹配、短 simulation session 与零 world generation 的拒绝测试。共享 `ValidateRawV2Payload` 现统一要求 16-byte session、正 world generation、任意嵌套层无未知字段，并验证 LiDAR 点数一致性；`ValidateRawWheelCommand` 已仅作为映射到该共享判据的兼容 wrapper。七个 RED 合计为 `10 failed`；最终聚焦 GREEN 为 `16 passed, 11 deselected`。此前受影响 Python 回归为 `56 passed in 16.77s`，CMake CTest 为 `4/4 passed`；本轮源码变更后仍需重新运行这些回归。

C1 已关闭；C2 当前完成 RecorderQueue、AtomicSegment、McapSessionWriter、RecorderSession 与真实 `slope_sim_stage4_recorder` 五个 TDD 单元。McapSessionWriter 以 AtomicSegment 的 fsync + rename 发布完成的官方 MCAP；官方 reader 已读回五个 channel、冻结 descriptor schema、session manifest 与逐 byte 原始 payload。RecorderSession 让 callback 只入队，consumer drain 保留原始 sequence/log/publish time；queue overflow 或 writer 异常会锁定 `SafeStopRequired` 并停止后续持久化，之后的 frame 明确返回 `kFaulted`。第五个单元的 RED 为 `1 failed, 31 deselected`（CMake target `slope_sim_stage4_recorder` 不存在）；GREEN 为真实 eCAL 五个 fixture `1 passed, 31 deselected`，并验证新的 `.mcap` 文件头与五 topic 各一帧的独立 result。持久证据 `results/stage4/c2-recorder-real-fixtures-20260811T011500+0800/` 记录 `clean_shutdown=true`、五 topic 各一帧、7,865-byte MCAP 和五个 publisher result。第六个单元的 RED 为 `1 failed, 32 deselected`（故障时缺少 result）；GREEN 为 `1 passed, 32 deselected`：超额 raw frame 非零退出、写入 `safe_stop_required=true`，并且不发布 partial MCAP。第七个单元的 RED 为 `1 failed, 33 deselected`（Command 要求恰好一个 consumer）；GREEN 为 `1 passed, 33 deselected`：runtime 与只读 Recorder 同时消费 command topic 时，唯一 Command 继续发布。随后完整 artifact 回归出现一项既有 LiDAR publisher 退出后的 peer-count 竞态，聚焦重跑为 `1 passed, 33 deselected`；CTest 为 `8/8 passed`。第八个单元实现了 Supervisor：Recorder 的 `safe_stop_required=true` 会终止唯一 Command，聚焦回归 `3 passed in 2.36s`。第九个单元为正式五秒窗口新增 Recorder `--duration-ms`，从冻结五 topic 的 100/10 Hz 合同导出每 topic 精确计数，并允许编排层向 Python runtime 注入同一 session identity。其首个 RED 为 `1 failed, 3 deselected`（Recorder 不认识 `--duration-ms`）；身份注入 RED 为 `1 failed, 3 deselected`（runtime 不接受 `session_id_factory`）。GREEN 为真实 eCAL `1 passed, 3 deselected in 7.69s`：`results/stage4/c2-recorder-command-runtime-five-second-20260811T011537+0800/` 中 Command 发布 `500`（活动/安全停车 `10/490`）帧，runtime 在 1,200 个 240 Hz 物理步中发布 WheelState `500` 和 LiDAR/RTK/IMU 各 `50`，Recorder 以 `clean_shutdown=true` 原子完成 3,671,694-byte MCAP、`recorded_count=1150`。同一源快照的 C2/Supervisor 回归为 `4 passed in 10.08s`，runtime/protocol 回归为 `16 passed in 0.56s`，CMake CTest 为 `8/8 passed`。第五次 C 阶段独立审查发现：运行时把五 topic 都限制为精确一个 peer，因而拒绝 Recorder 与 C++ Subscriber 同时读取四个 output。对此先增加方向感知 peer 合约测试；有效 RED 为 `1 failed, 2 deselected`，在 command `1`、output `2` 且 metadata 全部 verified 时旧实现超时。最小修复后 GREEN 为 `1 passed, 2 deselected`：command 仍要求精确一个远端 publisher，四个 output 则要求至少一个 verified read-only consumer。随后四参与者真实 eCAL/PyBullet 五秒门 `1 passed, 3 deselected in 7.71s`，持久证据在 `results/stage4/c2-recorder-command-subscriber-runtime-five-second-20260811T013849+0800/pytest-tmp/test_real_cpp_recorder_command0/`：runtime `1200` 步、`5.000088 s`、transport `650/0/0`；Command `500`（活动/安全停车 `10/490`）；Recorder 五 topic `500/500/50/50/50`、`recorded_count=1150`；Subscriber output `500/50/50/50`；MCAP `3,671,694` bytes，四方均 clean shutdown。另以 C++ syscall 截获器做两项确定性复验：在 MCAP `write()` 被暂停时，callback `Enqueue()` 仍在 `100 ms` 内返回；在目录 `fsync()` 定点失败时，已 rename 的最终名被撤销。两项均由完整 CTest `8/8 passed in 1.08s` 覆盖，C++ Subscriber/runtime 直接回归为 `5 passed, 29 deselected in 11.12s`。复审再发现 Subscriber 只要曾见过一个 publisher 就可忽略后续竞争 publisher；新增 `test_cpp_subscriber_rejects_a_window_that_observed_multiple_publishers` 的有效 RED 为 `1 failed, 34 deselected in 7.62s`，旧程序在先单、窗口内双 publisher 后错误以退出码 `0` 成功。最小修复在 Single 和 AllOutputs 都冻结 `publisher_count > 1` 的窗口冲突；GREEN 为 `1 passed, 34 deselected in 8.99s`，完整 Subscriber 回归为 `7 passed, 28 deselected in 20.31s`，C2/runtime/report 回归为 `8 passed in 10.10s`，完整 CTest 为 `8/8 passed in 1.05s`。最终复审还发现 AtomicSegment 的 close 失败路径可能二次关闭已释放 FD；将其 C++ test 从 Release 下无效的 `assert` 改为显式 `Require()` 后，close-failure RED 以 `AtomicSegment closed a file descriptor twice after close failure` 失败。实现现在在第一次 close 前失效化文件/目录 FD；同一 CTest GREEN 为 `1/1 passed`，完整 CTest 为 `8/8 passed in 1.05s`。最终复审同时要求 AllOutputs 的真实覆盖：`test_cpp_all_output_subscriber_rejects_competing_runtime_publisher_after_verified_peer` 先由独立 raw eCAL watcher 观测唯一 runtime `/sim/wheel/state` publisher，再启动同型 announcer；测试以非零 Subscriber 退出且无 result 通过（`1 passed, 35 deselected in 9.01s`）。最新 Subscriber 回归为 `7 passed, 29 deselected in 20.48s`，C2/runtime/report 回归为 `8 passed in 10.31s`。最终独立六维审查结论为 P0=0、P1=0、P2=0；C 阶段已关闭。

阶段 D 当前 checkpoint：Safe Reader 已以官方 MCAP reader 验证 footer/summary、唯一冻结 v2 schema、五 channel、manifest identity 与每帧 raw v2 payload；Replay planner 仅把四类 Simulator 输出映射到 `/replay/sim/*`，默认拒绝 WheelCommand。本轮新增独立 `slope_sim_stage4_replay` eCAL participant：它只消费已完成 MCAP、复核用户提供 descriptor 与 session identity，按原始文件顺序发送原始 bytes，并只创建四个隔离 publisher；每个 publisher 必须在 deadline 内连接恰好一个 subscriber，成功后才以排他 result JSON 记录四话题计数与 clean shutdown。注册 TDD 的 RED 为 `1 failed, 36 deselected`（`slope_sim_stage4_replay` target 不存在），GREEN 为 `1 passed, 36 deselected`。真实 eCAL integration 的 RED 为 CMake `unknown target 'slope_sim_client_replay_integration_test'`；GREEN 为 CTest `1/1 passed in 2.03s`：测试使用官方 Writer 生成完成的 output-only MCAP，四个 subscriber 收到的 WheelState/LiDAR/RTK/IMU payload 与源文件逐 byte 一致，子进程干净退出且 result JSON 完整。受影响的 CMake 回归为 `11/11 passed in 3.07s`；Export 与可选 ROS/RViz2/Livox Viewer 验收尚未开始。

阶段 D Export checkpoint：新增独立 `slope_sim_stage4_export`，只通过 Safe Reader 读取完成 MCAP，复核 descriptor/session identity 后解析 `/sim/lidar/points` 的冻结 Protobuf；每帧导出带 `frame_id` 的 ASCII PCD/PLY、量化为 1 mm 的 synthetic LVX2 以及 sidecar。sidecar 固定声明 `synthetic=true`、`quantization_m=0.001`、每点 1-byte padding 与源 MCAP SHA-256，避免将显示格式误称为 Livox 原生记录；导出先写入唯一 `.partial-<pid>` 目录再 rename，原始 MCAP 不会被修改。该单元 RED 为 CMake `unknown target 'slope_sim_client_export_test'`。首次 GREEN 尝试正确暴露构建图遗漏：测试未依赖 Export binary，子进程非零退出；补上 `add_dependencies` 后，真实回读 GREEN 为 `1/1 passed in 0.02s`。测试以官方 Writer 生成完成的 LiDAR MCAP，实际回读 PCD/PLY header 和 2 点计数、synthetic LVX2 magic/负载、sidecar 与 result JSON；随后以两个不同新输出目录连续执行，证明可重试且输入 MCAP 在两次导出后逐 byte 不变。受影响 CTest 回归为 `12/12 passed in 3.10s`；可选 ROS/RViz2/Livox Viewer 验收尚未开始。

阶段 D ROS Bridge checkpoint：主机已有 Jazzy、RViz2、`sensor_msgs`、`geometry_msgs`、`tf2_ros` 与 `rosgraph_msgs`，但尚无 `livox_ros_driver2`，后者继续只属于 `--with-ros` 依赖。先新增无 ROS runtime 依赖的 `ValidateRosSensorPair` 公共判据：它先复用 `ValidateRawV2Payload`，再拒绝 LiDAR/RTK/IMU 任意不同的 session、world generation 或 timestamp，供后续 TF/点云发布共用，禁止最近邻拼接。其 RED 为 CMake `unknown target 'slope_sim_client_ros_sensor_pair_test'`；首次构建暴露 test target 未链接生成 Protobuf 与 `char`/`std::byte` identity 转换边界，均已最小修复。fixture 的实际共享时间为 `1,000,000,000 ns`（而非测试最初假设的 `100,000,000 ns`）；修正后 GREEN 为 `1/1 passed`，并已验证篡改 IMU timestamp 必被拒绝。随后 Bridge 生命周期单元在独立 `STAGE4_WITH_ROS=ON` Jazzy 构建根完成：其有效 RED 为 `tests/stage4/test_cpp_client_sdk.py -k ros2_bridge_tool_is_registered` 的 `1 failed, 37 deselected`，运行时返回 `64`。根因是 `Node::get_name()` 的 `const char*` 与字面量使用 `!=`，比较了地址而非名称内容；最小修复显式构造 `std::string` 后，同一命令 GREEN 为 `1 passed, 37 deselected in 2.90s`，在 `source /opt/ros/jazzy/setup.zsh` 环境中输出 `ros2_bridge=ready`。首个真实 Bridge 单元已接入 eCAL `/sim/wheel/state`：独立 CTest 的 RED 先为 target 不存在，注册后旧 CLI 以 `ROS Bridge requires --dry-run` 退出且未订阅 eCAL；最小实现复用 `TopicContracts`、完整 metadata SHA-256 与 `ValidateRawV2Payload`，将 WheelState 发布为 `/slope_sim/wheel/state` 的 `sensor_msgs/JointState`，逐项验证时间戳、驱动速度与转向角。多帧回归的有效 RED 在首帧已到达 ROS 后再发送第二帧，旧实现因 `accepted != 1` 退出；改为“至少一帧、零拒绝”后，同一 CTest GREEN 为 `1/1 passed in 5.28s`。本轮传感器 Bridge 的 RED 为真实 CTest 中 `Bridge did not subscribe to LiDAR`；最小实现现为三条 raw eCAL subscriber 各自复核完整 metadata 和 `ValidateRawV2Payload`，缓存自有 bytes，仅在同一 timestamp 下调用 `ValidateRosSensorPair`。相同测试 GREEN 为 `1/1 passed in 5.29s`：LiDAR/RTK/IMU 三帧未齐时不产生 ROS 消息，齐全后才在 `/slope_sim/lidar/points` 发布 `PointCloud2`，其时间戳、frame_id、六个字段和首点数据均与冻结 fixture 一致；配对与 Bridge CTest 回归为 `2/2 passed in 5.29s`。目前尚未验证运行时错配身份的拒绝、RTK/IMU/TF 投影、RViz2 与 Livox Viewer 2 smoke。一次完整 SDK 回归中既有 Recorder LiDAR discovery 出现 transient exact-peer 失败；单独及 Bridge 后顺序复验均通过，未归因于 Bridge。

第二个 ROS 投影单元的 RED 为同一 CTest `Bridge did not publish paired ROS RTK and IMU frames`；GREEN 为 `1/1 passed in 5.30s`：同一 verified snapshot 还会输出 `/slope_sim/rtk/state` 的 `PoseStamped`（world 中心点和 heading 四元数）及 `/slope_sim/imu/attitude` 的 `Imu`（base_link roll/pitch 四元数）。相关 CTest 为 `2/2 passed in 5.29s`，Bridge 注册 pytest 为 `1 passed, 37 deselected`。当前剩余 ROS 工作为运行时错配身份拒绝、TF、RViz2 与 Livox Viewer 2 smoke。

第三个 ROS 投影单元的 RED 为同一 CTest `Bridge did not publish paired ROS RTK, IMU, and TF frames`；GREEN 为 `1/1 passed in 5.29s`：Bridge 通过 `/tf` 发布唯一 `world -> base_link`，其平移来自 RTK center，旋转按 RTK yaw 与 IMU roll/pitch 合成。关联的 `ValidateRosSensorPair`/Bridge CTest 为 `2/2 passed in 5.30s`，注册 pytest 为 `1 passed, 37 deselected`；`git diff --check` 通过。当前下一项是把错配 session、generation 或 timestamp 的真实 eCAL 输入作为 Bridge 集成回归，不允许发布 PointCloud2、RTK、IMU 或 TF。

传感器错配端到端回归也已加入同一 CTest：正常 Bridge 完成后，第二个真实 eCAL Bridge 仅收到 world generation 被篡改的 IMU（descriptor、session 和 timestamp 仍有效）。它没有发布 PointCloud2、RTK、IMU 或 TF，且以非零状态退出；该安全回归与正常投影合并后为 `1/1 passed in 10.45s`。该覆盖直接验证 runtime callback 对 `ValidateRosSensorPair` 的拒绝，而非只验证独立工具函数。

第四个 ROS 投影单元的 RED 为 `Bridge did not publish paired ROS RTK, IMU, TF, and clock frames`；GREEN 为 `1/1 passed in 10.46s`：Bridge 在同一已验证 sensor snapshot 发布 `/clock`，其时间精确等于 LiDAR timebase；world-generation 错配路径同样不发布 clock。最终配对/Bridge CTest 为 `2/2 passed in 10.48s`，注册 pytest 与报告合同 pytest 均为 `1 passed`，并且 `git diff --check` 通过。RViz2 二进制已安装但当前没有可连接的 DISPLAY，且 `livox_ros_driver2`/Livox Viewer 2 未安装；这两项真实显示 smoke 仍不可声称通过。

Replay ROS Bridge 单元的 RED 为 `ROS Bridge options are incomplete`，证明原实现既不接受 replay mode 也不会误接 live；GREEN 为独立真实 CTest `1/1 passed in 10.46s`。正式 `--mode replay` 仅订阅 `/replay/sim/*`，仅发布 `/replay/slope_sim/*`、`/replay/tf` 与 `/replay/clock`，但 payload validation 继续按原始冻结 `/sim/*` v2 合同执行。live/replay 及公共 pair 回归为 `3/3 passed in 20.94s`，Bridge 注册 pytest 为 `1 passed, 37 deselected`，`git diff --check` 通过。

阶段 E 安装事务的第一个纯 Python 单元已开始：`stage4_release_state.py` 在实际 release 目录写入前严格比较 schema、version、payload SHA-256、`with_ros` 和每个普通文件 SHA-256。其 RED 为 `2 failed`（模块不存在），GREEN 为 `2 passed`；随后原子 current 激活 RED 为 `AttributeError: activate_current`，GREEN 为 `3 passed in 0.02s`。完整同版本 release 只返回 `activate_only`，损坏或 option drift 返回 `reject`，激活通过同目录临时相对 symlink 加 `os.replace` 完成且不重写 release。此单元未调用 sudo、未写入 `/opt`，`py_compile` 与 `git diff --check` 均通过。

阶段 E 的首个单文件安装器单元已完成：`build_stage4_run.py` 生成内嵌冻结 manifest 的可执行 `slope-sim-stage4-<version>-ubuntu24.04-amd64.run`。其 RED 为 builder 文件缺失，GREEN 为本地 HTTP fixture 下载 payload、SHA-256 校验、唯一 `.staging/<version>-<pid>`、版本化 release 和原子 `current` 激活全部通过；HTTP 仅允许 localhost fixture，其他生产 URL 必须 HTTPS。失败路径 RED 为非本地 HTTP manifest 触发 traceback，导入 `sys` 后 GREEN 返回稳定 `error: release payload is invalid`。release-state 与 `.run` 回归共 `5 passed in 0.62s`，两脚本 `py_compile` 及 `git diff --check` 通过；本单元未使用 sudo、未访问正式网络或 `/opt`。

安装器第二单元已完成。`with_ros` 现作为内嵌 manifest 与 release identity 的强制字段：构建时可指定 true，运行时必须显式传入相同选项，漂移在下载前返回 `installer with_ros option differs from embedded manifest`。全局锁 RED 为持锁第二 installer 仍成功下载；GREEN 使用安装根 `.stage4-install.lock` 的非阻塞 `fcntl.flock`，第二实例在网络/staging 前失败且不创建 release。release-state 和 `.run` 回归为 `7 passed in 1.74s`，`py_compile` 与 `git diff --check` 通过。

同版本损坏拒绝已补到 `.run` 端到端路径：首次本地 HTTP 安装后篡改 release payload，再执行相同 installer 会返回 `same-version release differs, is damaged, or has option drift`，原文件保持篡改状态而不会下载、覆盖或切换。该回归复用已有 release-state TDD 判据；当前安装器/状态测试为 `7 passed in 1.77s`，报告合同为 `1 passed`，`git diff --check` 通过。

安装器恢复与失败事务已扩展为端到端回归：完整 release 的 `current` 被删除后，重复 `.run` 只修复 current 且不改 payload；下载 SHA 不符时 staging 被清空、候选 release 不发布、既有 current 继续指向旧版本。当前 release-state/installer 套件为 `8 passed in 2.40s`，两脚本 `py_compile` 与 `git diff --check` 通过。这仍是本地 HTTP fixture payload 的事务证明，不替代正式项目 payload、锁定依赖或干净机 smoke。

安装器锁定依赖单元已完成：`test_run_installer_downloads_each_locked_dependency_with_license` 的有效 RED 为 builder 拒绝含 `dependencies` 的 manifest（`error: release manifest shape is invalid`）；最小实现后 GREEN 为 `6 passed in 2.95s`。manifest 现允许可选的依赖列表，但每项都必须拥有安全的 name/filename、HTTPS（本地 fixture 除外）URL、SHA-256 与非空 license；重复名称或文件名在构建时拒绝。安装时依赖下载到 release 的 `dependencies/`，发布 manifest 保存 name、license、相对路径与 SHA-256；这些文件同时加入 `files`，故同版本重新执行会校验 payload 与全部依赖，损坏时拒绝覆盖而不是只补 current。release-state/installer 聚焦回归为 `9 passed in 2.95s`，两份脚本 `py_compile` 及 `git diff --check` 均通过。该证明仍只使用 localhost HTTP fixture，不替代正式 HTTPS 锁定清单或干净 Ubuntu 安装 smoke。

安装器传输恢复单元已完成：`test_run_installer_retries_a_transient_locked_dependency_download` 令依赖首个 HTTP 请求返回 `503`，有效 RED 为 `HTTP Error 503: Service Unavailable`；最小实现将单个 URL 的临时传输失败限制为最多三次尝试，GREEN 为 `1 passed, 6 deselected in 0.60s`，第二次请求成功且依赖文件仍经锁定 SHA-256 验证。摘要不匹配不属于可重试传输错误，下载文件会立即删除并失败，不使用备用 URL。release-state/installer 回归现为 `10 passed in 3.52s`，两份脚本 `py_compile` 与 `git diff --check` 均通过；此项同样只证明本地 fixture 行为，正式 HTTPS 清单和干净 Ubuntu smoke 仍未完成。

发布版本路径边界已补齐：`test_run_builder_rejects_noncanonical_release_version` 以 `01.2.3` 触发有效 RED，旧 builder 错误返回 `0` 并生成安装器；最小修复将版本限制为无数字前导零的三段 canonical SemVer，GREEN 为 `1 passed, 7 deselected in 0.05s`。release-state/installer 回归现为 `11 passed in 3.56s`，两份脚本 `py_compile` 与 `git diff --check` 均通过。

唯一交付文件的版本绑定已完成：`test_run_builder_requires_delivery_filename_to_match_release_version` 以版本 `4.0.6` 和错误输出名 `wrong.run` 得到有效 RED，旧 builder 错误返回 `0`；最小修复要求输出名严格为 `slope-sim-stage4-<version>-ubuntu24.04-amd64.run`，GREEN 为 `1 passed, 8 deselected in 0.05s`。本轮 release-state、installer 与报告合同回归为 `13 passed in 3.59s`，`py_compile` 与 `git diff --check` 均通过。

正式项目 payload 单元已完成：`test_run_installer_embeds_project_payload_and_default_resources` 的 RED 为 builder 不认识 `--project-payload`；最小实现只接受绝对、非链接且仅含常规文件的受控目录，将其内容和逐文件 SHA-256 内嵌到 `.run`，安装端只按固定文件表安全解包到 release。第一次 GREEN 尝试发现入口文件执行权限未恢复；解包端按已验证 tar member 权限掩码恢复后，同一测试 GREEN 为 `1 passed, 9 deselected in 0.61s`。release 中的项目入口、默认配置和 `files` 哈希均被实际读取验证；release-state/installer 回归为 `13 passed in 4.23s`，两份脚本 `py_compile` 与 `git diff --check` 均通过。当前只证明临时 fixture payload，正式仓库 payload 组装与干净机 smoke 尚未完成。

正式仓库 payload 组装单元已完成：新增 `stage4_project_payload.py`，其 RED 为脚本不存在；GREEN `test_project_payload_builder_copies_runtime_sources_and_excludes_build_inputs` 为 `1 passed in 0.04s`。组装器只从固定 allowlist 复制 `main.py`、Python runtime 源、C++、Protobuf、URDF、config 与必要 Python/C++/Ubuntu/ROS 锁，生成 `payload-manifest.json` 的逐文件 SHA-256；它显式排除 tests、build、results、reference checkout、`__pycache__` 及已取消的 cache manifest，并在唯一临时目录成功后原子发布目标。真实当前仓库 probe 在 `/tmp/stage4-payload-probe.PjOBBk/project-payload/` 完成：`118` 文件，包含 `cpp/client/stage4_recorder.cpp` 和 `slope_sim/interfaces/v2/simulation_runtime.py`，不含 `packaging/locks/python-package-cache.manifest.json`。payload/release-state/installer 回归为 `14 passed in 4.23s`，三份脚本 `py_compile` 与 `git diff --check` 均通过；payload 尚未与正式依赖构建、doctor/core smoke 或干净 Ubuntu 联合验收。

payload 已补入正式启动入口：`test_project_payload_builder_copies_runtime_sources_and_excludes_build_inputs` 先以缺少 `scripts/stage4_v2_simulation_runtime.py` 与 `scripts/stage4_v2_dashboard.py` 得到有效 RED；将两者加入固定 required allowlist 后 GREEN 为 `1 passed in 0.04s`。真实源树 probe 在 `/tmp/stage4-payload-probe.8ohH6w/project-payload/` 得到 `120` 个文件，两个入口均存在，已取消的 `python-package-cache.manifest.json` 继续不存在。payload/release-state/installer/report 聚焦回归为 `15 passed in 4.24s`，三份脚本 `py_compile` 与 `git diff --check` 均通过；尚未执行依赖构建、doctor/core smoke 或干净 Ubuntu 验收。

staging doctor 单元已完成：现有端到端安装测试先断言 release manifest 的 `doctor.files_verified=true`，有效 RED 为 `KeyError: 'doctor'`；最小实现抽取同一份普通文件/非链接/SHA 校验，下载和内嵌项目解包完成后必须通过该检查才写 manifest、rename release 和切换 current。doctor 结果同时属于同版本 identity，因此完整 release 的后续执行继续只补 current，而不会误判漂移。GREEN 为 `1 passed, 9 deselected in 0.67s`，payload/release-state/installer/report 聚焦回归仍为 `15 passed in 4.24s`，三份脚本 `py_compile` 与 `git diff --check` 均通过。该 doctor 只验证 staging 文件完整性；依赖构建后的 core smoke 与干净 Ubuntu 仍未完成。

共享 release-state 合同已与 `.run` 对齐：新增完整 installer manifest（含锁定 eCAL dependency 与 doctor）的状态测试；将既有 state fixture 升级为当前七字段身份后，旧实现因 `release manifest shape is invalid` 得到有效 RED（`3 failed, 1 passed`）。最小修复后，`stage4_release_state.py` 严格验证 dependencies 的名称、许可证、`dependencies/<file>` 路径、SHA、去重和其与 `files` 的交叉一致性，并要求 doctor 结果精确为 `files_verified=true`；GREEN 为 `4 passed in 0.02s`。payload/release-state/installer/report 聚焦回归为 `16 passed in 4.24s`，三份脚本 `py_compile` 与 `git diff --check` 均通过。

`install-state.json` 身份单元已完成：端到端安装测试先读取该文件，得到有效 RED `FileNotFoundError`；最小实现由 builder 读取当前仓库的 40 位 Git SHA，令 release manifest 与 install-state 同时冻结版本、commit、`with_ros`、依赖摘要与 doctor 结果，并让 install-state 的 `payload_manifest_sha256` 精确匹配最终 `manifest.json` 字节。相同版本重试现在同时比较两份状态文件及全部文件哈希。共享 release-state schema 也纳入 Git SHA；GREEN 为 installer `1 passed, 9 deselected in 0.69s`、release-state `4 passed in 0.03s`，payload/release-state/installer/report 聚焦回归为 `16 passed in 4.30s`，三份脚本 `py_compile` 与 `git diff --check` 均通过。依赖构建后的 core smoke 与干净 Ubuntu 仍未完成。

真实源码 core smoke 已重新取证：独立运行 simulator 不具备 output consumer 时会因 eCAL `send()` 返回 false 记录 error/drop，故不能当作 smoke；正式 gate 必须先启动 verified collector。`run_v2_runtime_ecal_gate` 的 pytest 门为 `1 passed, 9 deselected in 2.36s`，随后持久证据 `results/stage4/e-core-smoke-v2-runtime-20260811T041345+0800/` 以 `df_mid`/flat/`0.1 s` 完成：24 个物理步、WheelState 10、LiDAR/RTK/IMU 各 1；`runtime-result.json` 的 `error_count=0`、`dropped_count=0`，`collector-result.json` 的 `callback_errors=0`、`command_peer_count=1`，双方 `clean_shutdown=true`。eCAL 仍打印缺省 `ecal.yaml`/time-sync 插件警告，但 gate 的消息合同与 producer 计数均通过。该证据验证当前源码的真实核心链，不替代从安装后 release 执行的 smoke。

第九次真实安装前发现 release 入口在导入 `slope_sim.simulation` 时缺少 `pandas`。`test_stage4_runtime_lock_covers_import_time_pandas_dependency` 先因 explicit lock 未包含 pandas 得到有效 RED；最小修复仅在 `packaging/python-environment.yml` 声明 `pandas`，再由 conda-lock 从同一输入生成 `packaging/locks/python.conda-lock.yml` 与 `packaging/locks/python-linux-64.lock`，没有手工拼接 solver 输出。GREEN 为 `1 passed in 0.02s`，release setup 聚焦回归为 `15 passed in 4.02s`，`py_compile` 与 `git diff --check` 通过。

更新后的候选 `results/stage4/e-run-candidate-generated-dir-20260811/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run`（SHA-256 `4c5c971e4caabb916f8ffac922bcc125e854d9c4a7a3b3299fb220aa216398b4`）已在 `/tmp/stage4-public-install-pandas.15ciad/` 完成第九次单实例真实安装，`current -> releases/4.0.0`。release 的 Python 导入 `pandas=2.3.3`、`protobuf=6.33.6` 和 eCAL 模块，五个 C++ 工具存在且 `ldd` 无缺失库，release 内部无解析后的链接。

安装后真实 eCAL core smoke 已完成：从 `/tmp` 以 `PYTHONSAFEPATH=1` 和 `PYTHONPATH=<release>:<repo>` 启动开发 verifier，使 Simulator 与全部 `slope_sim` 导入严格来自 `releases/4.0.0`，避免仓库工作目录覆盖 release。证据 `results/stage4/e-release-runtime-ecal-pandas-isolated-20260811/` 以 `df_mid`/flat/5 秒通过：runtime 为 1,200 步，WheelState 500、LiDAR/RTK/IMU 各 50，published=650、error=0、drop=0；collector 的 command peer=1、四输出同样为 500/50/50/50、callback errors=0，双方 clean shutdown，collector process return code=0。eCAL 的默认 `ecal.yaml` 与 time-sync 插件警告仍存在但不改变 gate 结果。该项关闭本次安装后核心 smoke；干净机迁移与可选 ROS/RViz2 仍未完成。

安装后的最终联合负载已补齐：`results/stage4/e-release-c2-joint-20260811/` 使用 release 内的 `slope_sim_stage4_command`、`slope_sim_stage4_subscriber`、`slope_sim_stage4_recorder` 和已安装 Python/PyBullet runtime 完成 5 秒 eCAL 会话。三个 C++ participant return code 均为 0；runtime 为 1,200 步，WheelState 500、LiDAR/RTK/IMU 各 50，transport published=650、error=0、drop=0；Subscriber 收齐 500/50/50/50，Recorder 的 `five-second.mcap` 为 3,671,690 bytes，精确记录 command/wheel 各 500 与三类传感器各 50，共 1,150 帧，所有 participant clean shutdown。eCAL 默认配置与 time-sync 警告仍不影响结果。该门不替代尚未执行的干净机迁移或可选 ROS/RViz2 smoke。

安装器的 runtime setup 执行基础设施已完成一个 TDD 单元：`runtime_setup.entrypoint` 只能引用 `.run` 内嵌且逐文件 SHA 已校验的 Python 入口；全部锁定 payload/dependency 下载并通过初始 doctor 后，安装器才以固定 `--release-root`、`--dependencies-dir` 与 `--with-ros` 参数执行该入口。setup 产物重新进入 staging doctor 与 release manifest，setup 非零退出时不会发布 release 或切换 `current`。有效 RED 为 `1 failed, 10 deselected`（builder 返回 `release manifest shape is invalid`）；GREEN 为 `1 passed, 10 deselected in 0.64s`；installer/release-state/payload/report 回归为 `17 passed in 4.88s`，三脚本 `py_compile` 与 `git diff --check` 均通过。这只是后续真实 Python/eCAL/C++ 安装脚本的受控执行边界，尚未声称真实依赖构建、已安装 core smoke 或干净 Ubuntu 验收已完成。

E 的 C++ release setup 已完成两个连续 TDD 单元。第一单元新增 `scripts/stage4_release_setup.py`：它只接受 staging 内绝对非链接 release 与其直系 `dependencies/`，以 argv 配置、构建并安装 `cpp/phase0`，随后删除 `.stage4-build`，最后写入稳定的 `share/slope-sim/runtime-setup.json` 供外层 doctor 计入 release identity。有效 RED 为 `1 failed`（脚本不存在）；GREEN 为 `1 passed in 0.47s`，fixture 实际编译、执行了已安装二进制并验证临时 build tree 已删除。第二单元要求 payload 复制此入口，RED 为 `1 failed`（输出缺少脚本），GREEN 为 setup/payload `2 passed in 0.48s`。随后发现 `cpp/phase0` 没有 `install()`，生产 setup 即使构建成功也无法交付 C++ 工具；以现有已验证构建根运行实际 `cmake --install` 的 RED 为 `1 failed, 23 deselected`（`bin/slope_sim_stage4_command` 缺失）。最小 CMake 安装规则只发布 SDK archive/header 与 Command、Subscriber、Recorder、Replay、Export，ROS bridge 仍仅在 `STAGE4_WITH_ROS=ON` 发布。重配、重建后的 GREEN 为 `1 passed, 23 deselected in 3.75s`；受影响 pytest 为 `39 passed, 2 deselected in 6.13s`，Phase-0 CTest 为 `13/13 passed in 3.24s`，四个 E 脚本 `py_compile` 和 `git diff --check` 均通过。当前还没有安全的依赖解包/前缀构建布局，因此不将此 CMake 接线误报为可在干净机完成的真实 C++ 安装。

安装器到 setup 的锁定 dependency 身份也已闭环：下载完成后安装器以独占写入生成 `dependencies/locked-dependencies.json`，仅包含最终 release manifest 已记录的 name、相对文件名、SHA-256 和 license。setup 在创建 CMake build tree 前严格解析该索引，要求 schema、字段集合、安全的 `dependencies/<filename>` 路径、去重以及每个普通非链接文件的实际 SHA 均一致。索引缺失、路径逃逸或摘要不符均以 `error: locked dependency index is invalid` 退出，不能开始构建。有效 RED 为 `1 failed, 1 deselected`（旧 setup 忽略了含 `..` 的 filename 并成功构建）；GREEN 为 setup/installer 聚焦回归 `3 passed, 10 deselected in 1.07s`。扩展回归随后发现空依赖 release 没有创建 `dependencies/`，索引 writer 现先创建该目录；完整相关回归为 `41 passed, 2 deselected in 6.16s`，四脚本 `py_compile` 与 `git diff --check` 均通过。该索引不是 cache 或 materializer；它只描述本轮已锁定下载，后续 unit 将按该清单解压和构建真实依赖。

真实 C++ 源码输入的临时解压单元已完成：setup 仅对索引中 `.tar.gz`、`.tar.xz` 或 `.tgz` 的已验 SHA 文件操作，归档必须有唯一顶级目录，且所有 member 只能是安全路径的常规文件或目录；链接、特殊文件、重复/逃逸路径或损坏归档都会失败。解压输出固定为本轮 `.stage4-build/sources/<dependency>/`，CMake 可消费该目录；安装完成的 finally 会删除整个 build tree，不会把源码树或可重用 cache 留在 release。有效 RED 为 `1 failed, 2 deselected`（fixture CMake 找不到 `sources/ecal/source.marker`）；GREEN 为 `3 passed in 0.79s`，实际 fixture 已以临时解压源码完成 configure/build/install 后确认 release 没有 `.stage4-build` 或 `sources`。

E 的临时依赖前缀已完成三个连续 TDD 单元。第一单元的 RED 是 Phase-0 fixture 报 `protoc must come from the temporary dependency prefix`；最小实现从锁定 Protobuf 归档在 `.stage4-build/prefix` 构建并安装 `protoc`，再将该 prefix、`STAGE4_PROTOC_EXECUTABLE` 和锁定 MCAP include 目录传给 Phase-0，GREEN 为 `1 passed, 3 deselected in 0.37s`。第二单元的 RED 是 `locked eCAL was not installed into the Protobuf prefix`；eCAL 现只接受归档内完整 `.gitmodules` 子模块闭包，禁止下载、FetchContent、系统 package registry 或旧 cache/materializer 填充，并在同一 prefix 构建，GREEN 为 `1 passed, 4 deselected in 0.42s`。第三单元的 RED 是 eCAL configure 找不到 `CMakeFunctionsConfig.cmake`；先构建 eCAL 源码内的 `cmakefunctions` 再构建 eCAL 后 GREEN 为 `1 passed, 4 deselected in 0.46s`。本轮完整 E 聚焦回归为 `22 passed in 6.47s`，四份脚本 `py_compile` 与 `git diff --check` 通过。上述均使用锁定 fixture；正式 HTTPS 归档 manifest 与真实源码安装 smoke 尚未完成。

eCAL 的目标制品路径已完成一手核验：通过官方 GitHub `v6.1.1` release API 确认 `ecal_6.1.1-noble_amd64.deb` 的发布 URL 为 `https://github.com/eclipse-ecal/ecal/releases/download/v6.1.1/ecal_6.1.1-noble_amd64.deb`，官方 asset digest 为 `sha256:fe68188ecf48db98ac28e1917261769b2372418de0e1d92a9c74c79596c30e34`。因此安装器新增的 `.deb` 分支只以 `dpkg-deb --extract` 将已校验文件临时并入本轮 Protobuf prefix，定位唯一 `eCALConfig.cmake` 并显式传给 Phase-0；它不触碰系统 dpkg 数据库，也不恢复已取消的源码 materializer。该单元的有效 RED 为 `locked eCAL deb config was not discovered`，GREEN 为 `1 passed, 5 deselected in 0.38s`。本轮完整 E 聚焦回归为 `23 passed in 7.00s`，四个 E 脚本 `py_compile` 与 `git diff --check` 均通过。真实 `.deb` 内容、运行时库布局及从已安装 release 运行的 core smoke 尚未验收，不能据此声称干净机交付已完成。

Python runtime 的联网安装前置也已按 TDD 固化：`packaging/stage4-private-ca.pem` 是随私有 `.run` 带出的唯一私有 channel 信任根，已与受控源逐字节比较，SHA-256 为 `45d05c1e557e4b38f9c9b2e544364786791bbb9d5338277bf8a43f73c349d335`。payload CA 单元的 RED 为输出文件缺失的 `FileNotFoundError`，GREEN 为 `1 passed in 0.04s`。随后 runtime 单元的 RED 为 CMake `locked Python runtime was not created`；GREEN 为 `1 passed, 6 deselected in 0.30s`：当且仅当锁定依赖索引包含 `micromamba`，setup 使用 payload 内 `python-linux-64.lock` 和该 CA，以 `--no-rc --no-env --ssl-verify --safety-checks enabled` 在 staging 的 `release/runtime` 创建环境，并在 C++ configure 前核验 `runtime/bin/python`。官方 micromamba `2.8.1-0` Linux amd64 可执行发布物也已从 mamba-org release API 复核到 URL、SHA-256 `9689782d863c05a1bf5d2d371ba527104e7a4eb4310c1637d8653b751aed9c82` 和 BSD-3-Clause；它尚未写入正式 manifest 或下载。release setup/payload 回归为 `8 passed in 2.26s`，两脚本 `py_compile` 与 `git diff --check` 通过。正式锁定 manifest、真实安装下载及已安装 core smoke 仍未执行。

唯一交付文件的 payload 边界也已修正：正式 `.run` 在提供 `--project-payload` 时不再要求一个无关的网络 `payload`，只下载 manifest 中显式列出的依赖；release identity 的 `payload_sha256` 改为内嵌 project archive 的 SHA-256。旧的联网 payload 兼容合同继续保留给已有 fixture。该单元的有效 RED 是 builder 对无 payload manifest 返回 `error: release manifest shape is invalid`；GREEN 为 `1 passed, 11 deselected in 0.10s`，验证内嵌入口可安装且 release 中不出现虚假的 `runtime.bin`。完整 E 聚焦回归现为 `25 passed in 7.20s`，四个 E 脚本 `py_compile` 与 `git diff --check` 均通过。

锁定 Python runtime 的 eCAL wheel 安装单元已完成：仅当锁定 dependency 索引包含普通、非链接的 `ecal-python` wheel 时，setup 才在已创建的 `runtime/bin/python` 中离线执行 `pip install --no-index --no-deps`，并立即导入 `ecal.nanobind_core`、`ecal.msg.proto.core`、`ecal.msg.common.core`。本单元最初的 RED 证明旧 setup 没有调用 Python；实现后一次 GREEN 只暴露测试夹具读取了错误的日志目录，修正夹具断言后聚焦 GREEN 为 `1 passed in 0.33s`。完整 E 安装器相关回归为 `25 passed in 7.13s`，四份发行脚本 `py_compile` 与 `git diff --check` 均通过。真实 wheel 与已安装 eCAL `.deb` 的动态库 RPATH 及干净机 launch 仍未验收。

安装后 C++ 动态库闭包单元已完成：旧 setup 只把工具链接到本轮临时 Protobuf/eCAL prefix，并在结束时删除该 prefix。新的真实 CMake 共享库夹具先得到有效 RED：setup 虽返回成功，已安装工具随后因 `libstage4_runtime_support.so.1` 缺失以 `127` 退出。最小实现仅复制 prefix `lib/` 中的锁定共享库和安全的同目录相对链接到 release `lib/`，同时传入 `CMAKE_INSTALL_RPATH=$ORIGIN/../lib`；清理 `.stage4-build` 后同一工具可正常执行，GREEN 为 `1 passed in 0.51s`。完整 E 安装器相关回归为 `26 passed in 7.74s`，四份发行脚本 `py_compile` 与 `git diff --check` 均通过。该契约尚未替代真实 eCAL `.deb` 的完整动态依赖审计或干净 Ubuntu launch。

正式 release manifest 已完成一个独立 TDD 单元：新增 `packaging/stage4-release-manifest.json` 只声明 `micromamba`、`protobuf`、`ecal`、`ecal-python` 与 `mcap` 五个 HTTPS 锁定下载、许可证、文件名和 setup 入口，明确 `with_ros=false` 且没有已取消的网络 payload 或 cache。有效 RED 为 `1 failed in 0.10s`（正式 manifest 文件不存在）；GREEN 为 `1 passed in 0.02s`。其中 Protobuf 采用官方 `v33.6` release asset，而不是 SHA 不同的 tag archive；实际下载复核 SHA-256 为 `16498d7dc7967e9b100632138babd4b86b61592beeccdd556f67539d9c231355`。Windows CDP 后台页与 Linux CA 校验仍分别得到 `ERR_CONNECTION_REFUSED` 和 `SSL_ERROR_SYSCALL`，说明私有 HTTPS channel 当前未监听 `8443`；因此尚未构建或误报真实下载 candidate/install smoke。

在不访问网络的前提下，已从当前受控源树构建候选 `results/stage4/e-run-candidate-rc3v3A/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run`；它的 SHA-256 为 `becd2dbca0742e590ff988062dfbe9b803936fccffb94c0cb27aa6b1d4f75efe`、权限为 `0711`、大小为 `469217` bytes。`python3 -m py_compile` 和 `--help` 均通过，后者只公开 `--install-root` 与可选 `--with-ros`。普通用户的真实 install/core smoke 有意尚未运行：其 Python lock 必须从 Windows private channel 下载锁定 Protobuf，服务恢复前运行只会产生无效失败。

轻量化仓库精简已完成两批已审计删除：以 CodeGraph 和全仓引用检索确认，旧 Conda package/wheel/source cache、materializer、私有 Protobuf Conda producer、container dependency builder、user/network namespace 工具，以及 pip metadata 标准化、runtime tree digest、Conda prefix canonicalization 和 wheel ABI inventory，只在已取消的离线自包含/双根复现链内部互相调用，当前 `.run`/`stage4_release_setup.py`/payload 没有调用方。已删除其 `43` 个脚本、锁、recipe 与测试文件，共 `13,544` 行；保留了当前 release 实际使用的 `cpp-dependencies.lock`、`python.conda-lock.yml`、`python-linux-64.lock`、Ubuntu/ROS 锁与新的 online manifest。删除后检索无 live reference，release setup/payload/release-state/installer/report 回归为 `27 passed in 7.47s`，四个 release 脚本 `py_compile` 与 `git diff --check` 通过；真实源树 payload probe 只含五个当前锁，不含 cache、canonicalization、旧 builder 或 network-namespace 工具。

最新干净 Ubuntu 24.04 联网安装（2026-08-11）修复 eCAL 运行库遗漏的 Ubuntu YAML ABI：`libyaml-cpp-dev=0.8.0+dfsg-6build1` 已加入系统锁并在 runtime setup 前由安装器实际安装。该行为的 RED 是 installer bootstrap 命令缺少此包；最小修复后的聚焦 GREEN 为 installer/release-setup `16 passed in 4.15s`，并完成 `py_compile` 与 `git diff --check`。候选 `results/stage4/e-run-candidate-yaml-system-bootstrap-20260811/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run` 的 SHA-256 为 `923274cdac7e35b08c1ccf3d6c4259a76498b5e187e84304637c5ef120c10866`；唯一真实安装容器 `stage4-clean-yaml-system-bootstrap-20260811` 以退出码 `0` 完成，安装根 `/tmp/stage4-clean-ubuntu24.qg3iui/` 的 `current -> releases/4.0.0` 且无残留 staging。随后以只读挂载和 `--network none` 运行已安装的 `slope_sim_stage4_command --dry-run`，使用 release 内 descriptor 与冻结 WheelCommand fixture 输出 `validated command: /sim/wheel/command 100Hz`；该 smoke 同时确认 `$ORIGIN/../lib` 闭包与锁定 YAML ABI 可被实际加载。该结论只覆盖核心 `--with-ros=false` 交付；可选 ROS/RViz2 smoke 仍未执行。

可选 ROS 依赖的条件下载单元已完成：`test_selectable_ros_run_downloads_ros_dependencies_only_for_ros_install` 的有效 RED 为 builder 尚不识别 `ros_dependencies`；最小实现将其纳入 manifest 校验、与核心依赖做 name/filename 去重，并仅在首次 `--with-ros` 安装时进入下载及 release identity。本轮补齐了测试需要的最小内嵌 payload，使断言实际到达条件下载边界：核心安装不下载 ROS 依赖且 release manifest 的 `dependencies=[]`，而首次 `--with-ros` 安装会下载、校验并冻结该依赖。GREEN 为 `1 passed, 19 deselected in 0.93s`，installer/release-state/release-setup 直接回归为 `39 passed in 16.13s`；四个发行脚本 `py_compile` 和 `git diff --check` 通过。正式 ROS apt bootstrap 与 ROS-enabled 干净机验收仍未实现，不能由此 fixture 替代。

ROS CMake prefix 单元已完成：`test_release_setup_adds_jazzy_prefix_only_for_ros_build` 的 RED 为 fixture configure 报 `Jazzy prefix was not supplied`，旧命令只传入 `STAGE4_WITH_ROS=ON`。最小修复将临时依赖 prefix 与 `/opt/ros/jazzy` 组成唯一 `CMAKE_PREFIX_PATH`，后者只在 `--with-ros` 选择时追加，因此不要求调用者预先 source Jazzy，核心构建不引入 ROS 路径。GREEN 为 `1 passed, 15 deselected in 0.62s`；installer/release-state/release-setup 直接回归为 `40 passed in 11.47s`，四个发行脚本 `py_compile` 与 `git diff --check` 通过。未重建已失效的历史 ROS build root；正式 apt bootstrap 和 ROS-enabled 干净机安装仍未开始。

ROS apt bootstrap 单元已完成：`test_selectable_ros_run_bootstraps_only_locked_ros_apt_packages` 的 RED 是 ROS 安装虽下载 source `.deb` 却没有任何 sudo 调用。最小实现只在 `ros2-apt-source` 这一已校验依赖实际存在时，严格读取 payload 内的 `ros2-apt-packages.lock`，复核 Ubuntu 24.04 amd64、唯一 Jazzy package name、版本和架构，再依序执行 staging source 的 `sudo dpkg --install`、`sudo apt-get update` 与带 `name=version` 的 `sudo apt-get install --yes`；无 source 的通用 `--with-ros` 身份/附件合同不触发 apt。fixture GREEN 为 `1 passed, 20 deselected in 0.74s`；完整回归曾发现该实现错误要求所有通用 ROS fixture 都有 apt lock，最小条件修复后四条受影响回归为 `4 passed, 17 deselected in 2.08s`，完整 installer/release-state/release-setup 回归为 `41 passed in 12.00s`。该单元未调用真实 sudo 或安装 package。

正式 ROS apt lock payload 单元已完成：新增 `packaging/locks/ros2-apt-packages.lock`，冻结 CMake Bridge/RViz2 所需的 Jazzy package 版本，并将它加入 `stage4_project_payload.py` 的受控锁 allowlist。RED 为 payload 输出缺少该 lock，GREEN 为 `1 passed in 0.05s`。本机 APT 元数据及已安装的 `ros2-apt-source_1.2.0~noble_all.deb` 一致给出官方 ROS pool URI、版本和 SHA-256 `0804d9b13db770eb87019be414cd78378835228ad5fa801fc88758596dd8f7e5`；该 pool 只提供 HTTP，但下载内容仍由固定 SHA-256 作为信任根。新的 `test_release_manifest_pins_the_official_ros_bootstrap_and_livox_message_source` 先以缺少 `ros_dependencies` 得到 RED，加入精确 `ros2-apt-source` 与 `livox_ros_driver2=1.2.6` 归档后又发现 Debian 文件名的 `~` 被安全组件规则拒绝；最小修复仅允许 `packages.ros.org/ros2/ubuntu/pool/*` 的外部 HTTP，并把 `~` 作为安全文件名字符，GREEN 为 `1 passed, 2 deselected`。安装器仍拒绝其他外部 HTTP URL。随后 ROS setup fixture 的 RED 依次为 `Livox CustomMsg source was not supplied`、`Jazzy Python environment was not supplied` 与 `Jazzy ament prefix was not supplied`；setup 现只在 `--with-ros` 时校验临时解压的官方 `CustomMsg.msg`/`CustomPoint.msg`、传入 `STAGE4_LIVOX_MSG_SOURCE`，并为 CMake/build/install 子进程显式注入 Jazzy 的 `PYTHONPATH` 与 `AMENT_PREFIX_PATH`。二者的聚焦 GREEN 为 `1 passed, 15 deselected`。Bridge 新增 `/slope_sim/lidar/custom`（replay 为 `/replay/slope_sim/lidar/custom`）官方 `livox_ros_driver2::msg::CustomMsg` 输出，复用同一已验证的 LiDAR/RTK/IMU snapshot；32 位 v2 reflectivity/tag/line 到官方 8 位字段显式饱和而不回绕。嵌套 ROSIDL 子项目已在 `/tmp/stage4-livox-rosidl.jjwvn5/` 从固定 reference 的两份官方 `.msg` 实际生成 `livox_ros_driver2/msg/custom_msg.hpp` 和 `liblivox_ros_driver2__rosidl_typesupport_cpp.so`。最后 `test_ros_apt_lock_covers_livox_message_generation` 的 RED 为 generator lock 缺失；锁定 generator/runtime/C++ typesupport/两套 FastRTPS typesupport 后，与 Jazzy fixture 一起 GREEN 为 `2 passed, 15 deselected`。本轮 manifest/installer/payload/release-setup 直接回归为 `42 passed in 12.20s`，`py_compile`、两份 lock JSON 与 `git diff --check` 均通过。新候选为 `results/stage4/e-run-candidate-ros-livox-20260811/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run`，SHA-256 `7d10dcdebac3241606b475626e2766a45f96b017e0aa9aa9f06018dd07df9b38`，`--help` 及 payload 的 `137` 个文件清单均通过，且实际含接口子包和 ROS lock。历史 ROS CMake 根引用了已清理的 MCAP/eCAL build 输入，无法作为此次 Bridge CTest 的 RED/GREEN；新的 `--with-ros` 候选与单实例安装将重新构建该真实 CTest，当前不得把独立 ROSIDL 编译误报为 Bridge/Livox Viewer 最终通过。

该 ROS 候选的交付报告合同随后通过 `1 passed`，脚本 `py_compile`、两份 lock JSON 与 `git diff --check` 也通过。本机只读核验显示全部 13 个锁定 Jazzy 包的 installed/candidate 版本都与 lock 完全一致。一次新的单实例 `--with-ros` 实装使用本机 Clash `127.0.0.1:7897`，但在已校验 ROS bootstrap 后首次提权处收到 `sudo: a terminal is required to read the password`，安装器按预期以 `error: locked ROS dependency bootstrap failed` 退出；证据为 `/tmp/stage4-ros-livox-real-install.qvYhgj/install.log`。该根没有 `current`、release 或残留 staging，未污染已有核心交付。随后 `sudo -n true` 仍返回 `1`，故真实 ROS 安装、重新构建 Bridge CTest、Livox Viewer 2 与最终 E 审查继续等待一次本机 `sudo -v` 认证；不应把该环境前置失败误报为代码或依赖失败。当前主机没有 `livox_viewer`、`livox_viewer2` 或 `LivoxViewer2` 可执行文件，且项目没有已锁定的 Viewer Linux 下载源，不能擅自下载未审计二进制。

最新人工终端启动的单实例 ROS 安装通过锁定下载、Python runtime、Abseil 与完整 Protobuf 构建后，在 ROSIDL configure 首次暴露真实边界错误：外层把 Conda Python `3.10` 作为全局 `Python3_EXECUTABLE`，而 Jazzy 的 `rosidl_adapter` 需要其系统 Python `3.12` 的 `em` 模块，最终报 `ModuleNotFoundError: No module named 'em'`。安装器以 `error: embedded runtime setup failed` 退出，根 `/tmp/stage4-ros-livox-manual.PgSajx/` 没有 `current`、release 或 staging，故失败原子性保持。TDD 新增 ROS fixture 对 `STAGE4_ROSIDL_PYTHON_EXECUTABLE=/usr/bin/python3` 的断言，RED 为 `1 failed, 16 deselected in 0.45s`；最小修复仅在 `--with-ros` 下由 setup 传入该值，并在 Phase-0 进入 ROSIDL 子项目之前切换生成器解释器，既保留前置 CTest 的锁定 Python `3.10`，也使 Jazzy 生成器使用 Python `3.12`。GREEN 为 `1 passed, 16 deselected in 0.50s`；manifest/installer/payload/release-setup 相关回归为 `42 passed in 12.13s`，四个发行脚本 `py_compile` 与 `git diff --check` 通过。新候选为 `results/stage4/e-run-candidate-ros-python312-20260812/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run`，SHA-256 `1136b435ab1b619d4e87aa15baa63573de33bb449a36f97ed23619a213a72a2a`，大小 `500256` bytes，payload `138` 文件；它尚待唯一的真实 `--with-ros` 安装验收。

第二次真实安装前的作用域修复已完成：上述只写 cache 的 `set(Python3_EXECUTABLE ...)` 不会覆盖顶层已解析的普通变量，故 ROSIDL 子目录仍会调用 Conda Python 3.10。新增 `test_phase0_uses_jazzy_python_in_the_current_rosidl_scope` 先得到有效 RED：`1 failed, 25 deselected in 0.76s`，缺少普通作用域赋值；Phase-0 现先以普通 `set(Python3_EXECUTABLE "${STAGE4_ROSIDL_PYTHON_EXECUTABLE}")` 切换 Jazzy 生成器，再同步 cache。GREEN 为 `1 passed, 25 deselected in 0.51s`，受影响 manifest/installer/payload/release-setup 回归为 `42 passed in 12.11s`，`py_compile` 与 `git diff --check` 通过。新候选为 `results/stage4/e-run-candidate-ros-python312-scope-20260812/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run`，SHA-256 `d2f23e2dfd8da9fbff05d02a68b97178512deec1f9953848006cea691d8e9896`，大小 `500260` bytes，内嵌 payload 为 `138` 个受控文件，`--help` 通过。尚未重启真实安装：当前 `sudo -n true` 明确返回 `sudo: a password is required`，且无运行中的安装/apt/dpkg 进程；用户在同一 Linux 终端执行一次 `sudo -v` 后，才能以该候选在新的空安装根启动唯一 `--with-ros` 实装。

第三次真实安装在同一唯一 staging 中完整编译锁定 Protobuf 后，已确认 ROSIDL adapter 首次使用 `/usr/bin/python3`（Jazzy 3.12），此前 `em` 错误不再出现；随后 `ament_package_xml()` 仍通过已创建的 `Python3::Interpreter` target 回退到 Conda 3.10，因缺少 `catkin_pkg` 失败。失败后根 `/tmp/stage4-ros-python312-scope-install-20260812/` 没有 `current`、release 或 staging，失败原子性保持。新的最小 TDD 单元 `test_phase0_retargets_the_existing_python_interpreter_for_ament_package_xml` 的 RED 为 `1 failed, 26 deselected in 0.85s`；Phase-0 的 ROS 分支现同时将既有 `Python3::Interpreter` 的 `IMPORTED_LOCATION` 指向 `STAGE4_ROSIDL_PYTHON_EXECUTABLE`，使 `ament_package_xml()` 与 ROSIDL adapter 读取同一 Jazzy 解释器。聚焦 GREEN 为 `2 passed, 25 deselected in 0.54s`，发行相关回归为 `42 passed in 16.97s`；系统 Jazzy 解释器实际导入 `ament_package`、`catkin_pkg` 与 `em` 均通过，`py_compile` 和 `git diff --check` 通过。新候选为 `results/stage4/e-run-candidate-ros-python312-ament-target-20260812/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run`，SHA-256 `e7396e3a350ba7401145ba7cd8d2fa7ce909c35ebe194db69aad7b90db749f2d`，大小 `500260` bytes，payload `138` 文件，`--help` 通过；尚待一次新的、唯一的真实 `--with-ros` 安装。

第四次真实安装已越过上述两处 Python scope 问题，并在构建 `rosidl_generator_rs` 时暴露最后一项 Jazzy 环境合同：官方 generator 直接读取 `os.environ['ROS_DISTRO']`，而 release setup 只注入了 `PYTHONPATH` 与 `AMENT_PREFIX_PATH`，因此得到 `KeyError: 'ROS_DISTRO'`。失败后根 `/tmp/stage4-ros-ament-target-install-20260812/` 只有安装锁和空 staging/releases，未生成 `current` 或 release，失败原子性保持。对官方 `/opt/ros/jazzy/setup.zsh` 的最小环境对照确认其导出 `ROS_DISTRO=jazzy`；现有 ROS fixture 的 RED 为 `Jazzy ROS distribution was not supplied`，最小修复仅在 `_jazzy_environment()` 设置 `ROS_DISTRO=jazzy`。GREEN 为 `1 passed, 16 deselected in 0.51s`，并以 `PYTHONPATH=/opt/ros/jazzy/lib/python3.12/site-packages ROS_DISTRO=jazzy /usr/bin/python3` 实际导入 `rosidl_generator_rs` 成功。受影响发行回归为 `42 passed in 18.93s`，`py_compile` 与 `git diff --check` 通过。新候选为 `results/stage4/e-run-candidate-ros-distro-20260812/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run`，SHA-256 `69b2b7ce37eda1a69299ce2d5ef035505e120421896e2e236fbb6bdf88dd4819`，payload `138` 文件，`--help` 通过；尚待一次新的、唯一的真实 `--with-ros` 安装。

第五次单实例真实 `--with-ros` 安装已完成：上述候选在 `/tmp/stage4-ros-distro-install-20260812/` 从唯一 staging 构建并原子发布为 `current -> releases/4.0.0`，release 含 `manifest.json`、`install-state.json` 和 `runtime-setup.json`，无 `.stage4-build` 残留。真实构建已完成 Jazzy ROSIDL 的 C/C++/Python/Rust 生成、`livox_ros_driver2` typesupport、ROS bridge 及其 live/replay 集成测试 target 链接；已安装核心五工具的 `ldd` 没有 `not found`，bridge 在 `source /opt/ros/jazzy/setup.zsh` 后解析全部 ROS 库并以 `--dry-run` 输出 `ros2_bridge=ready`。随后使用 release 内 descriptor 与冻结四个 v2 output fixture 进行两条已安装产物 smoke：live 的 `/sim/* -> /slope_sim/*` 与 replay 的 `/replay/sim/* -> /replay/slope_sim/*` 均由四个 eCAL publisher 发现唯一 bridge subscriber，bridge、publisher 与七个 ROS `topic echo --once` 均退出 `0`；七类输出为 JointState、PointCloud2、Livox CustomMsg、PoseStamped、Imu、TF 和 Clock。证据为 `/tmp/stage4-installed-ros-bridge-smoke-20260812/live-confirmed/` 与 `/tmp/stage4-installed-ros-bridge-smoke-20260812/replay-typed-confirmed/`，各自 `status.txt` 均为 `publisher_rc=0`、`bridge_rc=0`、`echo_rc=0`。真实 RViz2 复验在 GNOME `DISPLAY=:1`、OpenGL 4.6 中启动 3 秒后接收 SIGINT 并以 `0` 退出，证据为 `/tmp/stage4-rviz2-clean-smoke-20260812-final.log`。Livox Viewer 2 仍未安装，且没有已锁定/审计的 Linux 下载源；此项及 E 的最终独立六维审查尚不能标记通过。

第五次审查发现的 ROS bridge 普通 shell 启动 P1 已按独立 TDD 单元修复：旧 release 的原始 bridge 仅有 `$ORIGIN/../lib`，而 Jazzy 的 `libsensor_msgs__rosidl_typesupport_cpp.so` 位于 `/opt/ros/jazzy`，未 source 时直接启动会报动态库缺失。`test_ros_release_installs_a_bridge_launcher_that_loads_jazzy` 先得到有效 RED（`1 failed, 17 deselected in 0.53s`，release 无私有 bridge binary/launcher）；最小实现只在实际安装 bridge 的 ROS release 中将原二进制保留为 `bin/slope_sim_stage4_ros2_bridge.bin`，并以同名 POSIX launcher source 固定 `/opt/ros/jazzy/setup.sh` 后 `exec` 原二进制及原参数。首次 GREEN 暴露 Jazzy setup 与 `set -u` 不兼容，收紧为 `set -e` 后聚焦 GREEN 为 `1 passed, 17 deselected in 0.56s`；setup/installer/payload/reference 直接回归为 `43 passed in 13.48s`，四个发行脚本 `py_compile`、`git diff --check` 与报告合同均通过。候选为 `results/stage4/e-run-candidate-ros-launcher-20260812/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run`，SHA-256 `241cebe0df877a5730d70971049de0b4cfce59b0b4138451441631afae08962c`，大小 `500696` bytes；对既有成功 release 的等价普通 shell 环境（仅 `PATH`、`HOME`，不 source ROS）实际运行同一 setup 和 bridge 返回 `ros2_bridge=ready`。该候选尚未执行新的单实例真实 `--with-ros` 安装：当前 `sudo -n true` 仍要求密码，先启动会在下载/构建后才进入 ROS bootstrap，造成无效重复构建；待 sudo 凭据可用后只在新空安装根完成一次最终 launcher 验收。

第六次单实例真实 `--with-ros` 安装完成了 launcher 与已精确安装 Jazzy lock 的联合验收。`test_selectable_ros_run_skips_sudo_when_locked_packages_are_already_installed` 的 RED 为已安装包仍调用 `sudo`；最小修复在 bootstrap 前以 `dpkg-query` 逐个比对锁定 package 的 `install ok installed <version> amd64`，全量匹配时跳过 sudo，任一缺失、版本或架构漂移仍走原有 fail-closed sudo 路径。该单元与反向 bootstrap 场景 GREEN 为 `2 passed, 20 deselected in 1.31s`，release 相关回归为 `45 passed in 13.42s`。候选 `results/stage4/e-run-candidate-ros-launcher-installed-skip-20260812/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run` 的 SHA-256 为 `0a3eab0842e1d02d1df3b04bffcdbb99606b5f6a03077dca61dd58bd3872fc09`；它在 `/tmp/stage4-ros-launcher-install-20260812/` 的唯一 staging 中完成锁定 Protobuf、完整 Jazzy C/C++/Python/Rust ROSIDL、`livox_ros_driver2` typesupport 和 bridge/live/replay integration targets，并原子激活为 `current -> releases/4.0.0`，release 内无 `.stage4-build`。公开入口在无 ROS 环境变量的 `env -i HOME=/tmp PATH=<release>/bin:/usr/bin:/bin slope_sim_stage4_ros2_bridge --dry-run` 中返回 `ros2_bridge=ready`；核心五工具 `ldd` 无 `not found`。私有 `.bin` 直接 `ldd` 显示 `/opt/ros/jazzy` 的库未解析是预期隔离结果，正式接口必须经公开 launcher source Jazzy。

第七次单实例真实 `--with-ros` 安装完成了无预装 ROS 的 Ubuntu 24.04 bootstrap 验收。候选 `results/stage4/e-run-candidate-conda-direct-20260812/slope-sim-stage4-4.0.0-ubuntu24.04-amd64.run`（SHA-256 `04f93566bc97b0568218046bb9f96e6ec8a2add555abd7475488bc2cf3361783`）在干净容器中经锁定 ROS APT bootstrap、Conda runtime、Abseil、Protobuf 和项目构建后以 exit `0` 发布；`current -> releases/4.0.0`，release 内无 `.stage4-build`。其安装状态记录 `with_ros=true` 与文件 doctor 通过，Jazzy ROSIDL 的 C/C++/Python/Rust 生成、`livox_ros_driver2` typesupport 和公开 ROS bridge launcher 均完成；公开 launcher 的 `--dry-run` 输出 `ros2_bridge=ready`，release 内 descriptor 加冻结 WheelCommand 的 Command dry-run 输出 `validated command: /sim/wheel/command 100Hz`。核心工具动态依赖无缺失；bridge 私有 `.bin` 的 `/opt/ros/jazzy` 库由公开 launcher source 固定 setup 后解析，是预期分层。容器日志和安装根副本保留在 `/tmp/stage4-ros-clean-bootstrap-proxy-outer-conda-direct-{install.log,root}/`。

当前主机的已安装 ROS launcher release 已补做安装后最小运行验收：`/tmp/stage4-ros-launcher-install-20260812/current` 正确解析至 `releases/4.0.0`，manifest/install-state 都记录 `with_ros=true` 和 `doctor.files_verified=true`，release 内无 `.stage4-build`。在完全清空 ROS 环境变量的 `env -i HOME=/tmp PATH=<release>/bin:/usr/bin:/bin` 中，公开 `slope_sim_stage4_ros2_bridge --dry-run` 输出 `ros2_bridge=ready`；五个核心 C++ 工具的 `ldd` 均无 `not found`。以 release 内冻结 descriptor 和仓库冻结 `WheelCommand.bin` 实际运行 `slope_sim_stage4_command --dry-run` 返回 `validated command: /sim/wheel/command 100Hz`。此前较早 `ros-distro` release 的 bridge 原二进制在未 source Jazzy 时缺库只是历史候选行为，不代表当前 launcher release 回归。

Livox Viewer 2 已完成来源、崩溃和兼容启动审计。Livox Downloads Center 的 Mid-360 条目提供官方 Ubuntu `v2.3.0` ZIP（SHA-256 `e6b77ccd8467b4b9842aed537157bb5aded089fe167139aca92d2408f5727af2`）；当前 Linux `Viewer2_2.6.0_Linux.zip`（2026-07-30，SHA-256 `3a1e574d3d73ba0b36460c2a358d08f6c722ae0dc376395ba392ec0d533c7e31`）的 `ViewerConfig.json` 明确列出 `Mid360`。两者在多网卡/代理网络环境中都会在 `/Game/Maps/Viewer` 主界面发生空指针 `SIGSEGV`。官方组织的同类 issue 记录了相同的 `WBP_ViewerMain_C` 崩溃，并给出“关闭代理”或为程序保留单一物理网卡的实例；本机仅清除代理变量仍会崩溃，而以 `bwrap --unshare-net` 让 Viewer 仅见 loopback、无外部网络接口并保留 X11/GPU 后，2.6.0 已完成 `/Game/Maps/Viewer` 地图加载并在检查窗口持续运行。项目提供 `scripts/verify_livox_viewer2_linux.py` 固化该启动方式：它以独立 session 启动 bwrap，只检查同一 launcher PGID 中已进入 loopback-only namespace 且日志含 `LoadMap(/Game/Maps/Viewer)` 的后代，并排他写入实际 bwrap argv、launcher/Viewer PID 和 PGID、host/Viewer namespace 及接口清单；不下载、不内嵌专有 Viewer，也不修改主机网络或 Clash。本次可独立复核证据在 `/tmp/stage4-livox-viewer-no-network-evidence-20260812n.vqAEcI/`：`network-evidence.json` 记录 `launcher_pid=launcher_pgid=3614500`、`viewer_pid=3614508`、`viewer_pgid=3614500`，host 为 `net:[4026531833]`、Viewer 为独立 `net:[4026533037]`，且 Viewer 仅有 `lo`，并明确记录 `viewer_map_loaded=true`；`launcher.log` 记录 `LoadMap(/Game/Maps/Viewer)`，`livox-viewer2-main.png` 为实际主界面截图。`launcher_exit_code=241` 是截图并完成存活检查后，对该独立 launcher 进程组主动发送 SIGTERM 时 Python 的 `-15` 子进程状态映射到 shell 的结果，不计作运行期地图加载失败。该证据只证明 loopback-only 启动和主地图加载，不证明 LVX2 导入、合成/模拟点云显示或真实 MID-360 发现；实时点云显示由已验收的 ROS/RViz2 路径承担。标准 LVX2 离线回放仍是待完成验收，真实设备发现需要网络时仍应在单一物理网卡环境中按 Livox 网络拓扑单独验证。

测试重组后的 B2 关闭回归已完成：短 `100 ms` 运行窗口中，`LidarScanService` 在超过 `100 ms` 预算时先产生 `job_overrun` 告警，但 worker 的迟到响应尚未到达；旧 v2 工厂会在真实 `frame_failed` 后永久保留同刻 RTK/IMU，导致 Dashboard drain 超时。TDD 先证明超时告警不能提前撤销，再将实际迟到 response 消费后的终态明确为 `frame_failed(sensor_overrun)`；v2 工厂只对带精确 `lidar_link` identity 的任意 `frame_failed` 撤销同刻三传感器采样。RED 为 worker 终态事件缺失及 `raycast_failed` 真值残留；GREEN 为 worker/factory/runtime 聚焦 `12 passed`，Dashboard、verified-peer DIRECT、Viewer/报告合同直接回归 `11 passed`。关闭路径不放宽 timeout、不发布不完整三传感器组，且 worker 仍在 idle 后才正常 Stop。随后常规 DIRECT Dashboard smoke 证明另一个根因：只有 verified-peer 路径才按 `240 Hz` pacing，默认路径的 `100 ms` 窗口可在 `6.9 ms` 内结束，使健康首帧偶发跨过 worker 预算。统一每个正式物理步的 pacing 后，该 RED 转 GREEN；五次真实扫描采样均为 `57.2-71.6 ms`、`overrun=0`、完整写入 store，说明扫描本身未越界。

仓库精简与测试重组审计已收口：旧离线自包含链的 `43` 个脚本、锁、recipe 与测试文件经 CodeGraph、全仓引用及当前 `.run` payload/release setup 核验后删除，共 `13,544` 行；它们只服务于已取消的 cache/materializer、双根复现或网络隔离链。57 个旧平铺测试中，54 个迁移至 `tests/unit`、`tests/integration`、`tests/acceptance` 或 `tests/stage4`（2 个逐字节迁移、52 个仅随目录与 fixture/import 边界更新），3 个只验证已取消的离线缓存/网络隔离门而不再保留。迁移后的四个所有权目录有 `86` 个测试文件、无重复 basename；另有 `tests/test_ecal_installation.py` 与 `tests/test_ecal_transport.py` 两个未参与本轮迁移的顶层历史基线，故当前总数为 `88` 个测试文件。`pytest --collect-only -q` 收集 `2759` 项；迁移后直接回归与 `git diff --check` 均通过。移动障碍物、四车型、三场地、真实 eCAL、GUI、MCAP 和安装器门均由迁移后的权威测试或独立真实证据继续覆盖。

最终全阶段独立六维审查（2026-08-12，纯只读）复用 A Phase-0、B2 真实 GUI、C 联合 MCAP、D Replay/Export/ROS、RViz2、Livox Viewer loopback-only 地图加载 smoke、核心与 `--with-ros` 干净 Ubuntu 安装根及上述最终候选的证据。首轮发现两个 P1：报告缺少可审计的 E/全阶段审查记录、架构入口同时声明“已完成”和“仍待实施”；另发现测试统计口径、最终候选指针和轻量计划状态三个 P2。上述五项修正后，第二次独立只读复审确认 `Critical=0`、`Important=0`、`Minor=0`：最终候选及 SHA、历史候选边界、测试统计口径、入口状态和计划索引均与证据一致，取消项未恢复。该审查不将标准 LVX2 离线回放或 Viewer 模拟点云显示标为通过；二者仍待单独验收。

## 前置门禁 P0

| 检查项 | 当前状态 | 证据 |
|---|---|---|
| 阶段三 post-fix 真实 eCAL（主动转向 4+2） | PASS：六 topic active，command/wheel-state `500/500`、四传感器各 `50`，`sim/wall=0.998075`，20 障碍日志零丢失，wheel 原始日志/peer `500/500` 匹配，双方 clean shutdown | `results/stage4/p0-active-steering-4wd-retest-20260804T152217+0800/{runtime-result.json,peer-result.json,interface-logs/}` |
| 阶段三 post-fix 真实 eCAL（差速 2+0） | PASS：五 topic active，command `500`、wheel 原始日志/peer `499/499`，四传感器各 `50`，`sim/wall=0.998075`，20 障碍日志零丢失，双方 clean shutdown | `results/stage4/p0-df-back-2wd-20260804T153125+0800/{runtime-result.json,peer-result.json,interface-logs/}` |

## P0 本地异步 LiDAR Worker

| 检查项 | 当前状态 | 证据 |
|---|---|---|
| production spawn worker、20 障碍、DIRECT、10x5 实时门 | PASS | `results/stage4/lidar_worker_realtime_10x5_final_20260804.log`：954 captures/completes、0 failure/overrun、最大 heartbeat 6.98 ms、最低窗口 sim/wall 0.999961、worker exitcode 0 |
| 点云/worker 直接回归 | PASS | `tests/integration/test_lidar_pointcloud_direct.py tests/integration/test_lidar_worker.py`：69 passed |
| 阶段三 DIRECT 验收 | PASS | `scripts/verify_stage3_interfaces.py`：21/21 passed；20 障碍项 1200/1200、backlog=False |
| 全量非 eCAL 回归 | PASS | `python -m pytest -q -m "not ecal"`：2402 passed、4 deselected、exit 0 |

headless worker 只回传预编码紧凑 payload，父进程不再反序列化或逐点重构点云；Dashboard top-view 请求保持完整 frame 的点云/俯视图原子性。真实 eCAL、RViz2、Livox Viewer 和干净机迁移均未执行，不能由以上本地证据替代。

独立六维审查已完成：需求完整性、逻辑正确性、边界情况、代码质量、测试覆盖和实际运行结果均已复核，最终 `Critical=0`、`Important=0`、`Minor=0`。审查指出的逐窗口 sim/wall 与 child exitcode 两项门禁缺口均已按 RED/GREEN 修复后复审。

本地候选修复（2026-08-03）：TDD 已证明同步 LiDAR 扫描/编码期间的 GIL 调度会推迟 command callback。正式 simulation runtime 仅在实时循环内把 Python thread switch interval 收紧到不高于 1 ms，并在所有正常或异常退出路径精确恢复调用方原值；未修改 wire、transport API、PyBullet 原子扫描或 25 ms oracle。新鲜本地证据为聚焦测试 `4 passed`、transport/verifier 纯测试 `236 passed, 4 deselected`、runtime/lifecycle/realtime `254 passed`、双 LiDAR DIRECT 预算 `1 passed`，以及 `py_compile`、`git diff --check` rc=0。随后一次独立授权的真实 `4+2` 已确认 command callback 恢复，但 output wheel-state 的 `33.030 ms` 接收节拍失败；因此候选修复不能关闭 P0，`2+0` 继续阻断。

启动门修复（2026-08-04）：本次授权真实 `4+2` 未进入正式窗口，原因不是 LiDAR 性能，而是 peer 在 eCAL discovery 完成前写入 `ready`。eCAL 官方 `Publisher.send()` 对零已发现订阅者返回 `False`；runtime 将 warmup 的这些结果记录为 topic error 后，peer 的 start marker 又立即触发 sensor fence，导致 start snapshot 不能 active。`test_simulation_peer_marks_ready_only_after_all_resources_are_discovered` 先以 `discovery_calls == 0` 观察 RED，随后将 peer 的 discovery 移至 ready 前得到 GREEN `1 passed, 159 deselected`；非真实 eCAL 回归为 `247 passed, 4 deselected`。此修复尚未经过新的真实 `4+2`，因此 P0 仍为 FAIL，`2+0` 与 Task 2 继续阻断。

授权复测与日志快照修复（2026-08-04）：下一次获授权的 `4+2` 已确认 peer discovery 可完成，`measurement_start.ack` 中六话题均 active；但 runtime 在测量窗口初始化时引用已删除的 `log_start` 局部变量，报 `NameError` 后退出。该单次运行未重跑，证据为 `results/stage4/p0-active-steering-4wd-retest-20260804T145908+0800/`。随后以 `test_measurement_start_log_sample_uses_capture_snapshot` 先得到 `1 failed, 160 deselected` 的 RED，再将首条 logger 样本改为读取 `start_capture.log_snapshot`，同一命令 GREEN 为 `1 passed, 160 deselected`；非真实 eCAL 的 process-roundtrip/transport 回归为 `248 passed, 4 deselected`，语法编译和 diff 空白检查均通过。这只是本地修复证据，真实 P0 保持 FAIL，仍须新的单次 `4+2` 授权；`2+0` 与 Task 2 继续阻断。

数据面预检修复（2026-08-04）：下一次获授权的 `4+2` 证实 `log_start` 已不再触发，但 peer 的资源 peer count 可在 output 数据面真正可投递前变为非零。`15:14:30.870` peer ready 和共同 start 之后，runtime 约一秒内所有 warmup output `send()` 均返回 `False`，最终仍报 start snapshot 非 active；单次执行没有重跑，证据为 `results/stage4/p0-active-steering-4wd-retest-20260804T151427+0800/`。`test_simulation_peer_waits_for_output_data_plane_before_warmup` 先以缺少 preflight helper 得到 RED `1 failed, 161 deselected`，随后要求 peer 在 warmup 前收到五个 output topic 各至少一帧，GREEN 为 `1 passed, 161 deselected`；非真实 eCAL 的 process-roundtrip/transport 回归为 `249 passed, 4 deselected`，语法编译和 diff 空白检查均通过。这仍不是新的真实运行证据，P0 继续为 FAIL，须再取一条单次 `4+2` 授权；`2+0` 与 Task 2 继续阻断。

主动转向真实通过（2026-08-04）：用户在空闲主机上手动执行 data-plane preflight 修复后的唯一 `4+2`，退出码 `0` 且 verifier 输出 `PASS`。正式窗口 command/wheel-state 各 `500`、四个传感器各 `50`，所有 topic active；`1199` 步、`4.995833 s` 仿真时间、`sim/wall=0.998075`。20 障碍日志为 `1200` 条、零丢失、无持续 backlog、末尾 pending 为零；wheel 原始日志与 peer 为 `500/500` 完整匹配，双方 clean shutdown。独立读取 runtime/peer JSON 与协议 ACK 均与输出一致，证据在 `results/stage4/p0-active-steering-4wd-retest-20260804T152217+0800/`。接口日志保留的预热和故障注入 `publish_failed` 均在正式 start/end snapshot 外，不影响正式 oracle。P0 的 `4+2` 已通过；差速 `2+0` 现在需要独立单次授权，Task 2 仍等待两车型都通过。

差速真实通过（2026-08-04）：独立预检和授权后执行的 `df_back 2+0` 返回 rc=0 与 `PASS`。正式窗口 command `500`、wheel 原始日志/peer `499/499`，四个传感器各 `50`，五 topic active；`1199` 步、`sim/wall=0.998075`、20 障碍日志零丢失、无持续 backlog、末尾 pending 为零，双方 clean shutdown。start/end snapshot 的 `132` 个 error 是数据面就绪前预热累计，窗口差分为零且逐帧 oracle 已通过。证据在 `results/stage4/p0-df-back-2wd-20260804T153125+0800/`。两车型 P0 已通过，Task 2 解除阻断；A-E 仍未启动。

P0 独立六维复审（2026-08-04）：只读审查任务 `/root/p0_six_dimension_review` 未修改文件、未启动真实 eCAL/PyBullet；从需求完整性、逻辑正确性、边界情况、代码质量、测试覆盖和实际运行结果复核 P0。结论 `Critical=0`、`Important=0`、`Minor=0`。它确认两轮窗口中的历史 error 均不跨入正式 oracle，worker 生命周期/IPC、fence、epoch、generation、重建和关闭合同均有实现与测试。剩余 GUI eCAL、RViz2/Livox Viewer 与干净机迁移属于后续阶段范围，不阻塞 P0。

阶段二批量障碍物性能回归修复（2026-08-12）：常规 non-eCAL 回归通过路径发现 `verify_stage2_obstacles.py` 的 `add_50_clear_100` 在单次 `advance_pending_operation()` 中达到 `103-155 ms`，违反既有 `100 ms` DIRECT 硬门。根因不是调度抖动或阈值过紧：临时候选创建已按 `2 ms` 软预算让出，但随后正式候选创建和临时候选删除仍会一次处理整批。TDD 先以慢时钟注入得到两条 RED：正式候选创建必须在发布前让出、临时候选清理必须在发布前让出；另以删除失败注入得到 RED，证明逐项清理初版会丢失残留 body 的所有权。最小修复将规划、正式候选创建和临时清理均保留在同一 pending 事务中分帧推进，正式记录仍只在完整成功后原子发布；已创建正式候选和删除失败临时体均纳入 `pending_body_ids()`/cleanup debt。GREEN 为新增三条事务测试及既有 debt 回归 `5 passed`，协调器的当前车型 AABB 提交前拒绝测试已随单候选规划接口迁移且通过。完整障碍/场景/协调器/阶段二回归为 `187 passed`，v2/LiDAR/Viewer 直接回归为 `24 passed`；`verify_stage2_obstacles.py` 为 `19/19 PASS`，最大时间片 `9.15 ms`，未放宽 `100 ms` 硬门。
